# AFTERIMAGE — Development Roadmap & Backlog
**Document type:** Milestone backlog with acceptance criteria. Companion to `master_plan.md` §9 (which owns the milestone narrative and calendar); this file turns it into checkable work.
**Usage:** items are checked off in commits as they land; an item is *done* only when its acceptance criterion passes in CI or in a recorded playtest. Scope changes to a milestone are amendments here, made at milestone boundaries. This file is the day-to-day truth of "where are we."

---

## Definition of Ready to Code (all true before M0 begins)
- [x] Master plan ratified (`master_plan.md` v2)
- [x] Technology decisions locked under change control (`tech_guidelines.md`)
- [x] Foundation layer specified (`foundation_blueprints.md`)
- [x] Art direction + UX charter ratified (`art_direction.md`, `ux_charter.md`)
- [x] Story canon fixed; slice mission ground-truth outlines written (`story_bible.md`)
- [x] Godot 4.3.x exact patch chosen and recorded in `/docs/ENGINE_VERSION` (Pass 1: 4.3.0-stable)
- [~] GitHub Actions runner plan confirmed — Linux reference CI landed Pass 1 (`.github/workflows/ci.yml`); Windows/macOS nightly runners still open

## M0 — Foundations (w1–6)
*Exit (master_plan §9): walking skeleton — inputs recorded in a stub scene replay tick-perfect on every platform target. **Not yet reached** — Passes 1–2 delivered every M0 checklist item below; the exit criterion itself needs TruthSim (Pass 3) to have something real to replay. See `dev_log.md` for verification caveats: this sandbox has no Godot binary, so everything below is verified by lint/format tooling + algorithmic cross-checks locally, and by CI's real headless Godot run on push (confirmed green both passes).*
- [x] Repo scaffold per master_plan §5.8; lint config (gdtoolkit), CI pipeline skeleton — **AC:** a PR with a style violation fails *(Pass 1; gdlint/gdformat --check both wired into CI and verified locally against every source file)*
- [x] Fixed-point helpers + PRNG (xoshiro128**, splitmix64 seeding) — **AC:** unit tests vs. reference vectors green *(Pass 1: `src/core/fixed_math.gd`, `src/core/prng.gd`; reference vectors cross-derived from an executable Python transliteration, not hand-guessed)*
- [x] EventBus (typed, queued dispatch) — **AC:** ordering/reentrancy unit tests *(Pass 1: `src/core/event_bus.gd`, 11 tests incl. reentrant-publish-is-queued and self-unsubscribe-mid-dispatch)*
- [x] GameStateStore + save serialization + `schema_version` + migration harness — **AC:** save→load→save byte-identical fixture test *(Pass 2: `src/core/game_state_store.gd`, `src/core/save_migrations.gd`, `src/core/save_system.gd`; a real exercised v1→v2 migration, gzip-compressed JSON via `PackedByteArray.compress()`, and an explicit byte-identical save/load/save fixture test)*
- [x] Predicate evaluator (18 operators, blueprints §2) — **AC:** full operator test matrix; unknown-operator rejection *(Pass 1: `src/core/predicate.gd` + `src/core/world_query.gd`, 31 tests across evaluate() and validate())*
- [x] Fixed-tick harness with `InputFrame` recording/replay — **AC:** determinism corpus v0 (3 stub runs) re-simulates hash-identical on Linux/Win/mac in CI *(Pass 2: `src/core/{fixed_tick_clock,input_frame,replay_log}.gd`; 3 fixtures in `tests/corpus/` replayed through a disposable `StubSim` placeholder — proves the record/replay/hash/compare mechanism catches both agreement and divergence in-process. Cross-platform Linux/Win/mac agreement still needs the multi-OS CI runners tracked below)*
- [~] Determinism CI job (PR subset + nightly full) — **AC:** an intentionally seeded divergence is caught by the job *(Pass 2: divergence-detection proven by unit test (`test_digest_is_sensitive_to_an_extra_frame`), running as part of the normal fast suite. Still open: a dedicated CI job, the PR-subset/nightly-full split, and Windows/macOS runners — deferred until the corpus is large enough for that split to matter, consistent with the Windows/macOS item already open from Pass 1)*

## M1 — Truth Skeleton (w7–12)
*Exit: a fight can be recorded and replayed tick-perfect, and the fight is already fun. The tick-perfect half is reached as of Pass 7 — every item below is wired together and deterministic. "Already fun" is not, and structurally can't be from engineering passes alone: it's a human-playtest verdict against a real rendered build, which needs real art/UI (post-Pass-20 scope) and real testers. All checkboxes below are complete for what this sandbox can deliver.*
- [x] TruthSim actor model (integer mm positions, entity IDs) + grid collision & swept casts (tech §3.4) — **AC:** collision unit suite; corpus runs with movement *(Pass 3: `src/sim/{actor,actor_registry,collision_grid,swept_collision,truth_sim}.gd`. Swept circle-vs-AABB verified against an executable Python reference using exact Fraction arithmetic before porting — same rigor as the Pass 1 PRNG. "Corpus runs with movement" satisfied narrowly: TruthSim has its own direct correctness tests rather than replacing the Pass 2 StubSim corpus, which still proves the record/replay/hash mechanism on its own terms — see dev_log.md for the reasoning)*
- [x] Line-of-sight + sound propagation (occlusion grid, room/portal BFS) — **AC:** deterministic sense tests; noise rings render from truth events *(Pass 4: `src/sim/{line_of_sight,sound_graph}.gd`. LOS reuses CollisionGrid's blocked-cell data via Bresenham's line algorithm — verified against a Python reference before porting. Sound propagation is a room/portal graph with subtractive integer attenuation, solved by iterative relaxation (Bellman-Ford-style) rather than a priority-queue Dijkstra, appropriately scoped to small room counts. Angular vision cones deliberately deferred to Pass 5 — no consumer exists yet, and building the cone math now risked premature complexity (see dev_log.md). "Noise rings render from truth events" is not yet built: that's percept-side UI (Pass 8+), this pass delivers the truth-layer propagation it would read from)*
- [x] Sentry + Professional AI (utility patrol/investigate/engage/flee/report) — **AC:** paranoid/credulous bot stubs finish the graybox room *(Pass 5: `src/sim/{vision_cone,ai_utility,ai_archetype,ai_agent}.gd`. Vision cones — deferred from Pass 4 — verified against a Python reference across nine clean-angle cases (30°/45°/60°, whose cos² values are exact rationals) before porting, with a numerically-derived overflow bound enforced by assert. All five master_plan §5.5 states implemented as pure utility-scoring functions; FLEE is wired but inert (threat_level defaults to 0) until Pass 6 gives it real combat data to score against. Sentry/Professional differ only in perception parameters and REPORT usage — tactical behaviors ("flanks, checks corners") need pathfinding/combat that don't exist yet, so building bespoke behavior trees for them now was out of scope. The literal AC (bots finishing the graybox room) awaits Pass 7's room; this pass delivers the AI decision-making the bots will drive, with its own direct correctness tests instead)*
- [x] Combat verbs v1 (move/sprint/crouch/lean/aim/fire/reload/takedown/throw/Focus) — **AC:** every §4.9 tuning-checklist line has a recorded pass *(Pass 6: `src/sim/{movement_profile,weapon,combat_resolver,focus_state,lean}.gd` + InputMap actions in `project.godot`. Every verb built standalone-and-tested, not yet wired into TruthSim.step() — that wiring is Pass 7's literal job ("graybox test level wiring TruthSim+AI+combat"), the same order Pass 4/5 shipped LOS/sound and vision/AI ahead of their consumers. Fire and takedown reuse Pass 4/5's LineOfSight/VisionCone primitives rather than new geometry; noise-hearing (sprint/gunfire/throw) uses a distance-only v1 model pending a real room to route through SoundGraph. The literal AC (a *recorded pass* per tuning-checklist line) needs actual playtesting against a real build, which doesn't exist before Pass 7's graybox room — not satisfiable yet, same class of deferral as Pass 5's bot-stub AC. Focus's state machine (activation/duration/cooldown) is built and tested; its "0.4× time" effect is deliberately not — TruthSim's tick rate never varies (determinism contract), so that's a presentation-layer trick with no renderer to attach to yet)*
- [x] Graybox room (Zálesí ruins fragment) — **AC:** 3 external testers rate the sober fight "fun" without prompting on distortions (they don't exist yet) *(Pass 7: `tests/fixtures/graybox_room.gd` (a code-defined perimeter-walled room, not a real Godot scene — no level-authoring pipeline exists before Pass 13) + `TruthSim`'s combat/AI wiring: player movement, aim/fire/reload/takedown/throw/focus, and every spawned AI's perception-and-fire-back all resolve for real now (`src/sim/truth_sim.gd`), with a bot harness v0 (`tests/fixtures/bot_inputs.gd`, `tests/unit/test_bot_harness.gd`) soak-testing an aggressive and a cautious bot through a full encounter, hand-traced and asserted exactly. The literal AC (3 external testers judging "fun") needs real human playtesters against a real, rendered build — categorically outside what an engineering pass in this sandbox can satisfy, the same class of deferral as Pass 5/6's bot-stub and tuning-checklist ACs. What's real: a fight that resolves deterministically, replays tick-perfect, and is driven by every system Pass 3-6 built standalone)*
- [x] WitnessSystem v1 (who truly saw what) — **AC:** witness log matches hand-computed scenario fixtures *(Pass 5: `src/sim/witness_system.gd`, reusing VisionCone + LineOfSight against a roster of candidates. Every fixture in `test_witness_system.gd` is hand-computed geometry, not a black-box comparison)*

## M2 — The Split (w13–17)
*Exit: a phantom fools a playtester once, and the replay proves it.*
- [x] PerceptRenderer boundary (read-only truth views; op decorator pipeline) — **AC:** static check: no percept-path writes to truth (CI lint on module imports) *(Pass 8: `TruthSim.capture_percept_snapshot()` (`src/sim/truth_sim.gd`) is the one seam where truth hands data upward, as plain values (int/bool/Vector2i) copied out of each Actor — never an Actor/TruthSim reference — so "read-only" is a type-shape guarantee, not just a naming convention. `src/percept/{percept_op,percept_renderer}.gd` is the decorator pipeline (`PerceptOp.apply()`, `PerceptRenderer.render()`), with no real ops yet (Pass 9's job — `SubtitleDrift` etc. need a real percept-side renderer to be worth building against). The AC's literal static check is `tools/percept_truth_boundary_lint.py`, wired into CI's lint job: it derives its denylist from every `class_name` under `src/sim/` at scan time (so it never goes stale) and fails if any `src/percept/` file references one — verified locally against both a clean pass and a deliberately-reintroduced violation before committing)*
- [ ] Distortion shader library skeleton + photosensitivity caps (tech §4) — **AC:** caps unit-verified
- [x] Ops: `SubtitleDrift`, `AudioSwap`, `PhantomAudio`, `PhantomEntity` — **AC:** each op's Ground response + Theater disclosure works in the graybox *(Pass 9: `src/percept/{distortion_op,subtitle_drift,audio_swap,phantom_audio,phantom_entity}.gd`. Each op ships tier/cost/dramatic-intent/fairness-tags metadata (§4.2's table) and a `resolve_grounded()` — the Ground-response behavior as data, ready for Pass 10's Ground verb to call once it exists. `PhantomEntity` obeys Charter rule 1 ("never deals damage, never blocks") structurally, not just by convention: it only ever appends to a percept-side Dictionary `src/sim/` never reads back (Pass 8's boundary). `AudioSwap`/`PhantomAudio` operate on real truth-sourced `sound_events`, newly exposed from the same noise events Pass 7's AI hearing already computed. `SubtitleDrift` operates on an optional `subtitle` key with no real producer yet (no dialogue system before Pass 15) — tested against a synthetic snapshot, the same "no consumer yet" discipline used throughout this log. The literal AC (Ground response + Theater disclosure "working in the graybox") needs the Ground verb (Pass 10) and Theater (Pass 14) to actually exist — not satisfiable yet, same class of deferral as Pass 5/6/7's ACs. What's real: four op classes that compose correctly through Pass 8's pipeline over both synthetic and real captured TruthSim snapshots, proven by a dedicated integration test)*
- [x] Ground verb (timings, costs, on-empty behavior, §4.6) — **AC:** input-hold + toggle variants; suspicion tick stub on observed Ground *(Pass 10: `src/sim/ground_state.gd` (`GroundState`) is a plain "requested: bool this tick" hold-duration state machine — 75 ticks (2.5s baseline) — deliberately agnostic to whether the caller derived that bool from a continuous hold or a hold-to-toggle accessibility scheme (§4.16), since both produce an identical tick stream from this class's point of view; that satisfies "input-hold + toggle variants" without needing two implementations. `TruthSim.step()` blocks player movement/combat for any tick Ground is requested ("stationary and defenseless," §4.6) and, on the exact completing tick, checks whether any AI could see the player (reusing Pass 5's VisionCone/LineOfSight directly, not `AiAgent.perceive_and_decide()`, so the check never mutates an agent's own memory as a side effect) and publishes `GroundObserved` (weight 2, §4.7's own example) as the suspicion-tick stub — no real SuspicionGraph exists yet (Pass 16+) to consume it. `GroundCompleted` always publishes on completion regardless of observation, which is what "on empty grounding still tells you something true" actually requires. Fatigue cost (+1.5/use, §4.6) isn't billed yet — MindModel is Pass 11's job; `GroundState.use_count` is tracked now for that billing later, the same "state now, consumer later" pattern as Focus's `activation_count`)*
- [x] Clarity Mode stub (vignette flagging) — **AC:** all four ops flagged correctly in real time *(Pass 10: `src/percept/clarity_mode.gd` — `ClarityMode.active_flags(active_ops)` reports every active `DistortionOp`'s class/tier, verified against all four Pass 9 ops (`SubtitleDrift`/`AudioSwap`/`PhantomAudio`/`PhantomEntity`) active simultaneously. Pure data-layer stub: no vignette rendering exists (no Godot scene has been built in any pass so far), and the on/off toggle is a settings-system concern (Pass 19). `PerceptRenderer.render()` also gained the actual Ground-resolution wiring this pass: it reads `ground_just_completed` off the truth snapshot and calls `resolve_grounded()` instead of `apply()` on every active `DistortionOp` for that one tick — §4.6's "resolves all active ops in scope simultaneously," implemented for real)*
- [x] Replay Theater v0 (dual-pane, scrubber, op timeline; checkpoint snapshots) — **AC:** scrub-to-any-point ≤ 100 ms on Deck-class hardware *(Pass 14: `src/percept/replay_theater.gd` (`ReplayTheater`) — data model only, no scrubber UI exists before Pass 19. Takes a `scenario_factory: Callable` (never a typed `TruthSim` reference, so this stays outside the percept/truth boundary lint's denylist entirely, confirmed clean) plus a `ReplayLog`, and does one forward pass caching `capture_percept_snapshot()` at every tick, turning `truth_view_at(tick)` into an O(1) array lookup — the actual mechanism behind "scrub-to-any-point," even though the ms-level hardware benchmark itself isn't measurable in this sandbox (no Deck hardware, no profiler; deferred to a real profiling pass, Pass 19+, same class of deferral as every prior pass's hardware-dependent AC). This is a full-tick-cache, not a sparse checkpoint-plus-partial-resume scheme: `TruthSim` has no `capture_full_state()`/restore mechanism to resume mid-simulation from yet, so "checkpoint" here means "the whole timeline is precomputed once," a real memory-for-speed trade, documented as such rather than silently assumed free. `src/percept/op_timeline_span.gd` (`OpTimelineSpan`) is the "every DistortionOp as an annotated span" data shape (§4.12) — plain data, constructed directly for now; deriving spans automatically from a live Director + lifecycle events is a later pass's wiring job, the same gap Pass 13 already flagged for turning purchase records into live op instances. `percept_view_at()`/`dual_view_at()` delegate to Pass 8's already-proven `PerceptRenderer.render()`, taking real op instances as a parameter rather than reimplementing dispatch. 11 new tests (`test_replay_theater` 7, `test_op_timeline_span` 4), including a real correctness proof (the cached tick-5 snapshot matches an independently re-simulated fresh 5-tick run, not just "returns something").)*
- [ ] Art direction lock: distortion VFX + Grounding reveal (art_direction §4) — **AC:** the reveal reads as "beautiful" in a 5-person blind AB vs. a plain fade

## M3 — The Mind (w18–22)
*Exit: two testers with different playstyles get measurably different distortion profiles from the same mission.*
- [x] MindModel with §4.4 constants + band effects — **AC:** arithmetic unit tests; worked-example fixtures from the plan reproduce exactly *(Pass 11: four small composed state classes — `AcuteStressState`, `FatigueState`, `MoralInjuryState`, `IdentityStrainState` — one file per variable rather than one mega-class, since each variable's own §4.4 gain/decay methods alone approach gdlint's 20-public-method cap; `MindModel` composes all four and owns the one cross-variable rule the plan actually needs (the hub-rest floor: `max(fatigue, moralInjury) × 0.3` raises acute stress toward that floor, never lowers it). Every named gain/decay event from §4.4.1–§4.4.4 is its own method (e.g. `gain_moral_injury_civilian_casualty()`), each hand-verified against a Python Q16.16 reference before porting — including one real rounding surprise the reference caught before it could reach CI: the hub-rest floor's `× 0.3` factor has no exact Q16.16 representation, so `max(40,60)*0.3` lands on fx `1179660`, not the `FixedMath.from_int(18)` (`1179648`) a naive test would have asserted. Band thresholds (Quiet/Murmur/Loud/Crisis) live once, centrally, in `MindModel.band_for()` — not duplicated per variable — to keep the "cross-file enum" surface to the one place already proven safe in this codebase, after Pass 6's static-typing gotcha with bare vs. qualified enum names. 25 new tests, all fx-exact (no float tolerance). Band **effects** — tier unlocks, VFX flags, dialogue biasing, dashboard rendering — are not wired: they need DistortionDirector (Pass 12), a real Hub (Pass 18), and UI (Pass 19) respectively, so this pass delivers the constants + arithmetic + band computation the AC actually asks for, with every downstream consumer explicitly deferred. Substance/tool modifiers (§4.4.5) are a separate M3 line item below, out of this pass's scope — they need a multi-day effect window only a real hub calendar can drive, and no call sites bill into MindModel yet (Focus's acute-stress cost, Ground's fatigue cost, combat's moral injury, the calendar's identity strain and per-day decays) — same "state now, consumer later" pattern as every prior pass.)*
- [x] DistortionDirector (budget accrual, deck loading, caps, cooling, seeding) — **AC:** same seed+state → identical purchase log, in corpus *(Pass 12: `src/percept/distortion_director.gd` — deliberately takes only plain data (Dictionary[String,int] fx values, Strings, ints), never a `MindModel`/`src/sim/` reference, so it's fully decoupled from the truth layer and lives safely under `src/percept/` with zero percept/truth boundary risk. `compute_budget()` implements §4.3's `B = base(sceneType) × (0.4 + Σ wᵥ · v/100)` exactly, hand-verified against a Python Q16.16 reference (worked example: combat scene, 4 mind values + weights → budget 26). `purchase_one()` is a weighted pick from a `DeckEntry` deck via a seeded `Xoshiro128StarStar` stream (`seed_for(run_seed, mission_id, scene_id)`), respecting the global density cap (max 3 concurrent), tier-3+ 20s/600-tick spacing, and Ground-resolution weight decay (`notify_ground_resolved()`, halving an op class's selection weight, compounding on repeats — §4.3's "the mind that gets caught repeating a trick changes tricks"). `apply_death_cooling()` implements the ×0.6/×0.3 Charter rule 7 cooling. The AC's "same seed+state → identical purchase log" is proven directly (two independently constructed Directors, same seed, same deck/mind-state trace, byte-identical `purchase_log()`) rather than via new corpus fixture files — the determinism-corpus mechanism (Pass 2) is TruthSim-replay-specific; this is the equivalent guarantee for the Director's own decision stream. Legality itself ("an illegal purchase is a build failure, not a runtime clamp") is FairnessAuditor's job on the deck before it reaches the Director, not re-checked per purchase. 15 new tests (`test_deck_entry` 2, `test_distortion_director` 13), several reusing test_prng.gd's already-CI-pinned seed=0/seed=1 vectors rather than re-deriving PRNG output by hand. One arithmetic assumption caught before commit, not after (this pass's own "double-check any 'should agree' premise" discipline, learned the hard way in Pass 11): a naive `floor(10 × 0.4) = 4` expectation is actually `3` once 0.4's inexact Q16.16 representation is accounted for.)*
- [x] Fairness auditor v1 (static deck/level checks) — **AC:** each Charter rule has a fixture deck that fails it *(Pass 12: `src/percept/fairness_auditor.gd` — statically validates a deck (any Array of duck-typed op-like objects: real `DistortionOp` instances or test doubles shaped like one) against all 8 Fairness Charter rules (§4.5), each with its own dedicated fixture test proving the auditor actually catches that specific violation, per the AC. Several rules (1, 3, 6) are also structurally guaranteed elsewhere (percept/truth boundary, taxonomy design, `ClarityMode`'s `is DistortionOp` check) — this auditor adds an independent static check on top of each, the same belt-and-suspenders pattern Pass 8 established for the percept/truth boundary itself, not a replacement for the structural guarantee. Rules 2 and 4 (EntityMask, GeometrySwap) are checked by op-class-string matching even though neither class is implemented yet (Pass 9 built 4 of the 10 taxonomy classes) — the auditor is duck-typed and doesn't require the real subclass to exist, only a correctly-shaped deck entry, so it's ready the moment those classes land. Rule 7 (density/cooling caps) is the one deck-*level*, not per-entry, check: a mission's declared `encounter_cap` must not exceed the Director's global `MAX_CONCURRENT_OPS`. Rule 8 (Theater never lies/forges) is interpreted as: every entry's `dramatic_intent` must be one of the four named values (dread/doubt/grief/paranoia) — the Theater/debrief (Pass 14+) can't honestly disclose an op's intent if the op never declared a real one. 11 new tests, one per rule plus unknown-op-class and multi-violation aggregation checks.)*
- [x] Hub v1: calendar/sleep loop, mind dashboard (worksheets) — **AC:** a full sleep-debt week produces the §4.4.2 fatigue trace *(Pass 18: new top-level `src/hub/` directory. `src/hub/hub_calendar.gd` (`HubCalendar`) sequences day-level sleep choices (SKIPPED/PARTIAL/FULL) directly into Pass 11's `FatigueState` methods — never duplicating §4.4.2's constants, only calling the class that already owns them. The AC is satisfied literally: seven consecutive skipped-sleep days trace `12, 24, 36, 48, 60, 72, 84` exactly (whole integers, zero fixed-point rounding risk), and a mixed week (skipped/partial/full) traces correctly too. `Block` (evening/night/morning) is modeled as real data for a future scheduling UI but isn't driven yet — competing hub-time costs (org-board work, alibi construction, Sova sessions, Tereza calls, equipment prep) need those systems' own authored costs, which don't exist before real mission/hub content. `src/hub/mind_dashboard.gd` (`MindDashboard`) is a read-only view model over `MindModel`: bands by default ("Quiet"/"Murmur"/"Loud"/"Crisis"), exact values only when explicitly requested — "no shame in playing with the hood open" as an opt-in, not the default. `src/hub/{loadout_item,loadout}.gd` model §4.7/§4.11's night-phase kit vs. cover-consistency check: one inconsistent item flags the whole loadout, the same "one tell condition catches it" logic already used for NPC lies (§5). All three new classes reference `src/sim/`'s `MindModel`/`FatigueState` directly — unrestricted, since only `src/percept/` is boundary-linted. 18 new tests across 4 files.)*
- [ ] Substance model (stimulant/alcohol/sedative tradeoffs) — **AC:** dependency-dominance lint passes on all authored configs

## M4 — The Cover (w23–29)
*Exit: a full day/night/debrief cycle plays end-to-end.*
- [x] Dialogue DSL + compiler (`/tools/dlgc`) + runner + interrupt memory (blueprints §4) — **AC:** compiled prologue Sova scene plays; contradiction catch fires both directions *(Pass 15: `tools/dlgc.py` — a hand-rolled parser/compiler (line-based regex grammar: node headers, speaker/stance lines, drift annotations, structured/bare claim grants, guarded choices, goto/END) emitting graph JSON, plus a compact predicate-text parser producing exactly `src/core/predicate.gd`'s existing `{"op":...,"args":{...}}` shape (reusing Pass 1's `PredicateEvaluator` as-is — "one evaluator, unit-tested once"). `content/dialogue/prologue_sova.dlg` is a real, small Cold-Open scene (master_plan §2.6) with one branching choice; `python3 tools/dlgc.py` (no args) compiles every `content/dialogue/*.dlg` file and is now a CI lint step, verified locally against a deliberately-broken fixture (an unknown predicate operator) before being wired in — the compiled JSON itself is never committed (tech_guidelines §5.1: "never hand-edited, never committed"), so GDScript-side tests use hand-built Dictionaries matching the compiler's exact output format, one of them transcribed verbatim from the real compiler's output against the real prologue scene. `src/dialogue/{dialogue_graph,dialogue_runner,interrupt_memory}.gd` — a new top-level `src/dialogue/` directory (sibling to `core`/`sim`/`percept`), unrestricted by the percept/truth boundary lint (which only scans `src/percept/`). `DialogueRunner` walks the graph, filters choices by guard via `PredicateEvaluator`+`MockWorldQuery` (an existing test fixture whose own doc comment anticipated exactly this pass), and feeds every *structured* claim grant into `InterruptMemory`, which detects a contradiction whenever two statements share (subject, predicate) but disagree on object — checked symmetrically regardless of which statement's `source` came first, which is what "fires both directions" means and is exactly what's tested (an NPC statement contradicted later by the player, and the reverse). Guards apply to choices only, not individual lines, for v0 — the prologue content never needed per-line guards. 18 new tests across 3 files. Claim-grants stay a local, minimal `{subject, predicate, object, source}` tuple, deliberately not the full Claims/Provenance/DebriefLedger system (Pass 17's job); register-mark pre-selection/input-hold behavior is parsed and carried as data but has no UI to attach to before Pass 19.)*
- [x] NPC minds (KNOWS/HIDES/LIES + personality) + suspicion ledger + gossip tick — **AC:** scenario fixtures: a witnessed Ground reaches the right NPCs on the right days *(Pass 16: new top-level `src/social/` directory. `src/social/npc.gd` (`NPC`) is foundation_blueprints §5's schema as plain data (personality, loyalty_targets, knows/hides/lies, gossip_edges, trust, relationship_tier, state) — `suspicion` deliberately excluded from the NPC record itself and owned by `SuspicionGraph` instead, since suspicion is relational (who's noticing what), the same reasoning that keeps GroundState off Actor. `src/social/suspicion_ledger.gd` (`SuspicionLedger`) implements §4.7's named entry weights (seen-Grounding 2, police-pattern 4, cover-inconsistent-equipment 5, implausible-survival 6, interrupt-memory-inconsistency 3-6) and its "-1/week if unreinforced" decay as a pure function of (weight, day added, query day) — recomputed on every query, so there's no periodic-tick bookkeeping to get wrong or skip. `src/social/suspicion_graph.gd` (`SuspicionGraph`) composes one ledger per NPC plus the three named thresholds (25 wary / 50 active / 75 convinced). `src/social/gossip_sim.gd` (`GossipSim`) propagates one suspicion observation along an NPC's gossip_edges with per-edge delay (arrival day = source day + delay) and distortion (a seeded ±1 weight offset plus a blurred generic type, "gossip_of:<original>") — directly satisfying the AC: a `"seen_grounding"` entry at a witnessing NPC reaches every gossip-connected NPC on exactly `source_day + edge.delay_days`, verified in tests against `test_prng.gd`'s pinned seed=1 vector, not just asserted. Publishes `"SuspicionEntryAdded"` (foundation_blueprints §1.2's own example event name) per target via the existing `EventBus`. 18 new tests across 4 files. One arithmetic mistake in a test's own hand-computed expectation (not the production decay code) caught and fixed before commit — `6 - 5 = 1` at exactly five decay-weeks elapsed, not `0` as a first draft of the test wrongly asserted.)*
- [ ] Day-phase verbs (converse/eavesdrop/observe/plant/lift/photograph/small talk) — **AC:** M2 "Listening Room" day half playable in graybox
- [x] Debrief v1: claim drafting from event log, three honesty modes, truth-delta, consequence channels — **AC:** liar-bot smoke test; a believed-phantom claim drafts correctly *(Pass 17: new top-level `src/debrief/` directory. `src/debrief/claim.gd` (`Claim`) is foundation_blueprints §3's schema exactly (`id, subject, predicate, object, qualifiers, provenance[]`) as a pure, never-mutated data record — honesty mode/truth-delta/consequences deliberately live in `DebriefLedger` instead, keyed by claim id, not on the claim itself. `src/debrief/claim_drafter.gd` (`ClaimDrafter`) drafts claims from already-reduced "perceived event" data, proving §4.10's "quiet knife" directly: a believed-phantom event and a real event with the same subject/predicate draft byte-identical Claims (same PERCEIVED provenance, nothing marks the phantom one as suspect) — whether it's actually true is deferred entirely to submission time, never decided at drafting. `src/debrief/debrief_ledger.gd` (`DebriefLedger`) tracks candidates in deterministic insertion order (never Dictionary iteration order), computes truth-delta as a direct content-match (0 true / 1 false — §4.10 gives no graduated formula to implement, unlike MindModel's §4.4 or the Director's §4.3), asserts `VERIFIED_ONLY` requires grounded/evidence provenance, and asserts against resubmitting an already-submitted claim ("submission irreversible"). `FABRICATE` optionally bills a caller-supplied `MoralInjuryState` (Pass 11) directly — "lies price into moral injury immediately" — the one consequence channel already real and available; Doubek trust/resource budget/plot flags all need a hub/mission system that doesn't exist before Pass 18. The liar-bot smoke test (`test_liar_bot.gd`) fabricates a 5-claim batch (some matching truth, some not) end to end without erroring, and separately proves truth-delta is computed from actual content regardless of the fabricating mode chosen. 21 new tests across 4 files.)*
- [ ] Journal + camera/evidence systems (§4.17) — **AC:** photographed phantom yields empty frame; evidence provenance armors a claim
- [ ] Content validator v1 (IDs, reachability, calendar lint) — **AC:** seeded broken content fails CI *(Pass 13 laid the foundation this will build on: `tools/schemas/mission.schema.json` + `tools/content_validator.py` — a hand-rolled, dependency-free JSON-Schema-subset engine (type/required/properties/additionalProperties/enum/minimum/maximum/pattern/items), CI-wired, validating every `content/missions/*/mission.json` against the schema. Still v0-scoped: no cross-file ID resolution yet (there's exactly one ID field so far and nothing references another mission's ID), no reachability/calendar checks (nothing to reach or schedule yet — those need Pass 15's dialogue DSL and Pass 18's hub calendar), and no "seeded broken content fails CI" regression fixture (that's this AC's own job once the validator does more than schema-shape checking). What Pass 13 proved instead: the schema mechanism itself works, hand-verified by feeding it a deliberately-broken mission dict locally (bad ID pattern, over-cap `encounter_cap`, unknown `op_class`, missing required field) and confirming all four violations were caught precisely, before ever committing.)*

## M5 — Mission One for Real (w30–35)
*Exit: the theater moment lands in playtests ("that's what happened?!").*
- [ ] "Induction" full production (art, audio, deck, debrief spec) per its ground-truth doc (story_bible §4)
- [ ] Remaining ops (`HUDGlitch`, `ObjectSwap`, `FamiliarFace`, `EntityMask`, `GeometrySwap`, `TimeGap`, `MemoryEdit`) + remaining archetypes (Heavy, Runner, Technician, Civilian)
- [ ] Theater v1 (honesty report, export cards, streamer mode) — **AC:** export renders correctly at phone size (art_direction §8)
- [ ] Adaptive music v1 (stem buses, band-driven thinning) — **AC:** mix sheet loudness targets verified
- [ ] **Naming decision** (title-collision risk, master_plan §12) with trademark/storefront search — **AC:** decision recorded in §11.3 amendment
- [ ] Steam Deck profiling pass — **AC:** all tech §11.1 budgets green on Deck

## M6 — Slice Complete (w36–43)
- [ ] Prologue + "The Listening Room" + "Smoke Test" full production per ground-truth docs
- [ ] Hub cast dialogue (Doubek, Sova, Tereza arcs to slice depth); ending hooks stubbed
- [ ] Difficulty + psych sliders; first full accessibility pass (ux_charter §4 inventory audit — every committed item demonstrably present) *(Pass 19 laid real foundational scaffolding this will build on, without claiming the full inventory audit: a new `src/ui/` directory with `ThemePalette` (art_direction §5's ten hex anchors as named constants — a future real Godot `Theme` resource sources from these, not the reverse, since hand-authoring a `.tres` file blind with no editor to verify it in this sandbox is exactly the untested-Godot-behavior risk avoided since Pass 6/9), `MotionConstants` (§7's timing/easing constants), `AccessibilitySettings` (real, tested state for UI scale/subtitle size stepping, Clarity Mode's actual on/off gate — its own Pass 10 docstring named this as where the toggle belongs — and the Ground hold/toggle input-mode preference GroundState already accepts agnostically), and `ScreenSpec`/`MindDashboardScreen` (a "placeholder screen" as data — title + labeled rows, with a real `screen_reader_text()` generator honoring ux_charter's "screen-reader on all paperwork/menu screens" — rather than a hand-authored `.tscn` scene file this sandbox can't render or verify). What's still missing for the full AC: difficulty/psych sliders (no missions to tune difficulty against yet), actual Control-node UI (needs a real Godot editor session), and the remaining inventory items that need real art/audio assets (colorblind-safe textures, photosensitivity-safe rendering, subtitle plates, rumble, brightness/audio calibration) — all explicitly out of the 20-pass engineering arc's "no real art/audio/content authoring" scope. 22 new tests.)*
- [ ] Localization pipeline proven on one full scene incl. a drift pair round-trip
- [ ] Internal playtest #2 with full UX protocol (ux_charter §6) — **AC:** first-hour targets measured; anti-fun tripwires reviewed

## M7 — Polish & Slice Release (w44–48)
- [ ] External playtest round; tuning from telemetry; quit-point review complete
- [ ] Sensitivity review implemented (tracked issues closed); content toggles final
- [ ] Trailer from one real unstaged Afterimage comparison; store/demo packaging (prologue+M1 demo build)
- [ ] Slice release criteria: determinism corpus 100% green 14 consecutive nights; all bots finish all missions; ux_charter §2 targets met in the external round

---
*After M7: full-game content batches per master_plan §13. This file gains M8+ sections at that boundary, not before.*
