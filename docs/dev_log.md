# AFTERIMAGES: VRANOV — Development Log
**Document type:** Engineering progress log, one entry per pass. Companion to `roadmap.md` (the checkable backlog) — this file is the narrative "what happened and why," roadmap.md is the "what's left."

Engineering is being delivered in **20 planned passes**. Each pass is a substantial, self-contained chunk of work: implement, test, lint/format-verify, update this log and the roadmap, commit. The plan below is the current best estimate of how the 20 passes divide the work — later passes may adjust it (noted here when they do) as real constraints surface.

---

## The 20-pass plan (living document)

| Pass | Focus |
|---|---|
| **1** | Repo/project scaffold, custom test harness, CI. Deterministic core: `FixedMath`, `Xoshiro128StarStar` PRNG, `EventBus`, `Predicate`/`WorldQuery`. |
| 2 | `GameStateStore` + `SaveSystem` + schema versioning/migrations; fixed-tick sim harness + `InputFrame` recording/replay; determinism corpus v0 + CI job. |
| 3 | TruthSim actor model (integer-mm positions, entity IDs), grid collision, swept casts. |
| 4 | Line-of-sight (occlusion grid) + sound propagation (room/portal graph). |
| 5 | Utility AI skeleton + Sentry/Professional archetypes; WitnessSystem v1. |
| 6 | Combat verbs v1 (move/sprint/crouch/lean/aim/fire/reload/takedown/throw/Focus); InputMap actions. |
| 7 | Graybox test level wiring TruthSim+AI+combat; paranoid/credulous bot harness v0. |
| 8 | PerceptRenderer boundary (read-only truth views, op decorator pipeline) + static truth/percept separation lint. |
| 9 | First DistortionOp classes (`SubtitleDrift`, `AudioSwap`, `PhantomAudio`, `PhantomEntity`). |
| 10 | Ground verb; Clarity Mode stub. |
| 11 | MindModel (four variables, master_plan §4.4 constants, worked-example fixtures). |
| 12 | DistortionDirector (budget/deck/caps/cooling/seeding); fairness auditor v1. |
| 13 | Content pipeline: JSON schemas, content validator CLI, mission package loader + a stub mission fixture. |
| 14 | Replay Theater v0 (data model: dual-pane reconstruction, checkpoint snapshots). |
| 15 | Dialogue DSL parser/compiler + runtime graph + DialogueRunner + interrupt memory. |
| 16 | NPC mind schema + SuspicionGraph + GossipSim. |
| 17 | Claims/Provenance + DebriefLedger; liar-bot smoke test. |
| 18 | Hub skeleton: calendar/sleep economy, mind dashboard bindings, loadout data model. |
| 19 | UI shell (Theme resource, placeholder screens) + accessibility scaffolding. |
| 20 | Integration: playable prologue stub (Ground tutorial + one scripted `SubtitleDrift` + mini-Theater + trivial debrief) on placeholder art; full-suite green; log/roadmap finalized with what's left for post-Pass-20 (real art/audio/content authoring, M5-M7). |

This maps onto `roadmap.md`'s M0–M4 milestones; passes 18-20 begin reaching into M5/M6 territory (hub, UI, integration) at a skeleton/placeholder level. **No real art, music, or voice work happens in these passes** — every pass is engine/tooling/data code plus, where useful, programmer-art placeholders (colored rectangles, primitive shapes) purely so a system can be seen running. Actual asset production per `art_direction.md` and `master_plan.md` §6 is out of this 20-pass engineering arc.

---

## Pass 1 — Foundations: scaffold, deterministic core, test harness, CI

**Delivered:**
- **Project scaffold**: `project.godot` (title *Afterimages: Vranov*, 640×360 logical resolution integer-scaled per tech_guidelines D6, `warnings/treat_warnings_as_errors` on per D2), `docs/ENGINE_VERSION` pinning 4.3.0-stable, `.gitignore`/`.gitattributes` (LFS policy per tech D14), the `/src`, `/tests`, `/content`, `/tools` directory layout from tech_guidelines §5.8.
- **Deterministic core** (`src/core/`):
  - `fixed_math.gd` — Q16.16 fixed-point scalar arithmetic for MindModel/Director math (tech §3.2). Explicitly *not* for world positions (those stay plain integer mm per tech D4) — documented in the class header so a future pass doesn't reach for it in the wrong place.
  - `prng.gd` — `Xoshiro128StarStar`, seeded via four splitmix64 draws from one 64-bit seed (tech D5). One named stream per system is the intended usage pattern once TruthSim/Director/Gossip streams exist (Pass 2+).
  - `event_bus.gd` — typed pub/sub, registration-order dispatch, non-reentrant (queued) re-publish, snapshot-iterated so a handler unsubscribing itself mid-dispatch is safe (foundation_blueprints §1.2).
  - `world_query.gd` + `predicate.gd` — the Predicate language (foundation_blueprints §2): 18 leaf operators + `all`/`any`/`not` combinators, a duck-typed `WorldQuery` read interface, and a pure static `validate()` that content tooling will run in CI later (Pass 13).
- **Test harness** (`tests/framework/`, `tests/run_tests.gd`): a small custom xUnit-style harness (`AfterimageTestCase`, `AfterimageTestRunner`) — **not GUT**. See the decision note below.
- **67 unit tests** across 5 files (`test_fixed_math.gd` 15, `test_prng.gd` 10, `test_event_bus.gd` 11, `test_predicate_evaluate.gd` 20, `test_predicate_validate.gd` 11), plus a reusable `MockWorldQuery` test double in `tests/framework/`.
- **CI** (`.github/workflows/ci.yml`): a `test` job that downloads Godot 4.3-stable headless and runs the suite, and a `lint` job running `gdlint`/`gdformat --check`.
- **Title change**: the game is now titled **Afterimages: Vranov** (README, master_plan.md header + §12 risk resolved, tech_guidelines.md amendment log). The in-fiction mechanic keeps its original name, "the Afterimage."

**Decision — custom test harness instead of GUT (tech_guidelines.md §12 amendment):** `tech_guidelines.md` D13 locked in GUT. GUT has to be fetched from GitHub — as a git submodule or via the editor's AssetLib — and this authoring sandbox has no network path to GitHub (confirmed: `git`/`curl` to `github.com` return a policy-level 403 from the egress proxy, and `git submodule add` needs to resolve a real commit hash from the remote, which isn't possible offline). Rather than block Pass 1 on that or hand-copy a third-party project's source from memory, I built a small in-house harness whose assertion surface (`assert_eq`, `assert_true`, `assert_almost_eq`, …) deliberately mirrors GUT's naming, so adopting real GUT later — once someone with GitHub access pins a commit via submodule — is an addition, not a rewrite of any test file. Recorded as a proper amendment in `tech_guidelines.md` §12, not a silent deviation.

**Verification approach and its honest limits:** this sandboxed environment has no Godot binary and no working Docker daemon, so nothing in this pass has been executed inside the actual Godot runtime by me directly. Given that constraint, verification leaned on everything that *was* available:
1. **The PRNG is the highest-risk piece** (hand-porting 64-bit-unsigned bit-twiddling algorithms into a 64-bit-*signed* scripting language). Rather than trust the port by inspection, I wrote an executable Python reference implementation of splitmix64 + xoshiro128**, ran it to generate concrete output vectors for five seeds, then wrote a *second* Python model that mimics GDScript's exact arithmetic assumptions (signed 64-bit wraparound, arithmetic right-shift, explicit unsigned-shift correction via precomputed masks) and confirmed it reproduces the canonical vectors bit-for-bit before porting that exact logic to GDScript. The five seeds' first-12-draws are pinned directly into `test_prng.gd` as `VECTORS`. The one assumption that couldn't be verified without a real Godot process is that GDScript's `int` actually performs silent two's-complement wraparound on overflow and that `>>` is arithmetic — both are documented Godot 4 behaviors, and the test is written so that if either assumption is wrong, CI's real headless Godot run will fail loudly on `test_reference_vectors_match_across_seeds` rather than silently producing subtly-wrong randomness.
2. **`gdtoolkit` (gdlint/gdformat) was installed and actually run locally** (`pypi.org` is reachable through this session's egress policy even though `github.com` isn't) — every source and test file was run through `gdformat --check` and `gdlint` and is 100% clean, which catches real syntax errors, not just style: gdformat has to fully parse a file's GDScript grammar to reformat it, so a clean run is meaningful evidence the code is at least syntactically well-formed.
3. **GDScript closure semantics were treated as a known trap, not assumed**: GDScript lambdas capture outer local variables *by value*, not by reference, so a naive `var count := 0; bus.subscribe(..., func(e): count += 1)` pattern would silently produce a false test failure (the outer `count` never changes) that has nothing to do with whether `EventBus` itself is correct. Every `EventBus` test that needs a mutable counter across calls uses a small helper class with a bound `Callable(instance, "method")` instead, sidestepping the issue entirely.
4. **What's *not* verified yet**: actual execution inside Godot. The first real Godot run happens in CI on push (GitHub Actions runners have full internet access regardless of this sandbox's restrictions), and I will check that run's logs via the GitHub API/MCP tools immediately after pushing and fix forward in a follow-up commit within this same pass if anything surfaces — this is called out explicitly rather than assumed clean.

**Deferred to Pass 2** (see `roadmap.md` M0): `GameStateStore`, `SaveSystem` + schema/migration harness, the fixed-tick simulation harness, `InputFrame` recording/replay, and the determinism corpus + its CI job. M0's actual exit criterion ("inputs recorded in a stub scene replay tick-perfect") is **not yet reached** — Pass 1 built the deterministic primitives that harness will be built on top of, not the harness itself.

**Files added:** `project.godot`, `.gitignore`, `.gitattributes`, `docs/ENGINE_VERSION`, `.github/workflows/ci.yml`, `src/core/{fixed_math,prng,event_bus,world_query,predicate}.gd`, `tests/framework/{test_case,test_runner,mock_world_query}.gd`, `tests/run_tests.gd`, `tests/unit/{test_fixed_math,test_prng,test_event_bus,test_predicate_evaluate,test_predicate_validate}.gd`.

**Files changed:** `README.md` (title, status, test-running instructions), `docs/master_plan.md` (title, §12 risk resolved), `docs/tech_guidelines.md` (§12 amendment log: test harness decision, title decision), `docs/roadmap.md` (M0 checkboxes updated).

**Post-push CI verification (the loop this whole approach depends on):** first real CI run ([run 29654915740](https://github.com/DanielDmas/Afterimage/actions/runs/29654915740)) came back **lint: pass, test: fail** — checked via the GitHub Actions API, not assumed. The good news first: `gdlint`/`gdformat --check` passing for real on GitHub's infrastructure confirms the local `gdtoolkit` run in this pass was meaningful, not just theater. The failure was real and unrelated to any of the PRNG/overflow risk called out above — it was simpler and more fundamental:

```
SCRIPT ERROR: Parse Error: Could not find type "AfterimageTestRunner" in the current scope.
  at: GDScript::reload (res://tests/run_tests.gd:11)
```

**Root cause:** `.godot/` (Godot's project-scan cache, which is where global `class_name` → script-path resolution gets recorded) is correctly gitignored — it's a local editor artifact, not something to commit. But that means on a genuinely fresh checkout, Godot hasn't scanned the project yet, so none of this pass's `class_name` globals (`AfterimageTestRunner`, `FixedMath`, `Xoshiro128StarStar`, etc.) are resolvable the moment `--script res://tests/run_tests.gd` tries to parse a reference to one. Opening a project in the editor UI triggers this scan invisibly and automatically; running a script directly via `--headless --script` does not.

**Fix:** added an explicit one-time import pass — `godot --headless --path . --editor --quit` — before running the test script, in both `.github/workflows/ci.yml` and the README's local instructions. This forces the same project scan the editor would do on first open, without needing a display. Its own exit code is treated as non-authoritative (`|| true`) since a benign warning can make that pass itself report nonzero; the test-running step right after it is what actually gates the build, and it now starts from a warm class-name cache. This is exactly the verification loop described as the plan for this pass: something unverifiable locally (real Godot script-loading behavior on a cold checkout) surfaced through real CI, got diagnosed from the actual error text rather than guessed at, and was fixed forward in the same pass rather than discovered later.

**Confirmed green** ([run 29655073102](https://github.com/DanielDmas/Afterimage/actions/runs/29655073102), commit `a788729`): both jobs `success`. Pulled the actual test-step log text rather than trusting the exit code alone —
```
Afterimage test run — 5 suite(s), 67 test(s), 6849 assertion(s)
...
ALL PASSED (67/67)
```
— and `gdlint`/`gdformat --check` both clean on GitHub's own infrastructure, matching the local run byte-for-byte. **Pass 1 is closed.** One cosmetic, non-blocking note from the same log for the record: Godot prints `WARNING: ObjectDB instances leaked at exit` / `ERROR: 3 resources still in use at exit` after the test report but before process exit — harmless in a one-shot CLI runner that terminates immediately after (the OS reclaims everything), and it does not affect the exit code (`quit(0 if report.all_passed() else 1)` still returned 0). Worth a look with `--verbose` if a future pass wants zero engine noise in the log, but not worth chasing now.


---

## Pass 2 — GameStateStore, SaveSystem, fixed-tick harness, determinism corpus v0

**Delivered** (all under `src/core/`, engine-agnostic `RefCounted` classes, no scene-tree dependency, matching Pass 1's discipline):
- **`save_migrations.gd`** — the migration ladder (foundation_blueprints.md §6). `CURRENT_SCHEMA_VERSION = 2`, deliberately not 1: a real v1→v2 step is registered and exercised (v1 stored `day`/`flags` at the save's top level; v2 nests them under `campaign`), dispatched via a `match` statement rather than a self-referential `static var` dictionary of `Callable`s — the dictionary-of-callables version would have worked too, almost certainly, but it rests on an assumption about static-initializer ordering during self-referential class loading that I can't verify without running Godot, and the `match` version has no such assumption at all. Given the choice, the one with zero unverifiable assumptions won.
- **`game_state_store.gd`** — single serializable source of truth (blueprints §1.1): key-path `get_value`/`set_value` over a private `Dictionary`, `to_dict()`/`load_from_dict()` with migration on load. "All mutations are Events" is satisfied by `set_value()` publishing a `StateValueChanged` fact (old/new value) on the EventBus rather than by full event-sourced state reconstruction — that stronger guarantee is reserved for TruthSim's tick log, which is what actually has to survive re-simulation. Documented this distinction explicitly in the class doc so it doesn't read as an oversight later.
- **`save_system.gd`** — JSON + gzip persistence (tech D8/§5.3) via `PackedByteArray.compress()`/`decompress()` with `FileAccess.COMPRESSION_GZIP`. Decompression reads the required output-size hint from the gzip stream's own trailing ISIZE field (RFC 1952 §2.3.1: last 4 bytes = uncompressed size mod 2^32, little-endian) instead of inventing a custom header — this is externally-verifiable knowledge (the gzip spec itself, not a Godot-specific guess), and it means a save this writes is a genuine standalone `.gz` file, not a wrapper format. The one real assumption — that Godot's `COMPRESSION_GZIP` mode actually emits RFC 1952-framed output (magic bytes `0x1F 0x8B`, real trailer) rather than something merely similarly-named — is asserted directly in `test_save_system.gd::test_save_produces_a_valid_gzip_stream`, so a wrong assumption fails loudly in CI rather than silently corrupting saves.
- **`input_frame.gd`, `replay_log.gd`, `fixed_tick_clock.gd`** — the fixed-tick contract (tech §3.1): `InputFrame` is a generic `String -> Variant` action map (deliberately not tied to concrete verbs yet — InputMap actions land with combat in Pass 6), `ReplayLog` is the versioned seed+frames stream that later becomes both the mission save and the Theater's source, `FixedTickClock` is the 30 Hz whole-tick counter TruthSim will use instead of reading engine `delta`.
- **`tests/fixtures/stub_sim.gd`** — an explicitly disposable placeholder for TruthSim, folding PRNG + FixedMath + a toy position/accumulator over a replay's frames into a SHA-256 digest via `HashingContext`. Lives under `tests/fixtures/`, not `src/`, because it will be deleted the moment Pass 3's real TruthSim exists to test against instead.
- **`tests/corpus/run_00{1,2,3}.json`** — three recorded fixture runs (5/8/12 frames, distinct seeds), the "determinism corpus v0" roadmap.md asks for.
- **44 new tests** across 7 files (`test_input_frame` 6, `test_fixed_tick_clock` 6, `test_replay_log` 5, `test_save_migrations` 5, `test_game_state_store` 10, `test_save_system` 6, `test_determinism_corpus` 6), bringing the suite to 111 tests. All clean against `gdlint`/`gdformat --check` locally before commit (same discipline as Pass 1).

**On the determinism-corpus acceptance criterion, honestly scoped:** roadmap.md's M0 item reads "re-simulates hash-identical on Linux/Win/mac in CI." What Pass 2 actually proves is narrower and stated as such in `test_determinism_corpus.gd`'s header comment: the record→replay→hash→compare *mechanism* correctly detects both agreement (same fixture replayed twice ⇒ identical digest) and divergence (`test_digest_is_sensitive_to_an_extra_frame`, `test_digest_is_sensitive_to_run_seed`) — in-process, on one OS. I deliberately did not hand-write "golden" expected digest values into the corpus fixtures: I can't execute Godot here to compute them, and fabricating expected SHA-256 values by hand-reasoning about the stub sim's arithmetic would risk baking in *my* mistake as the "correct" answer rather than testing anything real. Self-consistency (replay twice, compare) sidesteps that risk entirely while still proving the property that actually matters. True cross-platform (Linux/Windows/macOS) byte-identical agreement — and a dedicated PR-subset-vs-nightly-full CI split — stay open, tracked in roadmap.md alongside the Windows/macOS runner gap already noted from Pass 1; both are deferred until there's a real multi-OS matrix and a corpus large enough to make the PR/nightly split matter, rather than built ahead of the need.

**Files added:** `src/core/{input_frame,replay_log,fixed_tick_clock,save_migrations,game_state_store,save_system}.gd`, `tests/fixtures/stub_sim.gd`, `tests/corpus/run_00{1,2,3}.json`, `tests/unit/{test_input_frame,test_fixed_tick_clock,test_replay_log,test_save_migrations,test_game_state_store,test_save_system,test_determinism_corpus}.gd`.

**Files changed:** `docs/roadmap.md` (M0 GameStateStore/InputFrame/determinism-CI items updated to reflect what's actually proven vs. still open).

**Post-push CI verification:** [run 29662878857](https://github.com/DanielDmas/Afterimage/actions/runs/29662878857) came back **lint: pass, test: 110/111 pass**. The good part first: both gzip assumptions this pass carried were confirmed correct on real Godot — `test_save_produces_a_valid_gzip_stream` (RFC 1952 magic bytes) and `test_save_load_save_is_byte_identical` both passed, so `COMPRESSION_GZIP` really does emit standard-framed gzip and the ISIZE-trailer decompression technique works as designed.

The one failure was a test bug, not a system bug: `test_save_and_load_round_trip_preserves_state` asserted `loaded.to_dict() == store.to_dict()` and failed with identical keys/values in different order (`{"schema_version", "campaign"}` vs. `{"campaign", "schema_version"}`). Reasoning from both results together (this failure plus the adjacent byte-identical test passing) points at `JSON.stringify()` canonicalizing key order on output independent of a Dictionary's insertion-order history: the byte-identical test's two sides both pass through `stringify()` as their last step and land on the same canonical order regardless of their respective in-memory history, which is exactly why *that* test is order-*in*sensitive while a bare `to_dict() == to_dict()` comparison across one JSON round-trip is not. Dictionary key order was never a real correctness requirement here — only that values survive the round trip — so the fix was to assert the specific values via `get_value()` instead of whole-Dictionary equality, which is both a more precise test of intent and immune to the ordering question entirely. Re-verified clean against `gdlint`/`gdformat --check` locally before pushing the fix.

**Confirmed green** ([run 29663067392](https://github.com/DanielDmas/Afterimage/actions/runs/29663067392), commit `1216601`): both jobs `success`, test log reads `ALL PASSED (111/111)` including `test_save_and_load_round_trip_preserves_state`. `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 2 is closed.**

---

## Pass 3 — TruthSim actor model: entity IDs, grid collision, swept casts

**Delivered** (all under `src/sim/`, per tech_guidelines.md §5.8's repo layout — the first files in that directory):
- **`actor.gd`, `actor_registry.gd`** — a truth-layer body (position + collision-circle radius) and its registry, assigning sequential entity IDs (tech §3.5 — never a Godot object instance ID, which isn't stable/reproducible across runs). Deterministic iteration via a sorted `all_ids()`.
- **`collision_grid.gd`** — a sparse uniform grid of blocked/free cells (a `Dictionary`, not a fixed-size array): no real level exists yet (the Pass 7 graybox room), so there's nothing to size a dense grid against, and a sparse one needs no upfront bounds. Correctly floors world coordinates to cell indices for negative coordinates too — GDScript's bare `/` truncates toward zero (C semantics), which is the wrong operation for a floor-based grid index, so `_floor_div()` corrects it explicitly (tested against negative-coordinate cases).
- **`swept_collision.gd`** — the actual "swept casts" tech_guidelines.md §3.4 asks for: swept circle-vs-AABB via the Minkowski-sum slab method (expand the static box by the circle's radius, sweep the circle's center as a point against the expanded box). Every arithmetic case — straight hit, parallel-and-outside, too-short-to-reach, already-overlapping (reports t=0), diagonal hit, moving away, exact-endpoint touch, and both zero-delta edge cases — was worked out first in an executable Python reference using exact `Fraction` arithmetic, the same "verify externally with a tool that isn't Godot, then port" discipline as Pass 1's PRNG. One deliberate, documented simplification: box corners are treated as square, not rounded (a true rounded-corner sweep needs a quadratic/sqrt solve per corner, which buys very little collision-feel difference for a top-down action game over the square-corner approximation). Actor-vs-actor collision only gets a *static* overlap test (`circles_overlap`, exact integer distance-squared, no sqrt) — true swept circle-vs-moving-circle collision is deferred until multiple simultaneously-moving bodies actually need it (AI in Pass 5+), not built ahead of that need.
- **`fixed_math.gd` gained `to_int_trunc()`** — truncates toward zero for both signs, needed because `move_with_collision()`'s existing `to_int_floor()` would over-shoot a stopped movement further in the negative direction for negative deltas (flooring a negative fraction rounds *away* from zero, which is exactly wrong for "stop at or before the impact point" semantics regardless of travel direction).
- **`truth_sim.gd`** — the tick orchestrator: one player `Actor`, `step(InputFrame)` reads `move_x`/`move_y` as a raw per-tick millimeter delta and resolves it through `SweptCollision.move_with_collision()` against the grid, publishing `ActorMoved` only when a move actually changed position. Deliberately narrow scope: no AI, no combat, no witnesses (Pass 5/6/4 respectively) — this is the foundation those passes build onto, and `step()`'s signature (one tick, one InputFrame) is meant to stay stable as they plug in.
- **39 new tests** across 4 new files (`test_actor_registry` 6, `test_collision_grid` 7, `test_swept_collision` 16, `test_truth_sim` 9) plus one addition to `test_fixed_math` (`to_int_trunc`), bringing the suite to 150 tests. All clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — the corpus stays on StubSim, not TruthSim, for now:** Pass 2's dev_log entry said "delete `stub_sim.gd` once TruthSim lands and the corpus points at that instead." On reflection, doing that *now* would make the corpus mechanism *less* representative, not more — a movement-only TruthSim with no walls in most fixtures has no more interesting divergence surface than the toy StubSim already proves against. `test_determinism_corpus.gd` and its 3 JSON fixtures are left untouched; TruthSim instead gets its own direct correctness tests (exact expected positions, not opaque hash comparisons), which is a better fit for verifying *this* kind of behavior anyway — collision outcomes are things I can and did work out exactly by hand/Python, not just check for reproducibility. Revisiting "point the corpus at real TruthSim" is deferred until AI and combat (Pass 5/6) give it enough real behavior to be worth the swap. Noted here as the kind of plan adjustment the top of this log already allows for ("later passes may adjust it as real constraints surface").

**Files added:** `src/sim/{actor,actor_registry,collision_grid,swept_collision,truth_sim}.gd`, `tests/unit/{test_actor_registry,test_collision_grid,test_swept_collision,test_truth_sim}.gd`.

**Files changed:** `src/core/fixed_math.gd` (+`to_int_trunc`), `tests/unit/test_fixed_math.gd` (+1 test), `docs/roadmap.md` (M1 TruthSim/collision item updated).

**Confirmed green** ([run 29663447877](https://github.com/DanielDmas/Afterimage/actions/runs/29663447877), commit `660968c`): both jobs `success`, test log reads `ALL PASSED (150/150)`. All 16 `test_swept_collision.gd` cases passed — including the diagonal-hit and already-overlapping-reports-t-zero cases that most depend on the ported slab-test arithmetic being right, not just the easy straight-line cases — and the aggregate zero-failures count confirms `test_collision_grid.gd`'s negative-coordinate floor-division tests and `test_actor_registry.gd` passed clean too. `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 3 is closed.**

---

## Pass 4 — Line-of-sight and sound propagation

**Delivered** (`src/sim/`, continuing Pass 3's foundation):
- **`line_of_sight.gd`** — `cells_along_line()` implements Bresenham's line algorithm (the standard integer-only err/e2 formulation, symmetric across all octants), verified against an executable Python reference across nine cases (horizontal, vertical, diagonal, both shallow and steep slopes, both negative directions, a single point, and a reversed-direction check) before porting. `has_clear_line()` builds on it against `CollisionGrid`'s existing blocked-cell data — deliberately *not* a second parallel occlusion grid, since wall geometry blocking movement and wall geometry blocking vision is the same geometry, and maintaining two copies would just be duplicated data with no behavioral upside. The two endpoints' own cells are exempted from the blocking check (an actor standing in a doorway, or anywhere else, shouldn't have their own occupied cell block their sightline).
- **`sound_graph.gd`** — an abstract room/portal graph (rooms are opaque string IDs; no real level exists yet). A sound's loudness is a subtractive integer budget that portals attenuate when crossed; `propagate()` finds the *loudest possible* arrival at every reachable room — a shortest-path problem (minimize total attenuation) — via iterative relaxation rather than a priority-queue Dijkstra, since real room graphs will be small (tens of rooms, not thousands) and relaxation is simpler to get right and to verify than a heap-based implementation. A room every path attenuates to ≤0 is *omitted* from the result, not present at 0 — tested explicitly, along with a case where a longer, less-attenuating path must win over a shorter, noisier direct one.
- **26 new tests** across 2 new files (`test_line_of_sight` 14, `test_sound_graph` 12), bringing the suite to 176 tests. Clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — no vision cone yet:** master_plan.md describes AI senses as "vision cones, sound propagation, last-known-position memory" (§5.5), and it was tempting to build the angular field-of-view wrapper (range + half-angle around a facing direction) as part of this pass. I started working through the math — an angle-free cone test using squared dot products to avoid `atan2`/`sqrt` in sim code (per tech_guidelines §3.2's "no ad-hoc float math") is workable, but only if the facing-direction vector is a small normalized magnitude rather than a raw world-space displacement, or the intermediate `dot²` and `|a|²·|b|²` terms overflow int64 — and even done carefully, it's a nontrivial piece of arithmetic with no actual caller to verify it against yet. The roadmap and dev_log's own Pass 4 scope only ever named "line-of-sight + sound propagation," not vision cones; that phrase belongs to Pass 5 (Sentry/Professional AI), which is where it has a real consumer and a real test (does this specific archetype notice the player in this specific scenario). Deferred there rather than built speculatively now.

**Files added:** `src/sim/{line_of_sight,sound_graph}.gd`, `tests/unit/{test_line_of_sight,test_sound_graph}.gd`.

**Files changed:** `docs/roadmap.md` (M1 LOS/sound-propagation item checked off with scope notes).

**Confirmed green** ([run 29665845419](https://github.com/DanielDmas/Afterimage/actions/runs/29665845419), commit `b5b15a2`): both jobs `success`, test log reads `ALL PASSED (176/176)`. All 14 `test_line_of_sight.gd` cases passed (including the negative-direction and reversed-direction Bresenham cases) and all 12 `test_sound_graph.gd` cases passed (including the loudest-path-wins-over-a-shorter-noisier-route relaxation test). `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 4 is closed.**

---

## Pass 5 — Vision cones, utility AI, Sentry/Professional archetypes, WitnessSystem v1

**Delivered** (`src/sim/`):
- **`vision_cone.gd`** — the angular field-of-view test deferred from Pass 4. Avoids `atan2()`/`sqrt()` entirely via a squared dot-product comparison (`dot(a,b)² >= |a|²|b|²cos²(θ)`, valid whenever `dot(a,b) >= 0`, i.e. for any half-angle under 90° — documented as this class's supported contract). Verified against a Python reference using three angles (30°/45°/60°) chosen specifically because their cos² values are exact rationals (3/4, 1/2, 1/4, via the double-angle identity) rather than approximations, across nine cases (dead ahead, exactly on the cone edge, just outside, out of range, directly behind, and both a narrow and a wide cone's inside/outside pair). The one real arithmetic risk — `facing_dir` needs a bounded magnitude to keep `facing_sq * scaled_dist_sq` inside int64 for a ~100m level — was checked numerically (not guessed): a bound of 20,000mm overflows, 15,000mm is right at the edge, 10,000mm has comfortable margin, so `FACING_COMPONENT_MAX = 10000` is enforced by an assert rather than left as a silent risk. `cos_sq_half_angle_fx_from_degrees()` is the one place `cos()` (a transcendental function) appears in this pass — explicitly content-authoring-time only, converting an authored half-angle once, never in the per-tick hot path, the same carve-out tech_guidelines §3.1 already grants duration-to-ticks conversion.
- **`ai_utility.gd`** — all five states master_plan §5.5 names (patrol/investigate/engage/flee/report) as pure functions of a `Perception` snapshot, so the decision logic is exhaustively testable without any hidden state. Caught a real design bug before it ever reached CI: my first draft scored `just_spotted` (REPORT) at 90 and `can_see_target` (ENGAGE) at 100, meaning REPORT could *never* win on the tick it matters most — re-deriving the actual numbers by hand (not just trusting the design intent) surfaced this, and REPORT now scores 110, deliberately outranking ENGAGE for exactly the one tick sight is newly acquired ("call it in, then engage from next tick"). `FLEE` is real and tested but currently inert: `threat_level` defaults to 0 and nothing populates it yet, since that's genuine combat/health data Pass 6 owns — the five-state shape is complete now without fabricating data that doesn't exist.
- **`ai_archetype.gd` + `ai_agent.gd`** — Sentry (6000mm range, 50° half-angle, no radio) and Professional (8000mm, 60°, reports on first sighting) as perception-parameter configurations, not bespoke behavior trees: master_plan's tactical flavor ("Professional... flanks, checks corners") needs pathfinding and combat that don't exist yet, and building unique behavior code for it now would have nothing real to execute against. `AiAgent` owns the one piece of state a pure utility function can't (memory of last-known position, and whether sight was *newly* acquired this tick vs. sustained) and is what actually calls `VisionCone` + `LineOfSight` together each tick.
- **`witness_system.gd`** — "who truly saw what," reusing the exact same `VisionCone`+`LineOfSight` query AI perception uses (a witness is anyone who genuinely could have seen an event, independent of what the player later believes or claims — the truth-layer fact the debrief's truth-delta and suspicion propagation will eventually read from).
- **42 new tests** across 5 new files (`test_vision_cone` 11, `test_ai_utility` 12, `test_ai_archetype` 4, `test_ai_agent` 8, `test_witness_system` 7), bringing the suite to 218 tests. Clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — literal AC deferred, the AI it needs is not:** roadmap.md's Sentry/Professional AC reads "paranoid/credulous bot stubs finish the graybox room" — neither bots nor a graybox room exist yet (both are Pass 7). This pass delivers what those bots will actually run *on* (the perception + decision-making), with its own hand-computed correctness tests in the meantime, exactly the same pattern used for Pass 3's collision AC and Pass 4's noise-ring AC.

**Files added:** `src/sim/{vision_cone,ai_utility,ai_archetype,ai_agent,witness_system}.gd`, `tests/unit/{test_vision_cone,test_ai_utility,test_ai_archetype,test_ai_agent,test_witness_system}.gd`.

**Files changed:** `docs/roadmap.md` (M1 Sentry/Professional and WitnessSystem items checked off with scope notes).

**Confirmed green** ([run 29666164688](https://github.com/DanielDmas/Afterimage/actions/runs/29666164688), commit `5ddaea7`): both jobs `success`, test log reads `ALL PASSED (218/218)`. All 11 `test_vision_cone.gd` cases passed (including the exactly-on-the-cone-edge and both narrow/wide-cone inside/outside pairs derived from the exact-rational cos² values), and every `test_ai_utility.gd`, `test_ai_archetype.gd`, `test_ai_agent.gd`, and `test_witness_system.gd` case passed — including `test_just_spotted_outranks_engage_on_the_spotting_tick` and `test_professional_reports_on_first_sighting_then_engages`, which are exactly the pre-CI REPORT-vs-ENGAGE scoring bug fix this pass's summary called out. `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 5 is closed.**

---

## Pass 6 — Combat verbs v1, InputMap actions

**Delivered** (`src/sim/`, all standalone and tested, *not yet wired into TruthSim.step()* — see scoping decision below):
- **`movement_profile.gd`** — walk/sprint/crouch as authored integer mm/tick speeds (never a runtime float m/s÷30Hz division — positions and their deltas are plain integer mm, tech_guidelines D4), plus each mode's noise contribution (`master_plan §4.9`: "sprint (noise)"). `resolve_delta()` turns a quantized ±1 direction into the per-tick delta `TruthSim.step()` already expects — its own class doc has said since Pass 3 that this translation is "a combat-verb concern (Pass 6), not this layer's." Diagonal movement is deliberately not speed-normalized this pass — documented as a feel-tuning question for the Pass 7 graybox playtest, not a correctness one.
- **`weapon.gd`** — ammo/reload as a tick-counted state machine (never wall-clock time, so a reload finishes on the same tick under any replay), with `cz75()`/`suppressed_32()` named constructors mirroring master_plan §4.9's sidearm pair (loud/reliable vs. quiet/weak) — the same "archetype as a factory constructor" style Pass 5's `AiArchetype.sentry()/professional()` established. The Argus-issue Škorpion is deliberately omitted: carrying it prices as a suspicion entry regardless of cover (§4.7), which has no model yet.
- **`combat_resolver.gd`** — `resolve_fire()` reuses Pass 5's `VisionCone` (a tight, hardcoded-offline `cos²(3°)` "dead-on" aim cone — never computed via `cos()` at runtime, so firing never depends on a platform's libm agreeing with the machine that recorded a replay) plus Pass 4's `LineOfSight`, rather than inventing ray-vs-circle geometry; picks the nearest unobstructed, in-cone, in-range target. `resolve_takedown()` is proximity + `LineOfSight`, no cone (a takedown is whatever's at contact range, not aimed). `resolve_throw()` clamps to a max range by rejecting over-range throws outright (a true clamped landing point would need a sqrt-based vector normalization this codebase avoids in sim code, tech_guidelines §3.2). `is_noise_heard_at()`/`hearing_range_mm()` give sprint/gunfire/thrown-object noise a working open-air hearing check now, as a documented v1 stand-in for SoundGraph's room/portal propagation (Pass 4) — which has no authored rooms to route through before Pass 7's graybox level.
- **`focus_state.gd`** — Focus's activation/duration/cooldown as a tick-counted resource gate (3s = 90 ticks active, 5s = 150 ticks cooldown, `activation_count` tracked for later billing). Deliberately does *not* implement the "0.4× time" effect itself: TruthSim's tick rate is fixed at 30Hz always (the determinism contract doesn't allow the sim's own rate to vary), so that has to be a presentation-layer trick (slowed audio/visual playback over the same fixed-rate ticks) with no renderer to attach to before Pass 7. The `+6 acute stress` cost from §4.9 is likewise not billed yet — MindModel (Pass 11) doesn't exist to receive it. Same "build the gate now, wire the consumer later" pattern Pass 5's `AiUtility` used for FLEE's `threat_level`.
- **`lean.gd`** — offsets the position a `LineOfSight` query is made *from*, sideways relative to a cardinal facing, without moving the actor's own collision circle. A 90° rotation of a unit cardinal vector is exact integer arithmetic (swap components, negate one) — no general-rotation trig needed. Composes directly with the existing `LineOfSight.has_clear_line()` with no changes to that class.
- **InputMap actions** (`project.godot`, tech_guidelines D11): all 14 combat-verb actions (`move_up/down/left/right`, `sprint`, `crouch`, `lean_left/right`, `aim`, `fire`, `reload`, `takedown`, `throw`, `focus`) declared with empty `events` arrays — no default key/button bound yet. Hand-authoring a Godot `InputEventKey` resource literal with no local editor available to generate or verify it risked a wrong keycode more than it was worth; key-binding choice belongs to ux_charter.md's "full remapping" scope (Pass 19), not this pass's verb *logic*. `tests/unit/test_input_map.gd` asserts every action actually registered — a real, CI-executed check on the one thing that genuinely could have gone wrong by hand (gdlint/gdformat don't parse `project.godot` at all).
- **50 new tests** across 6 new files (`test_movement_profile` 7, `test_weapon` 10, `test_combat_resolver` 17, `test_focus_state` 7, `test_lean` 8, `test_input_map` 1), bringing the suite to 268 tests. `resolve_fire()`'s cone-boundary cases were checked against `VisionCone.point_in_cone()`'s exact integer formula by hand before being chosen (a target at (5000,100) is ~1.146° off-axis, inside the 3° cone; (5000,400) is ~4.57° off-axis, outside it) — the same "verify the exact arithmetic first" discipline as every prior pass's real-arithmetic risk. Clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — verbs built standalone, not wired into TruthSim, and the tuning-checklist AC isn't satisfiable yet:** roadmap.md's AC for this item is "every §4.9 tuning-checklist line has a recorded pass" — that checklist (stop-distance feel, hitstop timing, aim-assist curves, screen-shake budget, etc.) is inherently a playtest verdict against a real, rendered, playable build, and no such build exists before Pass 7's graybox room. Recording a real pass on any tuning-checklist line before then would be fabricating a playtest that didn't happen. What this pass delivers instead is exactly what Pass 4 and Pass 5 delivered ahead of their own wiring passes: every verb as a standalone, directly-tested module, ready for Pass 7 to actually plug into `TruthSim.step()` and run against AI for the first time. `test_truth_sim.gd` is untouched — `TruthSim`'s existing move_x/move_y contract (an already-resolved per-tick mm delta) is unchanged; `MovementProfile.resolve_delta()` is what will produce that delta once something calls it, not a replacement for `TruthSim.step()`'s own signature.

**CI caught a real bug, and chasing it down surfaced two more:**

1. The first push (`4a735b5`) failed at real Godot compile time with `Cannot pass a value of type "Lean.Side" as "Side"` — a genuine Godot 4.3 GDScript limitation where a static method's parameter typed with its own class's *unqualified* inner-enum name doesn't unify with the *fully-qualified* name (`Lean.Side`) a caller in a different file must write. No local Godot editor could have caught this before pushing. `lean.gd`'s `peek_origin()` parameter is now typed `int` instead (enums are plain ints underneath). Fixed in `09b5843`.
2. That run still printed `ALL PASSED (260/260)` — `test_lean.gd`'s 8 tests were silently missing from the count entirely, not failing. `AfterimageTestRunner._run_script()` (`tests/framework/test_runner.gd`, built in Pass 1) called `push_error()` and returned early when `load()` failed, without recording any `CaseResult` — a suite that fails to load vanishes from both the numerator and denominator instead of failing the build, contradicting `run_tests.gd`'s own stated contract ("an empty run is a red build, never a silent green one"). Fixed in `1c264c5` with a `_record_load_failure()` helper that appends a failing `CaseResult` instead.
3. That fix's own regression test (`test_test_runner.gd`, run against a deliberately-broken fixture in `tests/fixtures/broken_suite/`) then failed *itself* on the next push — because `load()` on a script with a parse error turned out not to reliably return `null` in this Godot version at all; it can come back as a non-null `GDScript` that simply can't be instantiated, which the original fix's `script == null` check never caught. `_run_script()` now also checks `script.can_instantiate()`. The regression test was also over-specified (it asserted an exact count of 2 failures across two broken fixtures, which turned out to depend on exactly this same unverifiable Godot internal behavior) — simplified to one fixture and looser assertions on the one property that actually matters: a directory containing a broken suite must never report `all_passed()`. Fixed and confirmed in the same final push.

None of this is scoped to combat verbs specifically — it's foundational test-infrastructure hardening — but it's exactly this pass's own CI cycle that surfaced it, so it's recorded here rather than retroactively edited into Pass 1's entry. With this hardening, Pass 6 brings the suite to 269 tests total (50 combat-verb tests + 1 test-runner regression test).

**Files added:** `src/sim/{movement_profile,weapon,combat_resolver,focus_state,lean}.gd`, `tests/unit/{test_movement_profile,test_weapon,test_combat_resolver,test_focus_state,test_lean,test_input_map,test_test_runner}.gd`, `tests/fixtures/broken_suite/test_broken_type_reference.gd`.

**Files changed:** `project.godot` (`[input]` section, D11 note updated), `docs/roadmap.md` (M1 combat-verbs item checked off with scope notes), `tests/framework/test_runner.gd` (load-failure hardening).

---
