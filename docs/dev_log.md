# AFTERIMAGES: VRANOV — Development Log
**Document type:** Engineering progress log, one entry per pass. Companion to `roadmap.md` (the checkable backlog) — this file is the narrative "what happened and why," roadmap.md is the "what's left."

Engineering is being delivered in **20 planned passes**. Each pass is a substantial, self-contained chunk of work: implement, test, lint/format-verify, update this log and the roadmap, commit. The plan below is the current best estimate of how the 20 passes divide the work — later passes may adjust it (noted here when they do) as real constraints surface.

---

## The 20-pass plan (living document)

| Pass | Focus |
|---|---|
| **1** | Repo/project scaffold, custom test harness, CI. Deterministic core: `FixedMath`, `Xoshiro128StarStar` PRNG, `EventBus`, `Predicate`/`WorldQuery`. |
| 2 | `GameStateStore` + `SaveSystem` + schema versioning/migrations; fixed-tick sim harness + `InputFrame` recording/replay; determinism corpus v0 + CI job. |
| 3 | TruthSim actor model (integer-mm positions, entity IDs), grid collision, swept casts. |
| 4 | Line-of-sight (occlusion grid) + sound propagation (room/portal graph). |
| 5 | Utility AI skeleton + Sentry/Professional archetypes; WitnessSystem v1. |
| 6 | Combat verbs v1 (move/sprint/crouch/lean/aim/fire/reload/takedown/throw/Focus); InputMap actions. |
| 7 | Graybox test level wiring TruthSim+AI+combat; paranoid/credulous bot harness v0. |
| 8 | PerceptRenderer boundary (read-only truth views, op decorator pipeline) + static truth/percept separation lint. |
| 9 | First DistortionOp classes (`SubtitleDrift`, `AudioSwap`, `PhantomAudio`, `PhantomEntity`). |
| 10 | Ground verb; Clarity Mode stub. |
| 11 | MindModel (four variables, master_plan §4.4 constants, worked-example fixtures). |
| 12 | DistortionDirector (budget/deck/caps/cooling/seeding); fairness auditor v1. |
| 13 | Content pipeline: JSON schemas, content validator CLI, mission package loader + a stub mission fixture. |
| 14 | Replay Theater v0 (data model: dual-pane reconstruction, checkpoint snapshots). |
| 15 | Dialogue DSL parser/compiler + runtime graph + DialogueRunner + interrupt memory. |
| 16 | NPC mind schema + SuspicionGraph + GossipSim. |
| 17 | Claims/Provenance + DebriefLedger; liar-bot smoke test. |
| 18 | Hub skeleton: calendar/sleep economy, mind dashboard bindings, loadout data model. |
| 19 | UI shell (Theme resource, placeholder screens) + accessibility scaffolding. |
| 20 | Integration: playable prologue stub (Ground tutorial + one scripted `SubtitleDrift` + mini-Theater + trivial debrief) on placeholder art; full-suite green; log/roadmap finalized with what's left for post-Pass-20 (real art/audio/content authoring, M5-M7). |

This maps onto `roadmap.md`'s M0–M4 milestones; passes 18-20 begin reaching into M5/M6 territory (hub, UI, integration) at a skeleton/placeholder level. **No real art, music, or voice work happens in these passes** — every pass is engine/tooling/data code plus, where useful, programmer-art placeholders (colored rectangles, primitive shapes) purely so a system can be seen running. Actual asset production per `art_direction.md` and `master_plan.md` §6 is out of this 20-pass engineering arc.

---

## Pass 1 — Foundations: scaffold, deterministic core, test harness, CI

**Delivered:**
- **Project scaffold**: `project.godot` (title *Afterimages: Vranov*, 640×360 logical resolution integer-scaled per tech_guidelines D6, `warnings/treat_warnings_as_errors` on per D2), `docs/ENGINE_VERSION` pinning 4.3.0-stable, `.gitignore`/`.gitattributes` (LFS policy per tech D14), the `/src`, `/tests`, `/content`, `/tools` directory layout from tech_guidelines §5.8.
- **Deterministic core** (`src/core/`):
  - `fixed_math.gd` — Q16.16 fixed-point scalar arithmetic for MindModel/Director math (tech §3.2). Explicitly *not* for world positions (those stay plain integer mm per tech D4) — documented in the class header so a future pass doesn't reach for it in the wrong place.
  - `prng.gd` — `Xoshiro128StarStar`, seeded via four splitmix64 draws from one 64-bit seed (tech D5). One named stream per system is the intended usage pattern once TruthSim/Director/Gossip streams exist (Pass 2+).
  - `event_bus.gd` — typed pub/sub, registration-order dispatch, non-reentrant (queued) re-publish, snapshot-iterated so a handler unsubscribing itself mid-dispatch is safe (foundation_blueprints §1.2).
  - `world_query.gd` + `predicate.gd` — the Predicate language (foundation_blueprints §2): 18 leaf operators + `all`/`any`/`not` combinators, a duck-typed `WorldQuery` read interface, and a pure static `validate()` that content tooling will run in CI later (Pass 13).
- **Test harness** (`tests/framework/`, `tests/run_tests.gd`): a small custom xUnit-style harness (`AfterimageTestCase`, `AfterimageTestRunner`) — **not GUT**. See the decision note below.
- **67 unit tests** across 5 files (`test_fixed_math.gd` 15, `test_prng.gd` 10, `test_event_bus.gd` 11, `test_predicate_evaluate.gd` 20, `test_predicate_validate.gd` 11), plus a reusable `MockWorldQuery` test double in `tests/framework/`.
- **CI** (`.github/workflows/ci.yml`): a `test` job that downloads Godot 4.3-stable headless and runs the suite, and a `lint` job running `gdlint`/`gdformat --check`.
- **Title change**: the game is now titled **Afterimages: Vranov** (README, master_plan.md header + §12 risk resolved, tech_guidelines.md amendment log). The in-fiction mechanic keeps its original name, "the Afterimage."

**Decision — custom test harness instead of GUT (tech_guidelines.md §12 amendment):** `tech_guidelines.md` D13 locked in GUT. GUT has to be fetched from GitHub — as a git submodule or via the editor's AssetLib — and this authoring sandbox has no network path to GitHub (confirmed: `git`/`curl` to `github.com` return a policy-level 403 from the egress proxy, and `git submodule add` needs to resolve a real commit hash from the remote, which isn't possible offline). Rather than block Pass 1 on that or hand-copy a third-party project's source from memory, I built a small in-house harness whose assertion surface (`assert_eq`, `assert_true`, `assert_almost_eq`, …) deliberately mirrors GUT's naming, so adopting real GUT later — once someone with GitHub access pins a commit via submodule — is an addition, not a rewrite of any test file. Recorded as a proper amendment in `tech_guidelines.md` §12, not a silent deviation.

**Verification approach and its honest limits:** this sandboxed environment has no Godot binary and no working Docker daemon, so nothing in this pass has been executed inside the actual Godot runtime by me directly. Given that constraint, verification leaned on everything that *was* available:
1. **The PRNG is the highest-risk piece** (hand-porting 64-bit-unsigned bit-twiddling algorithms into a 64-bit-*signed* scripting language). Rather than trust the port by inspection, I wrote an executable Python reference implementation of splitmix64 + xoshiro128**, ran it to generate concrete output vectors for five seeds, then wrote a *second* Python model that mimics GDScript's exact arithmetic assumptions (signed 64-bit wraparound, arithmetic right-shift, explicit unsigned-shift correction via precomputed masks) and confirmed it reproduces the canonical vectors bit-for-bit before porting that exact logic to GDScript. The five seeds' first-12-draws are pinned directly into `test_prng.gd` as `VECTORS`. The one assumption that couldn't be verified without a real Godot process is that GDScript's `int` actually performs silent two's-complement wraparound on overflow and that `>>` is arithmetic — both are documented Godot 4 behaviors, and the test is written so that if either assumption is wrong, CI's real headless Godot run will fail loudly on `test_reference_vectors_match_across_seeds` rather than silently producing subtly-wrong randomness.
2. **`gdtoolkit` (gdlint/gdformat) was installed and actually run locally** (`pypi.org` is reachable through this session's egress policy even though `github.com` isn't) — every source and test file was run through `gdformat --check` and `gdlint` and is 100% clean, which catches real syntax errors, not just style: gdformat has to fully parse a file's GDScript grammar to reformat it, so a clean run is meaningful evidence the code is at least syntactically well-formed.
3. **GDScript closure semantics were treated as a known trap, not assumed**: GDScript lambdas capture outer local variables *by value*, not by reference, so a naive `var count := 0; bus.subscribe(..., func(e): count += 1)` pattern would silently produce a false test failure (the outer `count` never changes) that has nothing to do with whether `EventBus` itself is correct. Every `EventBus` test that needs a mutable counter across calls uses a small helper class with a bound `Callable(instance, "method")` instead, sidestepping the issue entirely.
4. **What's *not* verified yet**: actual execution inside Godot. The first real Godot run happens in CI on push (GitHub Actions runners have full internet access regardless of this sandbox's restrictions), and I will check that run's logs via the GitHub API/MCP tools immediately after pushing and fix forward in a follow-up commit within this same pass if anything surfaces — this is called out explicitly rather than assumed clean.

**Deferred to Pass 2** (see `roadmap.md` M0): `GameStateStore`, `SaveSystem` + schema/migration harness, the fixed-tick simulation harness, `InputFrame` recording/replay, and the determinism corpus + its CI job. M0's actual exit criterion ("inputs recorded in a stub scene replay tick-perfect") is **not yet reached** — Pass 1 built the deterministic primitives that harness will be built on top of, not the harness itself.

**Files added:** `project.godot`, `.gitignore`, `.gitattributes`, `docs/ENGINE_VERSION`, `.github/workflows/ci.yml`, `src/core/{fixed_math,prng,event_bus,world_query,predicate}.gd`, `tests/framework/{test_case,test_runner,mock_world_query}.gd`, `tests/run_tests.gd`, `tests/unit/{test_fixed_math,test_prng,test_event_bus,test_predicate_evaluate,test_predicate_validate}.gd`.

**Files changed:** `README.md` (title, status, test-running instructions), `docs/master_plan.md` (title, §12 risk resolved), `docs/tech_guidelines.md` (§12 amendment log: test harness decision, title decision), `docs/roadmap.md` (M0 checkboxes updated).

**Post-push CI verification (the loop this whole approach depends on):** first real CI run ([run 29654915740](https://github.com/DanielDmas/Afterimage/actions/runs/29654915740)) came back **lint: pass, test: fail** — checked via the GitHub Actions API, not assumed. The good news first: `gdlint`/`gdformat --check` passing for real on GitHub's infrastructure confirms the local `gdtoolkit` run in this pass was meaningful, not just theater. The failure was real and unrelated to any of the PRNG/overflow risk called out above — it was simpler and more fundamental:

```
SCRIPT ERROR: Parse Error: Could not find type "AfterimageTestRunner" in the current scope.
  at: GDScript::reload (res://tests/run_tests.gd:11)
```

**Root cause:** `.godot/` (Godot's project-scan cache, which is where global `class_name` → script-path resolution gets recorded) is correctly gitignored — it's a local editor artifact, not something to commit. But that means on a genuinely fresh checkout, Godot hasn't scanned the project yet, so none of this pass's `class_name` globals (`AfterimageTestRunner`, `FixedMath`, `Xoshiro128StarStar`, etc.) are resolvable the moment `--script res://tests/run_tests.gd` tries to parse a reference to one. Opening a project in the editor UI triggers this scan invisibly and automatically; running a script directly via `--headless --script` does not.

**Fix:** added an explicit one-time import pass — `godot --headless --path . --editor --quit` — before running the test script, in both `.github/workflows/ci.yml` and the README's local instructions. This forces the same project scan the editor would do on first open, without needing a display. Its own exit code is treated as non-authoritative (`|| true`) since a benign warning can make that pass itself report nonzero; the test-running step right after it is what actually gates the build, and it now starts from a warm class-name cache. This is exactly the verification loop described as the plan for this pass: something unverifiable locally (real Godot script-loading behavior on a cold checkout) surfaced through real CI, got diagnosed from the actual error text rather than guessed at, and was fixed forward in the same pass rather than discovered later.

**Confirmed green** ([run 29655073102](https://github.com/DanielDmas/Afterimage/actions/runs/29655073102), commit `a788729`): both jobs `success`. Pulled the actual test-step log text rather than trusting the exit code alone —
```
Afterimage test run — 5 suite(s), 67 test(s), 6849 assertion(s)
...
ALL PASSED (67/67)
```
— and `gdlint`/`gdformat --check` both clean on GitHub's own infrastructure, matching the local run byte-for-byte. **Pass 1 is closed.** One cosmetic, non-blocking note from the same log for the record: Godot prints `WARNING: ObjectDB instances leaked at exit` / `ERROR: 3 resources still in use at exit` after the test report but before process exit — harmless in a one-shot CLI runner that terminates immediately after (the OS reclaims everything), and it does not affect the exit code (`quit(0 if report.all_passed() else 1)` still returned 0). Worth a look with `--verbose` if a future pass wants zero engine noise in the log, but not worth chasing now.


---

## Pass 2 — GameStateStore, SaveSystem, fixed-tick harness, determinism corpus v0

**Delivered** (all under `src/core/`, engine-agnostic `RefCounted` classes, no scene-tree dependency, matching Pass 1's discipline):
- **`save_migrations.gd`** — the migration ladder (foundation_blueprints.md §6). `CURRENT_SCHEMA_VERSION = 2`, deliberately not 1: a real v1→v2 step is registered and exercised (v1 stored `day`/`flags` at the save's top level; v2 nests them under `campaign`), dispatched via a `match` statement rather than a self-referential `static var` dictionary of `Callable`s — the dictionary-of-callables version would have worked too, almost certainly, but it rests on an assumption about static-initializer ordering during self-referential class loading that I can't verify without running Godot, and the `match` version has no such assumption at all. Given the choice, the one with zero unverifiable assumptions won.
- **`game_state_store.gd`** — single serializable source of truth (blueprints §1.1): key-path `get_value`/`set_value` over a private `Dictionary`, `to_dict()`/`load_from_dict()` with migration on load. "All mutations are Events" is satisfied by `set_value()` publishing a `StateValueChanged` fact (old/new value) on the EventBus rather than by full event-sourced state reconstruction — that stronger guarantee is reserved for TruthSim's tick log, which is what actually has to survive re-simulation. Documented this distinction explicitly in the class doc so it doesn't read as an oversight later.
- **`save_system.gd`** — JSON + gzip persistence (tech D8/§5.3) via `PackedByteArray.compress()`/`decompress()` with `FileAccess.COMPRESSION_GZIP`. Decompression reads the required output-size hint from the gzip stream's own trailing ISIZE field (RFC 1952 §2.3.1: last 4 bytes = uncompressed size mod 2^32, little-endian) instead of inventing a custom header — this is externally-verifiable knowledge (the gzip spec itself, not a Godot-specific guess), and it means a save this writes is a genuine standalone `.gz` file, not a wrapper format. The one real assumption — that Godot's `COMPRESSION_GZIP` mode actually emits RFC 1952-framed output (magic bytes `0x1F 0x8B`, real trailer) rather than something merely similarly-named — is asserted directly in `test_save_system.gd::test_save_produces_a_valid_gzip_stream`, so a wrong assumption fails loudly in CI rather than silently corrupting saves.
- **`input_frame.gd`, `replay_log.gd`, `fixed_tick_clock.gd`** — the fixed-tick contract (tech §3.1): `InputFrame` is a generic `String -> Variant` action map (deliberately not tied to concrete verbs yet — InputMap actions land with combat in Pass 6), `ReplayLog` is the versioned seed+frames stream that later becomes both the mission save and the Theater's source, `FixedTickClock` is the 30 Hz whole-tick counter TruthSim will use instead of reading engine `delta`.
- **`tests/fixtures/stub_sim.gd`** — an explicitly disposable placeholder for TruthSim, folding PRNG + FixedMath + a toy position/accumulator over a replay's frames into a SHA-256 digest via `HashingContext`. Lives under `tests/fixtures/`, not `src/`, because it will be deleted the moment Pass 3's real TruthSim exists to test against instead.
- **`tests/corpus/run_00{1,2,3}.json`** — three recorded fixture runs (5/8/12 frames, distinct seeds), the "determinism corpus v0" roadmap.md asks for.
- **44 new tests** across 7 files (`test_input_frame` 6, `test_fixed_tick_clock` 6, `test_replay_log` 5, `test_save_migrations` 5, `test_game_state_store` 10, `test_save_system` 6, `test_determinism_corpus` 6), bringing the suite to 111 tests. All clean against `gdlint`/`gdformat --check` locally before commit (same discipline as Pass 1).

**On the determinism-corpus acceptance criterion, honestly scoped:** roadmap.md's M0 item reads "re-simulates hash-identical on Linux/Win/mac in CI." What Pass 2 actually proves is narrower and stated as such in `test_determinism_corpus.gd`'s header comment: the record→replay→hash→compare *mechanism* correctly detects both agreement (same fixture replayed twice ⇒ identical digest) and divergence (`test_digest_is_sensitive_to_an_extra_frame`, `test_digest_is_sensitive_to_run_seed`) — in-process, on one OS. I deliberately did not hand-write "golden" expected digest values into the corpus fixtures: I can't execute Godot here to compute them, and fabricating expected SHA-256 values by hand-reasoning about the stub sim's arithmetic would risk baking in *my* mistake as the "correct" answer rather than testing anything real. Self-consistency (replay twice, compare) sidesteps that risk entirely while still proving the property that actually matters. True cross-platform (Linux/Windows/macOS) byte-identical agreement — and a dedicated PR-subset-vs-nightly-full CI split — stay open, tracked in roadmap.md alongside the Windows/macOS runner gap already noted from Pass 1; both are deferred until there's a real multi-OS matrix and a corpus large enough to make the PR/nightly split matter, rather than built ahead of the need.

**Files added:** `src/core/{input_frame,replay_log,fixed_tick_clock,save_migrations,game_state_store,save_system}.gd`, `tests/fixtures/stub_sim.gd`, `tests/corpus/run_00{1,2,3}.json`, `tests/unit/{test_input_frame,test_fixed_tick_clock,test_replay_log,test_save_migrations,test_game_state_store,test_save_system,test_determinism_corpus}.gd`.

**Files changed:** `docs/roadmap.md` (M0 GameStateStore/InputFrame/determinism-CI items updated to reflect what's actually proven vs. still open).

**Post-push CI verification:** [run 29662878857](https://github.com/DanielDmas/Afterimage/actions/runs/29662878857) came back **lint: pass, test: 110/111 pass**. The good part first: both gzip assumptions this pass carried were confirmed correct on real Godot — `test_save_produces_a_valid_gzip_stream` (RFC 1952 magic bytes) and `test_save_load_save_is_byte_identical` both passed, so `COMPRESSION_GZIP` really does emit standard-framed gzip and the ISIZE-trailer decompression technique works as designed.

The one failure was a test bug, not a system bug: `test_save_and_load_round_trip_preserves_state` asserted `loaded.to_dict() == store.to_dict()` and failed with identical keys/values in different order (`{"schema_version", "campaign"}` vs. `{"campaign", "schema_version"}`). Reasoning from both results together (this failure plus the adjacent byte-identical test passing) points at `JSON.stringify()` canonicalizing key order on output independent of a Dictionary's insertion-order history: the byte-identical test's two sides both pass through `stringify()` as their last step and land on the same canonical order regardless of their respective in-memory history, which is exactly why *that* test is order-*in*sensitive while a bare `to_dict() == to_dict()` comparison across one JSON round-trip is not. Dictionary key order was never a real correctness requirement here — only that values survive the round trip — so the fix was to assert the specific values via `get_value()` instead of whole-Dictionary equality, which is both a more precise test of intent and immune to the ordering question entirely. Re-verified clean against `gdlint`/`gdformat --check` locally before pushing the fix.

**Confirmed green** ([run 29663067392](https://github.com/DanielDmas/Afterimage/actions/runs/29663067392), commit `1216601`): both jobs `success`, test log reads `ALL PASSED (111/111)` including `test_save_and_load_round_trip_preserves_state`. `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 2 is closed.**

---

## Pass 3 — TruthSim actor model: entity IDs, grid collision, swept casts

**Delivered** (all under `src/sim/`, per tech_guidelines.md §5.8's repo layout — the first files in that directory):
- **`actor.gd`, `actor_registry.gd`** — a truth-layer body (position + collision-circle radius) and its registry, assigning sequential entity IDs (tech §3.5 — never a Godot object instance ID, which isn't stable/reproducible across runs). Deterministic iteration via a sorted `all_ids()`.
- **`collision_grid.gd`** — a sparse uniform grid of blocked/free cells (a `Dictionary`, not a fixed-size array): no real level exists yet (the Pass 7 graybox room), so there's nothing to size a dense grid against, and a sparse one needs no upfront bounds. Correctly floors world coordinates to cell indices for negative coordinates too — GDScript's bare `/` truncates toward zero (C semantics), which is the wrong operation for a floor-based grid index, so `_floor_div()` corrects it explicitly (tested against negative-coordinate cases).
- **`swept_collision.gd`** — the actual "swept casts" tech_guidelines.md §3.4 asks for: swept circle-vs-AABB via the Minkowski-sum slab method (expand the static box by the circle's radius, sweep the circle's center as a point against the expanded box). Every arithmetic case — straight hit, parallel-and-outside, too-short-to-reach, already-overlapping (reports t=0), diagonal hit, moving away, exact-endpoint touch, and both zero-delta edge cases — was worked out first in an executable Python reference using exact `Fraction` arithmetic, the same "verify externally with a tool that isn't Godot, then port" discipline as Pass 1's PRNG. One deliberate, documented simplification: box corners are treated as square, not rounded (a true rounded-corner sweep needs a quadratic/sqrt solve per corner, which buys very little collision-feel difference for a top-down action game over the square-corner approximation). Actor-vs-actor collision only gets a *static* overlap test (`circles_overlap`, exact integer distance-squared, no sqrt) — true swept circle-vs-moving-circle collision is deferred until multiple simultaneously-moving bodies actually need it (AI in Pass 5+), not built ahead of that need.
- **`fixed_math.gd` gained `to_int_trunc()`** — truncates toward zero for both signs, needed because `move_with_collision()`'s existing `to_int_floor()` would over-shoot a stopped movement further in the negative direction for negative deltas (flooring a negative fraction rounds *away* from zero, which is exactly wrong for "stop at or before the impact point" semantics regardless of travel direction).
- **`truth_sim.gd`** — the tick orchestrator: one player `Actor`, `step(InputFrame)` reads `move_x`/`move_y` as a raw per-tick millimeter delta and resolves it through `SweptCollision.move_with_collision()` against the grid, publishing `ActorMoved` only when a move actually changed position. Deliberately narrow scope: no AI, no combat, no witnesses (Pass 5/6/4 respectively) — this is the foundation those passes build onto, and `step()`'s signature (one tick, one InputFrame) is meant to stay stable as they plug in.
- **39 new tests** across 4 new files (`test_actor_registry` 6, `test_collision_grid` 7, `test_swept_collision` 16, `test_truth_sim` 9) plus one addition to `test_fixed_math` (`to_int_trunc`), bringing the suite to 150 tests. All clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — the corpus stays on StubSim, not TruthSim, for now:** Pass 2's dev_log entry said "delete `stub_sim.gd` once TruthSim lands and the corpus points at that instead." On reflection, doing that *now* would make the corpus mechanism *less* representative, not more — a movement-only TruthSim with no walls in most fixtures has no more interesting divergence surface than the toy StubSim already proves against. `test_determinism_corpus.gd` and its 3 JSON fixtures are left untouched; TruthSim instead gets its own direct correctness tests (exact expected positions, not opaque hash comparisons), which is a better fit for verifying *this* kind of behavior anyway — collision outcomes are things I can and did work out exactly by hand/Python, not just check for reproducibility. Revisiting "point the corpus at real TruthSim" is deferred until AI and combat (Pass 5/6) give it enough real behavior to be worth the swap. Noted here as the kind of plan adjustment the top of this log already allows for ("later passes may adjust it as real constraints surface").

**Files added:** `src/sim/{actor,actor_registry,collision_grid,swept_collision,truth_sim}.gd`, `tests/unit/{test_actor_registry,test_collision_grid,test_swept_collision,test_truth_sim}.gd`.

**Files changed:** `src/core/fixed_math.gd` (+`to_int_trunc`), `tests/unit/test_fixed_math.gd` (+1 test), `docs/roadmap.md` (M1 TruthSim/collision item updated).

**Confirmed green** ([run 29663447877](https://github.com/DanielDmas/Afterimage/actions/runs/29663447877), commit `660968c`): both jobs `success`, test log reads `ALL PASSED (150/150)`. All 16 `test_swept_collision.gd` cases passed — including the diagonal-hit and already-overlapping-reports-t-zero cases that most depend on the ported slab-test arithmetic being right, not just the easy straight-line cases — and the aggregate zero-failures count confirms `test_collision_grid.gd`'s negative-coordinate floor-division tests and `test_actor_registry.gd` passed clean too. `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 3 is closed.**

---

## Pass 4 — Line-of-sight and sound propagation

**Delivered** (`src/sim/`, continuing Pass 3's foundation):
- **`line_of_sight.gd`** — `cells_along_line()` implements Bresenham's line algorithm (the standard integer-only err/e2 formulation, symmetric across all octants), verified against an executable Python reference across nine cases (horizontal, vertical, diagonal, both shallow and steep slopes, both negative directions, a single point, and a reversed-direction check) before porting. `has_clear_line()` builds on it against `CollisionGrid`'s existing blocked-cell data — deliberately *not* a second parallel occlusion grid, since wall geometry blocking movement and wall geometry blocking vision is the same geometry, and maintaining two copies would just be duplicated data with no behavioral upside. The two endpoints' own cells are exempted from the blocking check (an actor standing in a doorway, or anywhere else, shouldn't have their own occupied cell block their sightline).
- **`sound_graph.gd`** — an abstract room/portal graph (rooms are opaque string IDs; no real level exists yet). A sound's loudness is a subtractive integer budget that portals attenuate when crossed; `propagate()` finds the *loudest possible* arrival at every reachable room — a shortest-path problem (minimize total attenuation) — via iterative relaxation rather than a priority-queue Dijkstra, since real room graphs will be small (tens of rooms, not thousands) and relaxation is simpler to get right and to verify than a heap-based implementation. A room every path attenuates to ≤0 is *omitted* from the result, not present at 0 — tested explicitly, along with a case where a longer, less-attenuating path must win over a shorter, noisier direct one.
- **26 new tests** across 2 new files (`test_line_of_sight` 14, `test_sound_graph` 12), bringing the suite to 176 tests. Clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — no vision cone yet:** master_plan.md describes AI senses as "vision cones, sound propagation, last-known-position memory" (§5.5), and it was tempting to build the angular field-of-view wrapper (range + half-angle around a facing direction) as part of this pass. I started working through the math — an angle-free cone test using squared dot products to avoid `atan2`/`sqrt` in sim code (per tech_guidelines §3.2's "no ad-hoc float math") is workable, but only if the facing-direction vector is a small normalized magnitude rather than a raw world-space displacement, or the intermediate `dot²` and `|a|²·|b|²` terms overflow int64 — and even done carefully, it's a nontrivial piece of arithmetic with no actual caller to verify it against yet. The roadmap and dev_log's own Pass 4 scope only ever named "line-of-sight + sound propagation," not vision cones; that phrase belongs to Pass 5 (Sentry/Professional AI), which is where it has a real consumer and a real test (does this specific archetype notice the player in this specific scenario). Deferred there rather than built speculatively now.

**Files added:** `src/sim/{line_of_sight,sound_graph}.gd`, `tests/unit/{test_line_of_sight,test_sound_graph}.gd`.

**Files changed:** `docs/roadmap.md` (M1 LOS/sound-propagation item checked off with scope notes).

**Confirmed green** ([run 29665845419](https://github.com/DanielDmas/Afterimage/actions/runs/29665845419), commit `b5b15a2`): both jobs `success`, test log reads `ALL PASSED (176/176)`. All 14 `test_line_of_sight.gd` cases passed (including the negative-direction and reversed-direction Bresenham cases) and all 12 `test_sound_graph.gd` cases passed (including the loudest-path-wins-over-a-shorter-noisier-route relaxation test). `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 4 is closed.**

---

## Pass 5 — Vision cones, utility AI, Sentry/Professional archetypes, WitnessSystem v1

**Delivered** (`src/sim/`):
- **`vision_cone.gd`** — the angular field-of-view test deferred from Pass 4. Avoids `atan2()`/`sqrt()` entirely via a squared dot-product comparison (`dot(a,b)² >= |a|²|b|²cos²(θ)`, valid whenever `dot(a,b) >= 0`, i.e. for any half-angle under 90° — documented as this class's supported contract). Verified against a Python reference using three angles (30°/45°/60°) chosen specifically because their cos² values are exact rationals (3/4, 1/2, 1/4, via the double-angle identity) rather than approximations, across nine cases (dead ahead, exactly on the cone edge, just outside, out of range, directly behind, and both a narrow and a wide cone's inside/outside pair). The one real arithmetic risk — `facing_dir` needs a bounded magnitude to keep `facing_sq * scaled_dist_sq` inside int64 for a ~100m level — was checked numerically (not guessed): a bound of 20,000mm overflows, 15,000mm is right at the edge, 10,000mm has comfortable margin, so `FACING_COMPONENT_MAX = 10000` is enforced by an assert rather than left as a silent risk. `cos_sq_half_angle_fx_from_degrees()` is the one place `cos()` (a transcendental function) appears in this pass — explicitly content-authoring-time only, converting an authored half-angle once, never in the per-tick hot path, the same carve-out tech_guidelines §3.1 already grants duration-to-ticks conversion.
- **`ai_utility.gd`** — all five states master_plan §5.5 names (patrol/investigate/engage/flee/report) as pure functions of a `Perception` snapshot, so the decision logic is exhaustively testable without any hidden state. Caught a real design bug before it ever reached CI: my first draft scored `just_spotted` (REPORT) at 90 and `can_see_target` (ENGAGE) at 100, meaning REPORT could *never* win on the tick it matters most — re-deriving the actual numbers by hand (not just trusting the design intent) surfaced this, and REPORT now scores 110, deliberately outranking ENGAGE for exactly the one tick sight is newly acquired ("call it in, then engage from next tick"). `FLEE` is real and tested but currently inert: `threat_level` defaults to 0 and nothing populates it yet, since that's genuine combat/health data Pass 6 owns — the five-state shape is complete now without fabricating data that doesn't exist.
- **`ai_archetype.gd` + `ai_agent.gd`** — Sentry (6000mm range, 50° half-angle, no radio) and Professional (8000mm, 60°, reports on first sighting) as perception-parameter configurations, not bespoke behavior trees: master_plan's tactical flavor ("Professional... flanks, checks corners") needs pathfinding and combat that don't exist yet, and building unique behavior code for it now would have nothing real to execute against. `AiAgent` owns the one piece of state a pure utility function can't (memory of last-known position, and whether sight was *newly* acquired this tick vs. sustained) and is what actually calls `VisionCone` + `LineOfSight` together each tick.
- **`witness_system.gd`** — "who truly saw what," reusing the exact same `VisionCone`+`LineOfSight` query AI perception uses (a witness is anyone who genuinely could have seen an event, independent of what the player later believes or claims — the truth-layer fact the debrief's truth-delta and suspicion propagation will eventually read from).
- **42 new tests** across 5 new files (`test_vision_cone` 11, `test_ai_utility` 12, `test_ai_archetype` 4, `test_ai_agent` 8, `test_witness_system` 7), bringing the suite to 218 tests. Clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — literal AC deferred, the AI it needs is not:** roadmap.md's Sentry/Professional AC reads "paranoid/credulous bot stubs finish the graybox room" — neither bots nor a graybox room exist yet (both are Pass 7). This pass delivers what those bots will actually run *on* (the perception + decision-making), with its own hand-computed correctness tests in the meantime, exactly the same pattern used for Pass 3's collision AC and Pass 4's noise-ring AC.

**Files added:** `src/sim/{vision_cone,ai_utility,ai_archetype,ai_agent,witness_system}.gd`, `tests/unit/{test_vision_cone,test_ai_utility,test_ai_archetype,test_ai_agent,test_witness_system}.gd`.

**Files changed:** `docs/roadmap.md` (M1 Sentry/Professional and WitnessSystem items checked off with scope notes).

**Confirmed green** ([run 29666164688](https://github.com/DanielDmas/Afterimage/actions/runs/29666164688), commit `5ddaea7`): both jobs `success`, test log reads `ALL PASSED (218/218)`. All 11 `test_vision_cone.gd` cases passed (including the exactly-on-the-cone-edge and both narrow/wide-cone inside/outside pairs derived from the exact-rational cos² values), and every `test_ai_utility.gd`, `test_ai_archetype.gd`, `test_ai_agent.gd`, and `test_witness_system.gd` case passed — including `test_just_spotted_outranks_engage_on_the_spotting_tick` and `test_professional_reports_on_first_sighting_then_engages`, which are exactly the pre-CI REPORT-vs-ENGAGE scoring bug fix this pass's summary called out. `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 5 is closed.**

---

## Pass 6 — Combat verbs v1, InputMap actions

**Delivered** (`src/sim/`, all standalone and tested, *not yet wired into TruthSim.step()* — see scoping decision below):
- **`movement_profile.gd`** — walk/sprint/crouch as authored integer mm/tick speeds (never a runtime float m/s÷30Hz division — positions and their deltas are plain integer mm, tech_guidelines D4), plus each mode's noise contribution (`master_plan §4.9`: "sprint (noise)"). `resolve_delta()` turns a quantized ±1 direction into the per-tick delta `TruthSim.step()` already expects — its own class doc has said since Pass 3 that this translation is "a combat-verb concern (Pass 6), not this layer's." Diagonal movement is deliberately not speed-normalized this pass — documented as a feel-tuning question for the Pass 7 graybox playtest, not a correctness one.
- **`weapon.gd`** — ammo/reload as a tick-counted state machine (never wall-clock time, so a reload finishes on the same tick under any replay), with `cz75()`/`suppressed_32()` named constructors mirroring master_plan §4.9's sidearm pair (loud/reliable vs. quiet/weak) — the same "archetype as a factory constructor" style Pass 5's `AiArchetype.sentry()/professional()` established. The Argus-issue Škorpion is deliberately omitted: carrying it prices as a suspicion entry regardless of cover (§4.7), which has no model yet.
- **`combat_resolver.gd`** — `resolve_fire()` reuses Pass 5's `VisionCone` (a tight, hardcoded-offline `cos²(3°)` "dead-on" aim cone — never computed via `cos()` at runtime, so firing never depends on a platform's libm agreeing with the machine that recorded a replay) plus Pass 4's `LineOfSight`, rather than inventing ray-vs-circle geometry; picks the nearest unobstructed, in-cone, in-range target. `resolve_takedown()` is proximity + `LineOfSight`, no cone (a takedown is whatever's at contact range, not aimed). `resolve_throw()` clamps to a max range by rejecting over-range throws outright (a true clamped landing point would need a sqrt-based vector normalization this codebase avoids in sim code, tech_guidelines §3.2). `is_noise_heard_at()`/`hearing_range_mm()` give sprint/gunfire/thrown-object noise a working open-air hearing check now, as a documented v1 stand-in for SoundGraph's room/portal propagation (Pass 4) — which has no authored rooms to route through before Pass 7's graybox level.
- **`focus_state.gd`** — Focus's activation/duration/cooldown as a tick-counted resource gate (3s = 90 ticks active, 5s = 150 ticks cooldown, `activation_count` tracked for later billing). Deliberately does *not* implement the "0.4× time" effect itself: TruthSim's tick rate is fixed at 30Hz always (the determinism contract doesn't allow the sim's own rate to vary), so that has to be a presentation-layer trick (slowed audio/visual playback over the same fixed-rate ticks) with no renderer to attach to before Pass 7. The `+6 acute stress` cost from §4.9 is likewise not billed yet — MindModel (Pass 11) doesn't exist to receive it. Same "build the gate now, wire the consumer later" pattern Pass 5's `AiUtility` used for FLEE's `threat_level`.
- **`lean.gd`** — offsets the position a `LineOfSight` query is made *from*, sideways relative to a cardinal facing, without moving the actor's own collision circle. A 90° rotation of a unit cardinal vector is exact integer arithmetic (swap components, negate one) — no general-rotation trig needed. Composes directly with the existing `LineOfSight.has_clear_line()` with no changes to that class.
- **InputMap actions** (`project.godot`, tech_guidelines D11): all 14 combat-verb actions (`move_up/down/left/right`, `sprint`, `crouch`, `lean_left/right`, `aim`, `fire`, `reload`, `takedown`, `throw`, `focus`) declared with empty `events` arrays — no default key/button bound yet. Hand-authoring a Godot `InputEventKey` resource literal with no local editor available to generate or verify it risked a wrong keycode more than it was worth; key-binding choice belongs to ux_charter.md's "full remapping" scope (Pass 19), not this pass's verb *logic*. `tests/unit/test_input_map.gd` asserts every action actually registered — a real, CI-executed check on the one thing that genuinely could have gone wrong by hand (gdlint/gdformat don't parse `project.godot` at all).
- **50 new tests** across 6 new files (`test_movement_profile` 7, `test_weapon` 10, `test_combat_resolver` 17, `test_focus_state` 7, `test_lean` 8, `test_input_map` 1), bringing the suite to 268 tests. `resolve_fire()`'s cone-boundary cases were checked against `VisionCone.point_in_cone()`'s exact integer formula by hand before being chosen (a target at (5000,100) is ~1.146° off-axis, inside the 3° cone; (5000,400) is ~4.57° off-axis, outside it) — the same "verify the exact arithmetic first" discipline as every prior pass's real-arithmetic risk. Clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — verbs built standalone, not wired into TruthSim, and the tuning-checklist AC isn't satisfiable yet:** roadmap.md's AC for this item is "every §4.9 tuning-checklist line has a recorded pass" — that checklist (stop-distance feel, hitstop timing, aim-assist curves, screen-shake budget, etc.) is inherently a playtest verdict against a real, rendered, playable build, and no such build exists before Pass 7's graybox room. Recording a real pass on any tuning-checklist line before then would be fabricating a playtest that didn't happen. What this pass delivers instead is exactly what Pass 4 and Pass 5 delivered ahead of their own wiring passes: every verb as a standalone, directly-tested module, ready for Pass 7 to actually plug into `TruthSim.step()` and run against AI for the first time. `test_truth_sim.gd` is untouched — `TruthSim`'s existing move_x/move_y contract (an already-resolved per-tick mm delta) is unchanged; `MovementProfile.resolve_delta()` is what will produce that delta once something calls it, not a replacement for `TruthSim.step()`'s own signature.

**CI caught a real bug, and chasing it down surfaced two more:**

1. The first push (`4a735b5`) failed at real Godot compile time with `Cannot pass a value of type "Lean.Side" as "Side"` — a genuine Godot 4.3 GDScript limitation where a static method's parameter typed with its own class's *unqualified* inner-enum name doesn't unify with the *fully-qualified* name (`Lean.Side`) a caller in a different file must write. No local Godot editor could have caught this before pushing. `lean.gd`'s `peek_origin()` parameter is now typed `int` instead (enums are plain ints underneath). Fixed in `09b5843`.
2. That run still printed `ALL PASSED (260/260)` — `test_lean.gd`'s 8 tests were silently missing from the count entirely, not failing. `AfterimageTestRunner._run_script()` (`tests/framework/test_runner.gd`, built in Pass 1) called `push_error()` and returned early when `load()` failed, without recording any `CaseResult` — a suite that fails to load vanishes from both the numerator and denominator instead of failing the build, contradicting `run_tests.gd`'s own stated contract ("an empty run is a red build, never a silent green one"). Fixed in `1c264c5` with a `_record_load_failure()` helper that appends a failing `CaseResult` instead.
3. That fix's own regression test (`test_test_runner.gd`, run against a deliberately-broken fixture in `tests/fixtures/broken_suite/`) then failed *itself* on the next push — because `load()` on a script with a parse error turned out not to reliably return `null` in this Godot version at all; it can come back as a non-null `GDScript` that simply can't be instantiated, which the original fix's `script == null` check never caught. `_run_script()` now also checks `script.can_instantiate()`. The regression test was also over-specified (it asserted an exact count of 2 failures across two broken fixtures, which turned out to depend on exactly this same unverifiable Godot internal behavior) — simplified to one fixture and looser assertions on the one property that actually matters: a directory containing a broken suite must never report `all_passed()`. Fixed in `923a277`.

None of this is scoped to combat verbs specifically — it's foundational test-infrastructure hardening — but it's exactly this pass's own CI cycle that surfaced it, so it's recorded here rather than retroactively edited into Pass 1's entry. With this hardening, Pass 6 brings the suite to 269 tests total (50 combat-verb tests + 1 test-runner regression test).

**Files added:** `src/sim/{movement_profile,weapon,combat_resolver,focus_state,lean}.gd`, `tests/unit/{test_movement_profile,test_weapon,test_combat_resolver,test_focus_state,test_lean,test_input_map,test_test_runner}.gd`, `tests/fixtures/broken_suite/test_broken_type_reference.gd`.

**Files changed:** `project.godot` (`[input]` section, D11 note updated), `docs/roadmap.md` (M1 combat-verbs item checked off with scope notes), `tests/framework/test_runner.gd` (load-failure hardening).

**Confirmed green** ([run 29667111582](https://github.com/DanielDmas/Afterimage/actions/runs/29667111582), commit `923a277`): both jobs `success`, test log reads `ALL PASSED (269/269)`. All 8 `test_lean.gd` cases passed (the `Lean.Side` fix holds), and `test_test_runner.gd`'s regression test passed — confirming a broken suite is now recorded as a failure rather than silently dropped, without disturbing the rest of the real suite. No stray `SCRIPT ERROR` noise in the log this time. `gdlint`/`gdformat --check` clean on GitHub's infrastructure. This closes a three-round CI cycle (`4a735b5` → `09b5843` → `1c264c5` → `923a277`) that found and fixed a real language-level type-checking gotcha, a real silent-failure gap in shared test infrastructure, and a real gap in that fix's own first attempt — the exact kind of thing "verify externally, then port" can't catch when the thing under test is Godot's own runtime behavior rather than arithmetic a Python reference can check ahead of time. **Pass 6 is closed.**

---

## Pass 7 — Graybox test level wiring TruthSim+AI+combat; bot harness v0

**Delivered:** everything Pass 3-6 built standalone (movement, LineOfSight/SoundGraph, VisionCone/AiAgent, MovementProfile/Weapon/CombatResolver/FocusState) is now wired together into one real, playable-in-the-headless-sense fight.

- **`src/sim/actor.gd`** gained `hit_points`/`facing_dir` as plain public fields (the same reasoning `position`/`radius_mm` already got: master_plan §4.9's damage model, "2-4 hits"/"1-3 hits," is a real-playtest tuning question, not something to over-engineer a class hierarchy around before that data exists), plus `is_alive()`/`apply_damage()` (clamped at zero — no negative overkill).
- **`src/sim/truth_sim.gd`** is where the actual wiring happens, and `step()` keeps its exact Pass 3 signature (one InputFrame, one tick) as promised in that class's own doc comment since Pass 3 — every new verb is a new optional `InputFrame` key, not a breaking change to `move_x`/`move_y`'s contract (`test_truth_sim.gd`'s original 9 tests are untouched):
  - `spawn_ai()` adds an AI-controlled `Actor` with its own `AiAgent` and a default `Weapon.cz75()` (real per-archetype loadouts are content-authoring data that doesn't exist yet — same deferral as Pass 6's Škorpion suspicion consequence).
  - Player verbs actually resolve: fire (against every living other actor, via `CombatResolver.resolve_fire` — Pass 5/6's vision-cone/LOS primitives, no new geometry), reload (tick-counted, Pass 6's `Weapon` state machine), takedown (proximity+LOS instant kill), throw (lands or doesn't, emits a noise event), focus (activates the Pass 6 resource gate). A new optional `"sprinting"` InputFrame flag (independent of the movement delta itself) classifies this tick's noise without touching the existing move contract.
  - Every AI actor perceives the player for real each tick (`AiAgent.perceive_and_decide`, fed a `heard_noise` bool computed from this tick's actual noise events via `CombatResolver.is_noise_heard_at` — sprint/gunfire/thrown-object noise all feed real AI perception now, not just unit-tested primitives) and, v0-scoped, acts only on `ENGAGE` by firing back with its own weapon. `PATROL`/`INVESTIGATE`/`FLEE`/`REPORT` have no movement or radio consequence yet — a real patrol route needs authored waypoint data, and `REPORT`'s backup-calling needs other AI to call in; neither exists before a real content pipeline (Pass 13+). This is still a real fight: an AI that spots the player can kill them, and vice versa.
  - New events: `WeaponFired`, `TakedownPerformed`, `ThrowLanded`, `ActorDowned` (published the same "facts, past tense" way `ActorMoved` already was).
  - New read-only accessors for tests (and, later, a debug HUD): `ai_current_state()`, `is_focus_active()`, `focus_activation_count()`, `player_weapon_ammo()`, `player_weapon_is_reloading()`.
- **`tests/fixtures/graybox_room.gd`** (`GrayboxRoom`) — a code-defined perimeter-walled room, explicitly *not* a real Godot scene: no level-authoring pipeline exists before Pass 13's content pipeline, and "content is data not code" means even a real graybox belongs in an authored format, not hardcoded GDScript. The same kind of disposable stand-in Pass 2's `StubSim` was for the determinism corpus.
- **`tests/fixtures/bot_inputs.gd`** (`BotInputs`) — "aggressive" (closes distance, fires whenever it has a shot) and "cautious" (retreats, never fires) scripted input generators, explicitly *not* master_plan's actual paranoid/credulous bot pair (those specifically probe Ground usage, which doesn't exist before Pass 10) — named for what they really test here.
- **35 new tests** across 4 new files (`test_actor` 5, `test_truth_sim_combat` 12, `test_bot_harness` 3) plus a fixture split: `test_truth_sim.gd` crossed gdlint's max-public-methods limit once the Pass 7 wiring tests were added, so they moved to a new `test_truth_sim_combat.gd` — the same split `test_predicate.gd` needed pre-Pass-1. Bringing the suite to 289 tests (269 + 5 + 12 + 3 = 289; `test_truth_sim.gd` itself is net unchanged at 9). `test_bot_harness.gd`'s two-tick aggressive-encounter outcome (AI dead, player at 2/3 HP) was hand-traced through the exact tick-by-tick resolution order in `TruthSim.step()` before being asserted — the same "verify the exact arithmetic first" discipline as every prior pass's real-arithmetic risk, applied here to *ordering* logic instead of geometry. Clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — "fun" is not an engineering deliverable:** roadmap.md's graybox-room AC ("3 external testers rate the sober fight fun") needs real human playtesters against a real, rendered build with real art and controls — none of which exist or can exist from an engineering pass running in this sandbox. What this pass delivers is the thing that AC is actually gating on being *possible*: a fight that resolves deterministically tick-by-tick, replays identically from the same recorded inputs, and is driven by every system built since Pass 3 instead of standalone unit fixtures. M1's exit criterion ("tick-perfect" half) is reached; the "already fun" half waits on real content and real testers, tracked openly rather than claimed.

**Files added:** `tests/unit/{test_actor,test_truth_sim_combat,test_bot_harness}.gd`, `tests/fixtures/{graybox_room,bot_inputs}.gd`. No new `src/sim/` files this pass — every new class (`Actor`'s combat fields, `TruthSim`'s wiring) extends files that already existed.

**Files changed:** `src/sim/{actor,truth_sim}.gd`, `docs/roadmap.md` (M1 graybox-room item checked off with scope notes; M1 exit-criterion note updated). `tests/unit/test_truth_sim.gd` ends up byte-identical to before — its Pass 7 additions were written there first, then moved wholesale to the new `test_truth_sim_combat.gd` once the file crossed gdlint's method-count limit.

**CI found one real bug, and it was in the test, not the sim:** the first push (`1cc32d7`) failed a single test, `test_reload_restores_ammo_after_its_tick_count` — everything else, including the hand-traced two-tick combat outcome in `test_bot_harness.gd` (this pass's biggest unverified-until-CI assumption) and every other fire/reload/takedown/throw/focus wiring test, passed clean on the first try. The actual bug: `Weapon.advance_tick()` runs unconditionally every `step()` call, so the tick that *requests* reload already consumes its first countdown decrement — the test's timeline assumed `reload_ticks` further ticks were needed after the requesting tick, when it's `reload_ticks - 1`. Production code (`TruthSim`, `Weapon`) was correct throughout; only the test's own arithmetic was off by one. Fixed in `12d5367`.

**Confirmed green** ([run 29678333376](https://github.com/DanielDmas/Afterimage/actions/runs/29678333376), commit `12d5367`): both jobs `success`, test log reads `ALL PASSED (289/289)`. `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 7 is closed.**

---

## Pass 8 — PerceptRenderer boundary: read-only truth views, op decorator pipeline

**Delivered:** the start of M2 ("The Split") — master_plan.md §5.2's non-negotiable architecture invariant ("PerceptRenderer has read-only access to TruthSim; nothing in the percept path can mutate truth") is now a real, CI-enforced boundary, not just a paragraph in a design doc.

- **`TruthSim.capture_percept_snapshot()`** (`src/sim/truth_sim.gd`) is the one deliberate seam where truth hands data upward. Every field is a plain value — `int`/`bool`/`Vector2i` copied out of each `Actor` — never a reference to the `Actor` or the `TruthSim` itself. This is what actually makes "read-only" true rather than a naming convention: the returned `Dictionary` structurally cannot be used to reach back into truth's real state, because it contains no object references to truth at all. `test_truth_sim_percept.gd`'s core test proves exactly this — it mutates the returned snapshot (including appending a fake actor) and asserts the real `TruthSim` is completely unaffected.
- **`src/percept/percept_op.gd`** (`PerceptOp`) — the decorator base class: `apply(snapshot: Dictionary) -> Dictionary`, identity by default. **`src/percept/percept_renderer.gd`** (`PerceptRenderer`) — `render(truth_snapshot, active_ops)` composes them in order over a deep copy, so ops can never be seen to have mutated the caller's original. No real op classes yet: `SubtitleDrift`/`AudioSwap`/`PhantomAudio`/`PhantomEntity` (master_plan §4.2) are Pass 9's job, once a real percept-side renderer consuming this snapshot gives them a reason to exist — the same "build the pipe, not the water" order every prior pass has used for its own standalone-before-wired pieces (Pass 4's LOS before Pass 5's vision cone, Pass 6's verbs before Pass 7's wiring).
- **`tools/percept_truth_boundary_lint.py`** — the roadmap AC's literal "static check... CI lint on module imports," now real and wired into `.github/workflows/ci.yml`'s `lint` job. GDScript's global `class_name` mechanism means sim-layer classes are usable from anywhere without an explicit import statement, so a plain import scan wouldn't catch a violation — this scans for the class names themselves as whole-word identifiers in percept-side *code* (comment lines stripped first, so this file's own doc comments mentioning "TruthSim"/"Actor" for explanation aren't false positives). The denylist is derived from every `class_name` declaration found under `src/sim/` at scan time, not hardcoded, so it stays correct automatically as the sim layer grows in later passes. Verified locally two ways before committing: a clean pass against the real repo (17 forbidden names, 0 violations), and a deliberately-reintroduced violation (a throwaway file directly mutating `Actor.position` through a `TruthSim` reference) that the tool correctly caught and reported by file/line/class name, then removed.
- **8 new tests** across 3 new files (`test_percept_op` 1, `test_percept_renderer` 3, `test_truth_sim_percept` 4), bringing the suite to 297 tests. Clean against `gdlint`/`gdformat --check` locally before commit.

**Scoping decision — pipeline now, ops later, matching every prior pass's own order:** it would have been easy to also sketch a placeholder `SubtitleDrift` here to "prove the pipeline works end to end," but that's exactly the premature-scope pattern this log has avoided since Pass 4 (vision cones deferred until Sentry/Professional gave them a real consumer in Pass 5). A real op needs a real percept-side renderer actually consuming its output to be worth building against — that's Pass 9's explicit job in the 20-pass plan, not this one's.

**Files added:** `src/percept/{percept_op,percept_renderer}.gd`, `tools/percept_truth_boundary_lint.py`, `tests/unit/{test_percept_op,test_percept_renderer,test_truth_sim_percept}.gd`.

**Files changed:** `src/sim/truth_sim.gd` (+`capture_percept_snapshot()`), `.github/workflows/ci.yml` (new lint step), `docs/roadmap.md` (M2 PerceptRenderer-boundary item checked off).

**Confirmed green** ([run 29678619077](https://github.com/DanielDmas/Afterimage/actions/runs/29678619077), commit `8160d33`): both jobs `success` — the lint job's new "percept/truth architecture boundary" step passed specifically, not just the job as a whole — and the test log reads `ALL PASSED (297/297)`. `test_truth_sim_percept.gd`'s `test_mutating_the_returned_snapshot_does_not_affect_truth_sim` (the core read-only guarantee) and every `test_percept_op.gd`/`test_percept_renderer.gd` case passed clean on the first push — no fix-forward needed this time. `gdlint`/`gdformat --check` clean on GitHub's infrastructure. **Pass 8 is closed.**

---

## Pass 9 — First DistortionOp classes: SubtitleDrift, AudioSwap, PhantomAudio, PhantomEntity

**Delivered:** the first four op classes from master_plan.md §4.2's ten-class taxonomy, built on Pass 8's `PerceptOp`/`PerceptRenderer` pipeline.

- **`src/percept/distortion_op.gd`** (`DistortionOp extends PerceptOp`) — the shared metadata every op class in §4.2's table ships with: `op_class`, `tier`, `cost` (§4.3's director purchase price), `dramatic_intent` (dread/doubt/grief/paranoia), and `fairness_tags` (which Fairness Charter rules, §4.5, this op complies with by construction — read later by the fairness auditor, Pass 12's job, not enforced by one yet). Adds `resolve_grounded(snapshot) -> Dictionary`: the Ground-response behavior (§4.2's table column) as data, identity by default, ready for Pass 10's Ground verb to call once it exists. Subclasses set their fields directly in their own `_init()` rather than through a base constructor — avoids relying on GDScript `super._init()` chaining, one less untested-in-this-sandbox behavior in an already content-facing pass.
- **`SubtitleDrift`** (tier 1, cost 5) — operates on an optional `snapshot["subtitle"]` key (`{speaker_id, true_text}`); dialogue doesn't exist as a truth concept yet (Pass 15's DialogueRunner), so this is a no-op when that key is absent, tested against a hand-built synthetic snapshot rather than a real dialogue source — the same discipline Pass 8's `PerceptOp`/`PerceptRenderer` tests already established.
- **`AudioSwap`** (tier 1, cost 8) and **`PhantomAudio`** (tier 1, cost 8) — operate on `snapshot["sound_events"]`, which is *real* now: `TruthSim.capture_percept_snapshot()` (`src/sim/truth_sim.gd`) gained a `"sound_events"` field this pass, sourced from the exact same per-tick noise events Pass 7's AI hearing already computed internally (sprint/gunfire/thrown-object), now also tagged with a `"tag"`/`"source_id"` and exposed upward. `AudioSwap` relabels one real event's `rendered_tag`; `PhantomAudio` appends a synthetic one with no backing truth (`source_id: -1`). Both tag their own additions with `get_instance_id()` so `resolve_grounded()` can undo exactly what *that instance* did, correctly disambiguating multiple simultaneous instances of the same op — which Ground's own contract (§4.6: "resolves all active ops in scope simultaneously") makes the normal case, not an edge one.
- **`PhantomEntity`** (tier 3, cost 25) — appends a fake entry to `snapshot["actors"]` with a negative id (`-get_instance_id()`, guaranteed never to collide with a real `ActorRegistry` id, which always starts at 1). Charter rule 1 ("phantoms never deal damage and never block movement or bullets") is structural here, not just documented: this op only ever appends to a percept-side Dictionary that `src/sim/` never reads back at all (Pass 8's boundary, CI-enforced by `tools/percept_truth_boundary_lint.py`) — there is no code path by which a phantom's presence here could touch a truth outcome.
- **26 new tests** across 7 new files (`test_distortion_op` 2, `test_subtitle_drift` 5, `test_audio_swap` 5, `test_phantom_audio` 4, `test_phantom_entity` 5, `test_distortion_pipeline_integration` 1, plus 4 more added to `test_truth_sim_percept.gd`), bringing the suite to 323 tests. The integration test is the capstone: real `TruthSim` combat data → `capture_percept_snapshot()` → `PerceptRenderer.render()` with `PhantomEntity` + `AudioSwap` both active, proving the whole stack composes over genuine data, not just each op's own synthetic fixture. Clean against `gdlint`/`gdformat --check` and `tools/percept_truth_boundary_lint.py` locally before commit.

**Scoping decision — Ground response is data now, Ground itself is Pass 10; Theater disclosure is Pass 14:** roadmap.md's AC for this item ("each op's Ground response + Theater disclosure works in the graybox") needs two systems that don't exist yet. What this pass delivers is exactly what those systems will need once they do: `resolve_grounded()` on every op, and — for the Theater's eventual "what did this op actually do" accounting — enough structure (`op_class`, `tier`, `cost`, `dramatic_intent`) that a future Theater doesn't need this pass's classes touched again. Same class of deferral as every prior pass's unsatisfiable-yet AC.

**Files added:** `src/percept/{distortion_op,subtitle_drift,audio_swap,phantom_audio,phantom_entity}.gd`, `tests/unit/{test_distortion_op,test_subtitle_drift,test_audio_swap,test_phantom_audio,test_phantom_entity,test_distortion_pipeline_integration}.gd`.

**Files changed:** `src/sim/truth_sim.gd` (noise events gain `tag`/`source_id`; `capture_percept_snapshot()` gains `"sound_events"`), `tests/unit/test_truth_sim_percept.gd` (+4 tests), `docs/roadmap.md` (M2 ops item checked off with scope notes).

**CI caught one real bug, in `PhantomEntity`'s id scheme:** the first push (`12854e8`) failed a single test, `test_apply_adds_a_phantom_actor_with_a_negative_id` — everything else, including the capstone integration test and both `resolve_grounded`-removes-exactly-this-instance's-phantom tests, passed clean. The cause: Godot 4 sets an internal flag bit on a `RefCounted` object's instance id that can make `get_instance_id()` read back as already-negative in GDScript's signed 64-bit `int` — a bare `-get_instance_id()` could therefore double-negate back to *positive* for some instances, defeating the "never collides with a real (positive) actor id" guarantee. Fixed in `a6ad593` with `-absi(get_instance_id())`, which forces the sign unconditionally regardless of which way Godot's own convention happens to go for a given instance — a real Godot-specific behavior no local editor could have surfaced before pushing.

**Confirmed green** ([run 29679067167](https://github.com/DanielDmas/Afterimage/actions/runs/29679067167), commit `a6ad593`): both jobs `success` — the lint job's "percept/truth architecture boundary" step passed specifically — and the test log reads `ALL PASSED (323/323)`, including `test_phantom_entity.gd`'s previously-failing case and both instance-disambiguation tests. `gdlint`/`gdformat --check`/`percept_truth_boundary_lint.py` clean on GitHub's infrastructure. **Pass 9 is closed.**

---

## Pass 10 — Ground verb, Clarity Mode stub

**Delivered:** the Ground verb (master_plan.md §4.6) resolves real `DistortionOp`s for the first time, wired through the exact seam Pass 9's `resolve_grounded()` methods were built anticipating.

- **`src/sim/ground_state.gd`** (`GroundState`) — a hold-duration state machine: `DURATION_TICKS = 75` (2.5s baseline at 30Hz). Deliberately takes just a plain `requested: bool` per tick, agnostic to whether the caller derived it from a continuous hold or a hold-to-toggle accessibility scheme (§4.16) — both produce an identical tick stream from this class's point of view, so "input-hold + toggle variants" (the roadmap AC) is satisfied by the input *contract*, not two separate implementations. Releasing before completion resets progress to zero (§4.6 describes no partial-hold consequence).
- **`TruthSim` wiring** — `step()` now blocks player movement/combat for any tick Ground is requested ("stationary and defenseless," §4.6), and on the exact completing tick calls a new `_resolve_ground_completion()`: checks whether any AI could currently see the player (reusing `VisionCone`/`LineOfSight` directly rather than `AiAgent.perceive_and_decide()`, so the check never mutates an agent's own last-known-position memory as a side effect of a Ground check) and publishes `GroundObserved` (`suspicion_weight: 2`, §4.7's own example) — the "suspicion tick stub" the AC asks for; no real `SuspicionGraph` exists yet (Pass 16+) to consume it. `GroundCompleted` always publishes on completion regardless of observation, which is what §4.6's "grounding when nothing is distorted still *tells you something true*" actually requires. `capture_percept_snapshot()` gained `"ground_just_completed"`/`"ground_is_holding"` for percept to read.
- **`PerceptRenderer.render()`** now reads `ground_just_completed` off the truth snapshot and calls `resolve_grounded()` instead of `apply()` on every active `DistortionOp` for that one tick — §4.6's "resolves all active ops in scope simultaneously," implemented for real, not just anticipated. A plain `PerceptOp` with no override still falls through to `apply()`.
- **`src/percept/clarity_mode.gd`** (`ClarityMode.active_flags()`) — the data-layer stub for real-time distortion flagging (§4.16, Charter rule 6): reports every active `DistortionOp`'s class/tier, verified against all four Pass 9 ops active simultaneously. No vignette rendering exists (no Godot scene has been built in any pass so far) and the on/off toggle is a settings-system concern (Pass 19) — pure data now, presentation later, the same pattern Focus/Ground's own resource-gates used before either had a renderer to attach to.
- **17 new tests** across 3 new files (`test_ground_state` 5, `test_truth_sim_ground` 6, `test_clarity_mode` 3) plus 3 more added to `test_percept_renderer.gd`, bringing the suite to 340 tests. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py` locally before commit.

**Scoping decision — fatigue cost and real social-NPC suspicion are still deferred, documented, not silently skipped:** §4.6's fatigue cost (+1.5/use, +2.5 at fatigue Murmur+) isn't billed anywhere yet — MindModel (Pass 11) doesn't exist to receive it; `GroundState.use_count` is tracked now for that billing later, the same "state now, consumer later" pattern Focus's `activation_count` already established. "Observed by an Argus NPC in a social scene" is checked against whatever AI actors currently exist (combat archetypes, not social NPCs) — a real day-phase social-stealth system is Pass 16+ territory; the observation *mechanism* (vision cone + LOS) is identical either way, so this is real wiring against a placeholder cast, not a fabricated check.

**Files added:** `src/sim/ground_state.gd`, `src/percept/clarity_mode.gd`, `tests/unit/{test_ground_state,test_truth_sim_ground,test_clarity_mode}.gd`.

**Files changed:** `src/sim/truth_sim.gd` (Ground wiring, `GROUND_OBSERVED_SUSPICION_WEIGHT`, snapshot fields), `src/percept/percept_renderer.gd` (`resolve_grounded()` dispatch), `tests/unit/test_percept_renderer.gd` (+3 tests), `docs/roadmap.md` (M2 Ground/Clarity Mode items checked off with scope notes).

**Confirmed green (commit `737827f`):** CI run [29679484445](https://github.com/DanielDmas/Afterimage/actions/runs/29679484445) — both jobs `success`. Test job log shows the literal `Afterimage test run — 45 suite(s), 340 test(s), 7517 assertion(s)` and `ALL PASSED (340/340)`; confirmed all six `test_truth_sim_ground.gd` cases (movement/fire blocked while grounding, `GroundCompleted`-unseen, `GroundObserved`-seen with correct suspicion weight, use-count-increments-regardless-of-observation, early-release-resets, snapshot flags) and all five new `test_percept_renderer.gd` grounded-dispatch cases ran and passed by name in the raw log text, not just the aggregate count. The lone `ERROR: ... failed to load (parse or type error)` line in the log is the intentional Pass 6 `test_broken_type_reference.gd` fixture, not a regression. Lint job (`gdlint`, `gdformat --check`, `percept_truth_boundary_lint.py`) also green.

---

## Pass 11 — MindModel (four variables, §4.4 constants, worked-example fixtures)

**Delivered:** the Mind Model (master_plan.md §4.4) — acute stress, fatigue, moral injury, identity strain — as real, tested Q16.16 arithmetic, every named gain/decay event from the spec its own method.

- **Four small state classes, not one mega-class:** `src/sim/acute_stress_state.gd` (`AcuteStressState`), `fatigue_state.gd` (`FatigueState`), `moral_injury_state.gd` (`MoralInjuryState`), `identity_strain_state.gd` (`IdentityStrainState`). Each variable's own §4.4 gain/decay methods alone come close to gdlint's 20-public-method cap (confirmed the hard way: a first-draft single `MindModel` class with all four variables hit ~43 public methods and failed `gdlint` immediately), so each variable gets its own file — the same "small composed state machine" shape as `GroundState`/`FocusState`, just four of them instead of one. Every value is stored as a Q16.16 fixed-point int (`FixedMath`) and clamped to `[0, from_int(100)]`; there is no float storage anywhere, so nothing can drift under repeated deterministic replay.
- **`src/sim/mind_model.gd`** (`MindModel`) — composes the four states as public members (`mind.acute_stress`, `mind.fatigue`, `mind.moral_injury`, `mind.identity_strain`) and owns the two pieces of logic that genuinely span variables: `Band` (Quiet/Murmur/Loud/Crisis) and the static `band_for()` that computes it, defined once and shared by all four rather than duplicated per state class (Pass 6's static-typing gotcha — a bare vs. fully-qualified enum name not unifying across files — makes minimizing how many places declare/return a cross-file enum type worth doing deliberately, not just for DRY); and `apply_hub_rest()`, §4.4.1's "rest returns acute stress to a floor of `max(fatigue, moralInjury) × 0.3`" rule, the one rule that actually needs two other variables' state to compute.
- **Per-second decay converted to per-tick without accumulating rounding error:** §4.4.1's acute-stress decay rates (-0.4/s safe zone, -0.1/s mission-calm) are converted via a single multiply-then-divide — `(rate_fx * ticks) / TICK_RATE`, one truncating division per call — rather than dividing once per tick and accumulating up to `ticks` separate roundings. `advance_tick()` (1 tick) and `advance_ticks(n, zone)` (n ticks in one call) are proven to agree exactly over a 30-tick (1s) span in `test_per_tick_and_batch_decay_agree_over_one_second`. `MISSION_ALERTED` applies no decay at all — §4.4.1 only names safe-zone and no-hostiles-alerted rates.
- **Every worked-example fixture hand-verified against a Python Q16.16 reference before porting** (per this project's "verify externally, then port" discipline), including one real rounding surprise the reference caught: the hub-rest floor's `× 0.3` factor has no exact Q16.16 representation (`0.3 * 65536 = 19660.8`, rounds to `19661`), so `max(40, 60) * 0.3` lands on fx `1179660`, not the naively-expected `FixedMath.from_int(18)` (`1179648`). `test_hub_rest_raises_acute_stress_to_the_fatigue_or_moral_injury_floor` asserts the exact `1179660`, with the discrepancy documented inline — exactly the kind of bug this pass's whole verification discipline exists to catch before CI, not after.
- **25 new tests** across 5 new files (`test_acute_stress_state` 9, `test_fatigue_state` 4, `test_moral_injury_state` 5, `test_identity_strain_state` 3, `test_mind_model` 4), bringing the suite to 365 tests. Every assertion compares exact Q16.16 integers, never floats — no tolerance to hide a wrong formula behind. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py` locally before commit (also confirmed the full repo — 100 `.gd` files including the 10 new ones — stays clean, since `git ls-files` alone silently excludes untracked new files from a lint sweep).

**Scoping decision — band effects and every gain/decay call site are still deferred, documented, not silently skipped:** this pass delivers the AC's "arithmetic unit tests; worked-example fixtures reproduce exactly" in full, but nothing in `TruthSim`, combat, Ground, Focus, or a hub/calendar calls into `MindModel` yet — every dev_log entry that flagged "MindModel doesn't exist yet" (Focus's `+6` acute stress, Ground's `+1.5`/`+2.5` fatigue) still isn't billed, because the actual call sites belong to whichever system is doing the billing, not to MindModel itself; wiring them in now would mean guessing at combat/hub integration points before Pass 12 (DistortionDirector, the first real consumer of band state) and Pass 18 (the hub calendar that actually drives per-day/per-hour events) exist to receive them. Band **effects** (tier unlocks, tightened noise-ring rendering, screen-edge VFX, dialogue UI recoloring, input-latency rendering) are pure future-consumer territory for the same reason. Substance/tool modifiers (§4.4.5) are out of scope entirely — they need a multi-day "effect window" (the stimulant's 3-day fatigue-floor-and-stress-multiplier aftermath) only a real hub calendar can drive, and they're their own separate roadmap.md M3 line item, not folded into "four variables + worked-example fixtures."

**Files added:** `src/sim/{acute_stress_state,fatigue_state,moral_injury_state,identity_strain_state,mind_model}.gd`, `tests/unit/{test_acute_stress_state,test_fatigue_state,test_moral_injury_state,test_identity_strain_state,test_mind_model}.gd`.

**Files changed:** `docs/roadmap.md` (M3 MindModel item checked off with scope notes).

**Confirmed green (commits `73acda4` → `94de38a`):** the initial push (`73acda4`) failed CI's test job for real — [run 29679936081](https://github.com/DanielDmas/Afterimage/actions/runs/29679936081), `test_acute_stress_state.gd :: test_per_tick_and_batch_decay_agree_over_one_second` failed with `expected <498074>, got <498098>`. The Python-reference verification this pass leaned on had checked that each *individual* formula matched its worked example, but never checked the premise of that one test — that 30 separate per-tick truncating divisions would land on the same total as a single batched division — against actual truncation behavior. They don't: recomputing in Python confirmed the real Godot-reported numbers exactly (per-tick sum -26190 fx vs batched -26214 fx, a 24 fx gap from 30 independent truncations each losing ~0.2 fx), which is precisely why `advance_ticks()`'s batch form was built in the first place. Fixed by replacing the false "they agree" assertion with `test_per_tick_decay_accumulates_more_rounding_error_than_batched`, pinning both real values and asserting the batch form is strictly more precise (`94de38a`). [Run 29680067552](https://github.com/DanielDmas/Afterimage/actions/runs/29680067552): both jobs `success`, test log shows the literal `ALL PASSED (365/365)` with zero `[FAIL]` lines, and the corrected test passes by name. Lint job (`gdlint`, `gdformat --check`, `percept_truth_boundary_lint.py`) green on both runs.

---

## Pass 12 — DistortionDirector (budget/deck/caps/cooling/seeding); Fairness auditor v1

**Delivered:** the two systems that turn Pass 9's DistortionOp classes and Pass 11's MindModel into an actual authored, budgeted, fair distortion pipeline — still with no live game loop calling either yet, but both fully real and tested against master_plan.md §4.3/§4.5/§10.

- **`src/percept/deck_entry.gd`** (`DeckEntry`) — a purchasable deck entry as plain data (`op_class`, `tier`, `cost`, `variable_affinity`): content is data not code, and this is the shape Pass 13's real JSON loader will eventually populate.
- **`src/percept/distortion_director.gd`** (`DistortionDirector`) — deliberately takes only plain data (`Dictionary[String, int]` fx values, Strings, ints) everywhere, never a `MindModel` or any `src/sim/` class reference. That decoupling means it lives safely under `src/percept/` with zero percept/truth boundary risk (confirmed clean against `tools/percept_truth_boundary_lint.py`), even though its whole job is truth-adjacent, deterministic decision-making — a future caller reads `mind.acute_stress.value_fx()` etc. into a Dictionary before calling in, and turns a purchase record's `op_class` String into a real DistortionOp afterwards.
  - `compute_budget()` implements §4.3's `B = base(sceneType) × (0.4 + Σ wᵥ · v/100)` exactly. Worked example (combat scene, base 25; acute_stress=40/fatigue=20/moral_injury=10/identity_strain=5 with mission weights 1.0/0.5/1.5/0.2): Σ ≈ 0.66, multiplier ≈ 1.06, B ≈ 26.5, floors to 26 — hand-verified against a Python Q16.16 reference before porting.
  - `purchase_one()` is a weighted pick from a `DeckEntry` deck via a seeded `Xoshiro128StarStar` stream, seeded by `seed_for(run_seed, mission_id, scene_id)` (a simple deterministic bit-mixing combination — tech_guidelines D5's one-named-stream-per-system). Respects the global density cap (`MAX_CONCURRENT_OPS = 3`), tier-3+ 20s/600-tick purchase spacing, and per-op-class Ground-resolution weight decay (`notify_ground_resolved()` halves an op class's selection weight in-scene, compounding on repeats — §4.3's "the mind that gets caught repeating a trick changes tricks"). Charter legality itself ("an illegal purchase is a build failure, not a runtime clamp") is FairnessAuditor's job on the deck *before* it ever reaches the Director — not re-checked per purchase.
  - `apply_death_cooling()` implements Charter rule 7's ×0.6 first-death / ×0.3 second-and-later cooling.
  - **AC ("same seed+state → identical purchase log, in corpus") proven directly**, not via new corpus fixture files: two independently constructed Directors, same seed, same deck and mind-state trace, produce byte-identical `purchase_log()` output (`test_purchase_one_reproducible_given_same_seed`). The Pass 2 determinism-corpus mechanism is specifically TruthSim-replay verification; this is the equivalent guarantee for the Director's own decision stream, which doesn't touch TruthSim at all.
  - The weighted-pick tests reuse `test_prng.gd`'s already-CI-pinned seed=0/seed=1 `next_u32()` vectors (deriving `next_fixed()` from them by hand: `u32 >> 16`) rather than re-deriving PRNG output from scratch — the same real-reference discipline, against an existing already-verified fixture instead of a fresh script.
- **`src/percept/fairness_auditor.gd`** (`FairnessAuditor`) — statically validates a deck (any Array of duck-typed op-like objects: real `DistortionOp` instances or correctly-shaped test doubles) against all 8 Fairness Charter rules, each with its own dedicated failing fixture per the roadmap AC. Several rules (1 phantoms-never-damage, 3 inputs-never-distorted, 6 Clarity-Mode-flaggable) are also structurally guaranteed elsewhere (the percept/truth boundary, the taxonomy's own design, `ClarityMode`'s `is DistortionOp` check) — this auditor adds an *independent* static check on top of each, the same belt-and-suspenders pattern Pass 8 established for the percept/truth boundary itself. Rules 2 (`EntityMask`) and 4 (`GeometrySwap`) are checked by op-class-string matching even though neither class is implemented yet (Pass 9 built 4 of the 10 taxonomy classes) — duck-typed validation means the auditor never needed the real subclass to exist, only a correctly-shaped deck entry, so it's ready the moment those classes land. Rule 7 (density/cooling caps) is the one deck-*level*, not per-entry, check: a mission's declared `encounter_cap` must not exceed `DistortionDirector.MAX_CONCURRENT_OPS` (inlined rather than cross-referenced, matching MindModel's own per-state-class constant duplication). Rule 8 (Theater never lies/forges) is interpreted as: every entry's `dramatic_intent` must be one of the four §4.2-named values (dread/doubt/grief/paranoia) — the Theater/debrief (Pass 14+) can't honestly disclose an op's intent if the op never declared a real one.
- **26 new tests** across 3 files (`test_deck_entry` 2, `test_distortion_director` 13, `test_fairness_auditor` 11), bringing the suite to 391 tests. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py` locally before commit (106 `.gd` files including the 5 new ones).
- **One arithmetic assumption caught before commit, not after** — this pass's own answer to Pass 11's CI-caught false premise: a naive `floor(10 × 0.4) = 4` expectation for `compute_budget()`'s hub-scene edge case is actually `3`, since 0.4 has no exact Q16.16 representation (`0.4 × 65536 = 26214.4`, rounds to `26214`, and `10 × 26214` floors to `3.9999...`, not `4.0`). Caught by re-running the Python reference against the *exact* formula before pinning the test value, not by inspection.

**Scoping decision — nothing here is wired into a live scene loop yet, and `.filter()`/`.any()` Array methods were deliberately avoided:** `DistortionDirector` and `FairnessAuditor` are both real, fully tested, and ready to be called — but nothing in `TruthSim` or a hub calendar calls `grant_budget()`/`purchase_one()` per scene tick yet (that needs Pass 18's hub calendar and a real per-scene cadence this pass doesn't invent), and nothing turns a purchase record's `op_class` String into a live `DistortionOp` instance yet (that factory is a natural Pass 13 content-pipeline job, once mission decks are real JSON rather than GDScript-constructed `DeckEntry` arrays). Separately: `test_fairness_auditor.gd`'s fixtures use hand-written `_tags_excluding()`/`_any_violation_has_prefix()` loop helpers instead of GDScript's `Array.filter()`/`Array.any()` — those methods have never been exercised anywhere in this codebase's CI history, and with no local Godot binary in this sandbox to check Godot 4.3's exact behavior, a plain `for` loop (already proven correct across 11 prior passes) was the lower-risk choice.

**Files added:** `src/percept/{deck_entry,distortion_director,fairness_auditor}.gd`, `tests/unit/{test_deck_entry,test_distortion_director,test_fairness_auditor}.gd`.

**Files changed:** `docs/roadmap.md` (M3 DistortionDirector + Fairness auditor v1 items checked off with scope notes).

**Confirmed green (commit `05531b3`):** CI run [29680669581](https://github.com/DanielDmas/Afterimage/actions/runs/29680669581) — both jobs `success`. Test log shows the literal `ALL PASSED (391/391)` with zero `[FAIL]` lines; confirmed all 15 `test_distortion_director.gd` cases (including the seed=0/seed=1 PRNG-vector-derived weighted-pick and reproducibility tests, and the Ground-resolution decay sequence) and all 11 `test_fairness_auditor.gd` cases (all 8 Charter rule fixtures plus unknown-op-class and multi-violation aggregation) ran and passed by name in the raw log text. Lint job (`gdlint`, `gdformat --check`, `percept_truth_boundary_lint.py`) also green. The hand-computed PRNG-vector arithmetic — never previously run through an actual Godot interpreter before this push — held up exactly as verified.

---

## Pass 13 — Content pipeline: JSON schemas, content validator CLI, mission package loader

**Delivered:** the first real "content is data, not code" pipeline (tech_guidelines.md D7/§5.1) — a JSON Schema, a CI-wired validator, a GDScript loader, and one real mission package fixture that already drives Pass 12's `DistortionDirector` end to end.

- **`tools/schemas/mission.schema.json`** — a versioned JSON Schema (draft-07) for a mission package's v1 scope: `id` (namespaced per tech_guidelines §5.2), `scene_type`, `mission_weights`, `encounter_cap`, and `deck` (an array of `{op_class, tier, cost, variable_affinity}`, `op_class` restricted to the 10 taxonomy classes `FairnessAuditor.KNOWN_OP_CLASSES` already lists). Dialogue/NPC-mind/debrief/ending-table schemas are later, separate versioned files under the same directory (D7), not folded into this one.
- **`tools/content_validator.py`** — a hand-rolled, dependency-free JSON-Schema-subset validator (matching `tools/percept_truth_boundary_lint.py`'s own stdlib-only precedent rather than adding a `jsonschema` pip dependency): implements exactly the keywords `mission.schema.json` uses (`type`, `required`, `properties`, `additionalProperties`, `enum`, `minimum`, `maximum`, `pattern`, `items`), scans `content/missions/*/mission.json`, and exits non-zero with a precise per-field violation list on any failure. Verified locally (not committed as a fixture) against a deliberately-broken mission dict — bad ID pattern, `encounter_cap` exceeding the global max, an unknown `op_class`, a missing required field — and all four violations were caught exactly, before this was ever wired into CI.
- **`content/missions/m00_stub/mission.json`** — the first real, schema-valid mission package: a combat-scene deck of 4 entries (including one tier-3 `PhantomEntity` with two affinity variables), reusing the exact mission-weight values (1.0/0.5/1.5/0.2) Pass 12's own worked-example test already hand-verified, so loading it and re-running that same budget computation is a genuine identity check, not a new number to trust blind.
- **`src/percept/mission_package.gd`** (`MissionPackage`) and **`src/percept/mission_loader.gd`** (`MissionLoader`) — the in-memory loaded form and its static loader. Both live under `src/percept/`, confirmed clean against `tools/percept_truth_boundary_lint.py`, since neither references any `src/sim/` class (mission weights arrive as plain floats converted to Q16.16 via `FixedMath.from_float()`, exactly the function `DistortionDirector.compute_budget()` already expects). `MissionLoader` asserts on malformed input rather than handling it gracefully — content reaching this loader is assumed to have already passed `content_validator.py` in CI, the same "contract violation, not a runtime case to design around" stance `FixedMath.div()` already takes on division by zero.
- **A real, CI-only gotcha caught by reasoning rather than a failed run** (no local Godot binary to check against): Godot's `JSON.parse_string()` parses every JSON number as `float`, since JSON itself has no separate int type — a naive `var encounter_cap: int = data["encounter_cap"]` would assign a float to an int-typed variable. Fixed with explicit `int()` casts on every schema-integer field (`encounter_cap`, `tier`, `cost`) before it reaches a typed variable or a `DeckEntry` constructor, documented inline so a future edit doesn't "simplify" them away.
- **7 new tests** across 2 files (`test_mission_package` 1, `test_mission_loader` 6 — including one that loads the real `m00_stub` fixture and feeds its deck straight into `DistortionDirector.compute_budget()`/`purchase_one()`, reproducing Pass 12's exact budget-26 worked example and a same-seed reproducibility check, proving the pipeline's output is actually usable, not just parseable), bringing the suite to 398 tests. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py` locally before commit (110 `.gd` files including the 4 new ones).

**Scoping decision — FairnessAuditor is not yet wired into the content pipeline, and the validator's own regression fixture is deferred:** `DeckEntry` (Pass 12) carries `{op_class, tier, cost, variable_affinity}` — no `dramatic_intent` or `fairness_tags`, the fields `FairnessAuditor.validate()` needs. Running the auditor against real mission JSON therefore isn't possible yet without either extending the deck schema (a versioned migration, per D7 — not done silently here) or building a separate op-instantiation step first; both are natural jobs for whichever pass first needs real Charter-checked authored content, not this one. Relatedly, `FairnessAuditor`'s rule 6 check (`entry is DistortionOp`) was written for validating *live op instances*, and would incorrectly fail every plain `DeckEntry` fed through it directly — a real design note for whenever that wiring happens, not a bug in either class as currently scoped. Separately, `content_validator.py` has no committed broken-fixture regression test of its own (unlike `tools/percept_truth_boundary_lint.py`, which also has none) — that's explicitly roadmap.md M4's "Content validator v1" AC ("seeded broken content fails CI"), once ID resolution and reachability checks give the validator enough surface area to make a dedicated regression fixture worth maintaining.

**Files added:** `tools/schemas/mission.schema.json`, `tools/content_validator.py`, `content/missions/m00_stub/mission.json`, `src/percept/{mission_package,mission_loader}.gd`, `tests/unit/{test_mission_package,test_mission_loader}.gd`.

**Files changed:** `.github/workflows/ci.yml` (new lint-job step: `python3 tools/content_validator.py`), `docs/roadmap.md` (M4 Content validator v1 item annotated with Pass 13's foundational work, not checked off).

**Confirmed green (commit `553804b`):** CI run [29680951250](https://github.com/DanielDmas/Afterimage/actions/runs/29680951250) — both jobs `success`, including the new "content package schema validation" lint step. Test log shows the literal `ALL PASSED (398/398)` with zero `[FAIL]` lines; confirmed all 6 `test_mission_loader.gd` cases and `test_mission_package.gd`'s case ran and passed by name in the raw log text — including real `res://` file loading via `FileAccess`/`JSON.parse_string()` and the `int()`-cast fix around Godot's float-only JSON number parsing, both unverifiable locally without a Godot binary and confirmed correct on the first push.

---

## Pass 14 — Replay Theater v0 (data model: dual-pane reconstruction, checkpoint snapshots)

**Delivered:** the reconstruction mechanism behind master_plan.md §4.12's Afterimage Theater — no UI (Pass 19's job), but a real, tested data model that can answer "what did truth look like at tick T" and "what did the player perceive at tick T" for any T in a recorded run.

- **`src/percept/replay_theater.gd`** (`ReplayTheater`) — takes a `scenario_factory: Callable` (never a typed sim-layer reference) plus a `ReplayLog` (which lives under `src/core/`, outside the percept/truth boundary lint's denylist, so it's referenced directly). On construction, does one forward pass: calls the factory once, steps through every frame, and caches `capture_percept_snapshot()` at every tick. `truth_view_at(tick)` is then an O(1) array lookup — the actual mechanism behind the AC's "scrub-to-any-point ≤ 100 ms," even though the literal millisecond benchmark isn't measurable in this sandbox (no Deck hardware, no profiler — deferred to a real profiling pass, same class of deferral every prior hardware-dependent AC has gotten). This is a full-tick cache, not a sparse checkpoint-plus-partial-resume scheme — documented explicitly as a real memory-for-speed trade, not something quietly assumed free: `TruthSim` has no `capture_full_state()`/restore mechanism to resume mid-simulation from, only the percept-safe, read-only `capture_percept_snapshot()`, so there's nothing to resume *from* at a sparse checkpoint yet.
- **`src/percept/op_timeline_span.gd`** (`OpTimelineSpan`) — the "every DistortionOp as an annotated span (class, cause variable, resolution: grounded/believed/acted-upon)" data shape §4.12 asks for. Plain data (`op_class`, `tier`, `cause_variable`, `tick_start`, `tick_end`, `resolution` enum defaulting to `UNDISCLOSED`), constructed directly for now — deriving spans automatically from a live `DistortionDirector`'s purchase log plus real op-lifecycle events is a later pass's wiring job, the same "op_class String → live instance" gap Pass 13 already flagged and left open.
- **`percept_view_at()`/`dual_view_at()`** delegate straight to Pass 8's already-proven `PerceptRenderer.render()`, taking real op instances as a parameter — this class reconstructs *which* ops are active at a tick (`spans_active_at()`) and hands rendering off, rather than reimplementing dispatch logic that already exists and is already tested.
- **11 new tests** across 2 files (`test_replay_theater` 7, `test_op_timeline_span` 4), bringing the suite to 409 tests. The load-bearing one: `test_truth_view_at_matches_independent_resimulation` — the theater's cached tick-5 snapshot is asserted equal to an *independently* re-simulated fresh 5-tick run's `capture_percept_snapshot()`, proving the cache holds the actual re-simulated state, not just some plausible-looking Dictionary. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py` locally before commit (114 `.gd` files including the 4 new ones).

**Scoping decision — no scrubber UI, no automatic span derivation, no sparse checkpointing, and the hardware AC is unmeasurable here:** this pass is explicitly "v0 (data model)" per its own line in the 20-pass plan — a scrubber widget reading from `ReplayTheater` is Pass 19's UI-integration job; turning a live mission run into real `OpTimelineSpan`s (reading `DistortionDirector.purchase_log()` plus whatever eventually marks an op's deactivation/Ground-resolution tick) needs the wiring Pass 13 already deferred for turning deck data into live op instances, so span construction stays manual/test-only for now; a sparse checkpoint-plus-partial-resimulation scheme (caching only every Nth tick and replaying forward from the nearest one) needs `TruthSim` to gain genuine state capture/restore across `ActorRegistry`, `AiAgent`, `Weapon`, `FocusState`, and `GroundState` — a substantial change to five Pass 3–10 classes that this pass deliberately didn't take on, in favor of the simpler, fully-correct full-tick-cache design; and the AC's literal "≤ 100 ms on Deck-class hardware" can't be benchmarked in a sandbox with no Godot binary and no Deck, so what's verified here is the *mechanism* (O(1) lookup) that would make that benchmark trivially true once real profiling exists to confirm it.

**Files added:** `src/percept/{replay_theater,op_timeline_span}.gd`, `tests/unit/{test_replay_theater,test_op_timeline_span}.gd`.

**Files changed:** `docs/roadmap.md` (M2 Replay Theater v0 item checked off with scope notes).

**Confirmed green (commit `b10bd3b`):** CI run [29681256238](https://github.com/DanielDmas/Afterimage/actions/runs/29681256238) — both jobs `success`. Test log shows the literal `ALL PASSED (409/409)` with zero `[FAIL]` lines; confirmed all 7 `test_replay_theater.gd` cases (including `test_truth_view_at_matches_independent_resimulation`, the real correctness proof) and all 4 `test_op_timeline_span.gd` cases ran and passed by name in the raw log text. The typed inline lambda scenario factory (`func() -> TruthSim: return TruthSim.new(...)` as a `Callable`) and the percept/truth boundary lint both held up exactly as designed. Lint job (`gdlint`, `gdformat --check`, `percept_truth_boundary_lint.py`, `content_validator.py`) also green.

---

## Pass 15 — Dialogue DSL parser/compiler + runtime graph + DialogueRunner + interrupt memory

**Delivered:** the full text-to-runtime dialogue pipeline foundation_blueprints.md §4 specs — a real writer-facing DSL, a compiler, a graph runtime, and interrupt memory's symmetric contradiction detection — proven against one real, small, authored scene.

- **`tools/dlgc.py`** — a hand-rolled line-based parser/compiler (no parser-generator dependency, matching `percept_truth_boundary_lint.py`/`content_validator.py`'s stdlib-only precedent): node headers (`== id ==`), speaker/stance lines, `drift:` annotations (SubtitleDrift pairs, tech §5.4), bare and structured claim grants (`+claim.id` / `+claim.id(subject, predicate, object)`), guarded choices, and `goto`/`END`. Also implements §2's compact predicate text form (`trustAtLeast(npc.doubek, 40)`, `all(...)`/`any(...)`/`not(...)` combinators with proper nested-paren-aware argument splitting), compiling directly to the exact `{"op":...,"args":{...}}` JSON shape `src/core/predicate.gd`'s `PredicateEvaluator` (Pass 1) already expects — the operator/argument-name table is a hand-kept mirror of `predicate.gd`'s own `OPERATOR_SPECS`, documented as needing to stay in sync (the same cross-language spec-duplication `prng.gd`'s Python reference already lives with). `python3 tools/dlgc.py` (no args) compiles every `content/dialogue/*.dlg` file and is now a CI lint step; verified locally against a deliberately-broken fixture (an unknown predicate operator, added and removed without ever being committed) before being wired in.
- **`content/dialogue/prologue_sova.dlg`** — a real, small Cold-Open scene (master_plan §2.6: "Dr. Sova teaches Ground diegetically... one scripted misheard line, immediately disclosed") with one branching choice (pipes vs. footsteps), a `drift:` annotation on the footsteps line, and both bare and structured claim grants. The compiled JSON itself is never committed (tech_guidelines §5.1: "never hand-edited, never committed") — confirmed correct by running the compiler locally and inspecting its real output before transcribing it verbatim into a GDScript test fixture.
- **`src/dialogue/{dialogue_graph,dialogue_runner,interrupt_memory}.gd`** — a new top-level `src/dialogue/` directory, sibling to `core`/`sim`/`percept`, entirely unrestricted by `tools/percept_truth_boundary_lint.py` (which only scans `src/percept/`). `DialogueGraph` loads a compiled-JSON-shaped Dictionary (real or hand-built, mirroring Pass 8/9's "synthetic snapshot" testing discipline). `DialogueRunner` walks it node by node, filters choices by guard using `PredicateEvaluator` against a caller-supplied `WorldQuery` — reusing `tests/framework/mock_world_query.gd`, an existing fixture whose own doc comment literally anticipated "dialogue... tests" reusing it — and feeds every *structured* claim grant into `InterruptMemory` as it's reached, attributed to the last line's speaker (a documented v0 heuristic; the DSL has no separate per-grant speaker syntax yet).
- **`InterruptMemory`** — a local, minimal `{subject, predicate, object, source}` tuple (deliberately not the full Claims/Provenance/DebriefLedger system, Pass 17's job): a contradiction is any pair of statements sharing (subject, predicate) but disagreeing on object, checked against *every* prior statement regardless of its source — which is exactly what makes "fires both directions" true by construction: an NPC statement contradicted later by the player, and a player statement contradicted later by an NPC, are caught by the identical code path, not two separate ones.
- **18 new tests** across 3 files (`test_dialogue_graph` 3, `test_interrupt_memory` 7, `test_dialogue_runner` 8), bringing the suite to 427 tests. `test_dialogue_runner.gd` plays the real compiled prologue scene through both branches end to end, satisfying the AC's "compiled prologue Sova scene plays"; `test_interrupt_memory.gd` proves both contradiction directions directly. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py`/`tools/dlgc.py` locally before commit (120 `.gd` files including the 5 new ones).

**Scoping decision — no UI, no per-line guards, no real Claims system, and one pre-existing Predicate naming mismatch left untouched:** stance tags (`[procedural]`/`[warm]`/`[pressing]`) and drift annotations are parsed and carried through as data with no consumer yet — register-mark-driven pre-selection/input-hold (master_plan §4.4.4, §4.8) and SubtitleDrift's real dialogue producer both need a UI (Pass 19) or a hub/day-phase loop (Pass 18) that doesn't exist. Guards apply only to choices, not individual lines — the prologue content never needed per-line guards, and adding the parsing complexity without a concrete AC-driving need would be the premature-scope pattern this log has avoided since Pass 4. `InterruptMemory` is explicitly not Pass 17's Claims/Provenance/DebriefLedger — no claim IDs feed into it structurally, no campaign persistence, no qualifiers. Separately noticed but deliberately not touched: `src/core/predicate.gd`'s `mindBand` operator validates against lowercase variable names (`"stress"`, not `"acute_stress"`) predating `MindModel` (Pass 11) by ten passes — a pre-existing mismatch, not this pass's to silently fix, since nothing in the prologue scene exercises `mindBand` and fixing it would mean touching already-CI-confirmed Pass 1 code outside this pass's actual scope.

**Files added:** `tools/dlgc.py`, `content/dialogue/prologue_sova.dlg`, `src/dialogue/{dialogue_graph,dialogue_runner,interrupt_memory}.gd`, `tests/unit/{test_dialogue_graph,test_interrupt_memory,test_dialogue_runner}.gd`.

**Files changed:** `.github/workflows/ci.yml` (new lint-job step: `python3 tools/dlgc.py`), `docs/roadmap.md` (M4 Dialogue DSL item checked off with scope notes).

**Confirmed green (commit `453278e`):** CI run [29681644984](https://github.com/DanielDmas/Afterimage/actions/runs/29681644984) — both jobs `success`, including the new "dialogue DSL compile check" lint step. Test log shows the literal `ALL PASSED (427/427)` with zero `[FAIL]` lines; confirmed all 3 `test_dialogue_graph.gd`, all 8 `test_dialogue_runner.gd` (including both real prologue-scene branches), and all 7 `test_interrupt_memory.gd` cases (including both contradiction directions) ran and passed by name in the raw log text. The entire DSL/compiler/runtime/interrupt-memory pipeline — verified only via Python and hand-tracing before this push, never through an actual Godot interpreter — held up exactly as designed on the first try.

---

## Pass 16 — NPC mind schema + SuspicionGraph + GossipSim

**Delivered:** the Argus social graph's data spine (foundation_blueprints.md §5, master_plan.md §4.7) — NPC minds, a per-NPC suspicion ledger with real decay, and one-hop gossip propagation with per-edge delay and distortion, in a new top-level `src/social/` directory (sibling to `core`/`sim`/`percept`/`dialogue`).

- **`src/social/npc.gd`** (`NPC`) — §5's schema as plain data: `personality` (pride/fear/greed), `loyalty_targets`, `knows` (claim ID strings, may include false beliefs genuinely held — whether they're objectively true is Pass 17's Claims/Provenance question, not this class's), `hides`/`lies` (each carrying a Predicate tree as its unlock/tell condition, reusing `src/core/predicate.gd` as-is), `gossip_edges`, `trust`, `relationship_tier`, `state`. `suspicion` is deliberately *not* an NPC field — it lives in `SuspicionGraph` instead, since suspicion is relational (who's noticing what about Radek), the same reasoning that already keeps `GroundState`/`FocusState` off `Actor` rather than folding tick-state into the entity itself.
- **`src/social/suspicion_ledger.gd`** (`SuspicionLedger`) — one per NPC. Implements §4.7's named entry weights exactly (seen-Grounding 2, police-pattern-behavior 4, cover-inconsistent-equipment 5, implausible-survival 6, interrupt-memory-inconsistency 3–6 as a documented range) and its "-1/week if unreinforced" decay as a *pure function* of `(weight, day_added, query_day)`, recomputed on every `total()` call rather than mutated on some periodic tick — there's no stateful decay step to accidentally run twice, skip, or run out of order. An entry whose `day_added` is still in the future (a not-yet-arrived gossip echo) simply doesn't count until the query day reaches it.
- **`src/social/suspicion_graph.gd`** (`SuspicionGraph`) — composes one ledger per NPC and the three named threshold bands (25 wary / 50 active / 75 convinced) from §4.7; `level_for_total()` is a small static so the threshold logic itself is independently testable without needing a whole graph.
- **`src/social/gossip_sim.gd`** (`GossipSim`) — propagates *one* suspicion observation from a source NPC along its own `gossip_edges` (arrival day = source day + edge delay; §4.7's "per-edge delay (1–3 days)"), applying distortion when an edge calls for it: a seeded ±1 weight offset (`Xoshiro128StarStar`, tech D5's one-stream-per-system) and a blurred, generic type string (`"gossip_of:<original>"`) standing in for "details blur." Publishes `"SuspicionEntryAdded"` (foundation_blueprints §1.2's own named example event) per target via the existing `EventBus`. This directly satisfies the roadmap AC — "a witnessed Ground reaches the right NPCs on the right days" — with `"seen_grounding"` as the literal entry type exercised in tests, arrival days asserted exactly, and the distortion delta hand-verified against `test_prng.gd`'s already-CI-pinned seed=1 vector (the same PRNG-vector-reuse discipline Pass 12's `DistortionDirector` tests established) rather than trusted by inspection.
- **18 new tests** across 4 files (`test_npc` 3, `test_suspicion_ledger` 5, `test_suspicion_graph` 5, `test_gossip_sim` 5), bringing the suite to 445 tests. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py`/`tools/dlgc.py` locally before commit (128 `.gd` files including the 8 new ones).
- **One real arithmetic mistake caught before commit, not after** — Pass 11/Pass 12's own discipline paying off again: a first draft of `test_entry_decays_by_one_per_week_elapsed` asserted a weight-6 entry decays to exactly 0 after five full weeks (day 35). Hand-recomputing showed `6 - 5 = 1`, not `0` — the entry doesn't fully decay until six weeks (day 42). The *production* decay formula was correct throughout; only the test's own expectation was wrong, caught by re-deriving the arithmetic rather than trusting the first number written down.

**Scoping decision — one-hop propagation only, no multi-hop BFS cascade, no countermeasures, no personality-driven reactions yet:** foundation_blueprints §5 calls `GossipSim` a "breadth-first propagation... along gossip edges" — read literally, a rumor should cascade through a neighbor's neighbors, each leg adding its own delay. That needs a real day/event-scheduling system to land each hop's arrival on the correct future day and re-trigger further propagation from every newly-informed NPC along the way — infrastructure that doesn't exist before Pass 18's hub calendar. What's built now is the actual per-edge mechanic (delay + distortion, deterministic and tested) that multi-hop cascading will later call repeatedly; wiring true multi-hop BFS is explicitly left for whenever that scheduling exists. Separately out of scope: §4.7's countermeasures (alibi actions, scapegoats, "ugly jobs convincingly") need real missions/hub time to spend, and personality-driven reaction flavor (§5: "drives leverage response, being-caught reactions, gossip distortion flavor") has no consumer yet — `NPC.personality` is stored but nothing reads it. Both are documented deferrals, not silent gaps.

**Files added:** `src/social/{npc,suspicion_ledger,suspicion_graph,gossip_sim}.gd`, `tests/unit/{test_npc,test_suspicion_ledger,test_suspicion_graph,test_gossip_sim}.gd`.

**Files changed:** `docs/roadmap.md` (M4 NPC minds/suspicion ledger/gossip tick item checked off with scope notes).

**Confirmed green (commit `e16a709`):** CI run [29686489579](https://github.com/DanielDmas/Afterimage/actions/runs/29686489579) — both jobs `success`. Test log shows the literal `ALL PASSED (445/445)` with zero `[FAIL]` lines; confirmed all 5 `test_gossip_sim.gd` cases (including the seed=1 PRNG-vector-derived distortion test) and all 5 `test_suspicion_ledger.gd` cases (including the corrected day-35/day-42 decay arithmetic) ran and passed by name in the raw log text. Lint job (`gdlint`, `gdformat --check`, `percept_truth_boundary_lint.py`, `content_validator.py`, `dlgc.py`) also green.

---

## Pass 17 — Claims/Provenance + DebriefLedger; liar-bot smoke test

**Delivered:** the epistemic backbone behind the Debrief system (foundation_blueprints.md §3, master_plan.md §4.10) — a real Claim schema, a provenance chain, an append-only ledger with truth-delta computation and honesty-mode-gated submission, and a liar-bot smoke test — in a new top-level `src/debrief/` directory.

- **`src/debrief/claim.gd`** (`Claim`) — §3's schema exactly (`id, subject, predicate, object, qualifiers, provenance[]`), a pure data record never mutated after construction. Honesty mode, truth-delta, and consequences deliberately live in `DebriefLedger` instead, keyed by claim id — the same "state lives where it's relational, not on the atomic record" reasoning Pass 16 already used to keep `suspicion` off `NPC`. `has_provenance_type()` checks the provenance chain for any of the four types (`PERCEIVED`/`GROUNDED`/`EVIDENCE`/`TOLD`) a claim can carry.
- **`src/debrief/claim_drafter.gd`** (`ClaimDrafter`) — drafts Claims from already-reduced "perceived event" Dictionaries (`{id, subject, predicate, object, qualifiers?}`), deliberately agnostic to where those events came from (a future `ReplayTheater` percept view, Pass 14, or a `TruthSim` percept snapshot, Pass 8 — the caller's job to reduce either into this shape). This directly proves §4.10's "quiet knife": a believed-phantom event and a real event with the same subject/predicate draft byte-identical `Claim`s, same `PERCEIVED` provenance — drafting never looks at truth, only at what was perceived, so nothing about the drafted claim itself flags the phantom one as suspect.
- **`src/debrief/debrief_ledger.gd`** (`DebriefLedger`) — candidates tracked in deterministic insertion order (an explicit `Array[String]` of ids alongside the lookup Dictionary, never relying on Dictionary iteration order, tech_guidelines §3.5). Truth-delta v0 is a direct content match (0 true / 1 false) — §4.10 gives no graduated formula the way MindModel's §4.4 or the Director's §4.3 do, so this is the most honest reading of "engine-computed truth-delta" rather than an invented scale nothing can verify against. `submit_claim()` asserts `VERIFIED_ONLY` requires `GROUNDED`/`EVIDENCE` provenance (§4.10: "a verified claim can never later be contradicted") and asserts against resubmitting an already-submitted claim ("submission irreversible with a dedicated autosave"). `FABRICATE` optionally bills a caller-supplied `MoralInjuryState` (Pass 11, `src/sim/`) directly via `gain_knowing_lie_in_debrief()` — "lies price into moral injury immediately, the liar knows" — the one consequence channel already real and wireable; Doubek trust, resource budget, and plot flags all need a hub/mission system (Pass 18+) that doesn't exist yet.
- **`tests/unit/test_liar_bot.gd`** — foundation_blueprints §7's "liar bot... fabricates every claim to smoke-test the consequence graph": a 5-claim batch (two of which don't match ground truth — a phantom sighting and a misidentified witness) submitted entirely under `FABRICATE`, regardless of what's actually true, confirming the whole batch processes without erroring and moral injury accumulates exactly (5 × the debrief-lie constant). A second test confirms truth-delta is computed from actual content match independent of the chosen mode — fabricating doesn't change what's objectively true, only what's claimed, and both are tracked separately.
- **21 new tests** across 4 files (`test_claim` 5, `test_claim_drafter` 4, `test_debrief_ledger` 10, `test_liar_bot` 2), bringing the suite to 466 tests. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py`/`tools/dlgc.py` locally before commit (135 `.gd` files including the 7 new ones).

**Scoping decision — no real event-log integration, no Doubek/resource/plot consequences, and provenance beyond PERCEIVED is unwired:** `ClaimDrafter` takes already-reduced perceived-event data as input rather than reading a real `TruthSim`/`ReplayTheater` event log directly — that reduction step (deciding which raw percept-layer facts are "salient" enough to draft) is a future pass's job once a real mission/game loop produces one worth reducing. `GROUNDED`/`EVIDENCE`/`TOLD` provenance are modeled and checkable (`has_provenance_type()`) but nothing yet *attaches* them to a drafted claim — Ground verb resolution (Pass 10), photograph/evidence systems (§4.9, unbuilt), and `InterruptMemory`'s `told` statements (Pass 15) all exist as separate mechanisms that would need to feed provenance entries into a claim at drafting or submission time, wiring left for whenever a real mission-end flow calls all of them together. Doubek trust, resource budget for the next cycle, and plot flags (§4.10's other three consequence channels) all need hub/mission infrastructure that doesn't exist before Pass 18.

**Files added:** `src/debrief/{claim,claim_drafter,debrief_ledger}.gd`, `tests/unit/{test_claim,test_claim_drafter,test_debrief_ledger,test_liar_bot}.gd`.

**Files changed:** `docs/roadmap.md` (M4 Debrief v1 item checked off with scope notes).

**Confirmed green (commit `cb042b5`):** CI run [29686754157](https://github.com/DanielDmas/Afterimage/actions/runs/29686754157) — both jobs `success`. Test log shows the literal `ALL PASSED (466/466)` with zero `[FAIL]` lines; confirmed all 5 `test_claim.gd`, 4 `test_claim_drafter.gd`, 10 `test_debrief_ledger.gd` (including the `MoralInjuryState` cross-directory integration tests — `src/debrief/` calling into `src/sim/` directly for the first time), and 2 `test_liar_bot.gd` cases ran and passed by name in the raw log text. Lint job (`gdlint`, `gdformat --check`, `percept_truth_boundary_lint.py`, `content_validator.py`, `dlgc.py`) also green.

---

## Pass 18 — Hub skeleton: calendar/sleep economy, mind dashboard bindings, loadout data model

**Delivered:** the Safehouse Hub's data layer (master_plan.md §4.11) — a day-level calendar driving Pass 11's real fatigue math, a bands-first read-only mind dashboard, and a cover-consistency-checked loadout model — in a new top-level `src/hub/` directory.

- **`src/hub/hub_calendar.gd`** (`HubCalendar`) — `advance_day()` sequences a day's sleep choice (`SKIPPED`/`PARTIAL`/`FULL`) directly into `FatigueState`'s own methods (Pass 11), plus optional `hours_awake_past_18`/`ground_uses` billing for that day — this class never re-declares §4.4.2's constants, it only calls the class that already owns them, the same "sequence calls, don't duplicate the math" discipline `DistortionDirector` (Pass 12) already established for `MindModel`. The AC — "a full sleep-debt week produces the §4.4.2 fatigue trace" — is satisfied literally: seven consecutive skipped-sleep days trace `12, 24, 36, 48, 60, 72, 84` exactly, a whole-integer sequence with zero fixed-point rounding to worry about, plus a mixed week (skipped/partial/full) tracing `12, 24, 36, 21, 33, 45, 5` correctly. `Block` (evening/night/morning, §4.11) is modeled as real data for a future scheduling UI (Pass 19) but isn't driven by anything yet — the other hub-time competitors (org-board work, alibi construction, Sova sessions, Tereza calls, equipment prep) all need their own authored time costs, which don't exist before real mission/hub content.
- **`src/hub/mind_dashboard.gd`** (`MindDashboard`) — a read-only view model over `MindModel`, never mutating it: `snapshot()` returns each variable's band label ("Quiet"/"Murmur"/"Loud"/"Crisis") by default, with exact values included only when `show_numbers` is explicitly requested — "bands not numbers by default... no shame in playing with the hood open" read as opt-in, not default-on.
- **`src/hub/loadout_item.gd`**/**`src/hub/loadout.gd`** (`LoadoutItem`, `Loadout`) — §4.7/§4.11's night-phase kit vs. cover-consistency check: a loadout is cover-consistent only if every equipped item is — one inconsistent item (a silenced weapon under a day coat) flags the whole kit, the same "one tell condition catches it" logic §5's NPC `lies[{claim, tell}]` already uses, rather than averaging risk away across a kit.
- All three new classes reference `src/sim/`'s `MindModel`/`FatigueState` directly by name — completely unrestricted, since `tools/percept_truth_boundary_lint.py` only scans `src/percept/`, and the hub isn't part of the percept/truth boundary at all.
- **18 new tests** across 4 files (`test_hub_calendar` 5, `test_mind_dashboard` 5, `test_loadout_item` 1, `test_loadout` 7), bringing the suite to 484 tests. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py`/`tools/dlgc.py` locally before commit (143 `.gd` files including the 8 new ones).

**Scoping decision — no block-level scheduling, no org board, no Sova sessions, no save integration yet:** `HubCalendar.Block` exists as data with nothing reading it — a real day/night schedule UI where the player allocates evening/night/morning blocks across competing activities is Pass 19's job once there's a UI to allocate them in. The org board (§4.11: "corkboard-language relationship/suspicion map... fed by day-phase observation") would read `SuspicionGraph`/`GossipSim` (Pass 16) directly, but no rendering or day-phase-observation-to-suspicion-entry pipeline exists yet to feed it — that's real content/UI work, not a data-model gap this pass could close on its own. Dr. Sova sessions (§4.11: moral-injury decay scaled by disclosure) already have their math built (`MoralInjuryState.apply_sova_session()`, Pass 11) and their dialogue shape started (Pass 15's DSL, `prologue_sova.dlg`) — wiring an actual Sova-session scene to that decay call is a natural next integration once real hub content exists, not invented here. `GameStateStore`/`SaveSystem` (Pass 2) integration — persisting `HubCalendar.current_day`, a `MindModel`, and a `Loadout` across sessions — is also left for whenever the hub is wired into an actual save/load flow.

**Files added:** `src/hub/{hub_calendar,mind_dashboard,loadout_item,loadout}.gd`, `tests/unit/{test_hub_calendar,test_mind_dashboard,test_loadout_item,test_loadout}.gd`.

**Files changed:** `docs/roadmap.md` (M4 Hub v1 item checked off with scope notes).

**Confirmed green (commit `1ca88eb`):** CI run [29686979813](https://github.com/DanielDmas/Afterimage/actions/runs/29686979813) — both jobs `success`. Test log shows the literal `ALL PASSED (484/484)` with zero `[FAIL]` lines; confirmed all 5 `test_hub_calendar.gd` cases (including the AC's exact sleep-debt-week trace) and all 5 `test_mind_dashboard.gd` cases ran and passed by name in the raw log text. Lint job (`gdlint`, `gdformat --check`, `percept_truth_boundary_lint.py`, `content_validator.py`, `dlgc.py`) also green.

---

## Pass 19 — UI shell (Theme resource, placeholder screens) + accessibility scaffolding

**Delivered:** the UI shell's data layer — a theme palette, motion constants, real accessibility settings state, and a "placeholder screen" abstraction — in a new top-level `src/ui/` directory, with a deliberate architectural choice explained below: no hand-authored Godot `.tscn`/`.tres` files this pass.

- **The central scoping decision, made before writing anything:** this is the first pass touching UI at all, and every prior pass has stayed within `extends RefCounted` GDScript classes — no Node, no scene tree, no `.tscn`/`.tres` resource files (tech_guidelines §2.2: "sim-core classes must not depend on the scene tree"). Godot's `.tscn`/`.tres` format is a real serialization format with its own header/ID/reference conventions; hand-authoring one blind, with no Godot editor or renderer in this sandbox to load and verify it, is exactly the kind of untested-in-this-sandbox Godot behavior this codebase has avoided since Pass 6's static-typing gotcha and Pass 9's `RefCounted` instance-id gotcha — except a malformed scene file wouldn't just fail a test, it could break CI's "Import project" step for the *entire repo*. So "Theme resource" and "placeholder screens" are built as real, tested GDScript data — a `Theme` resource and actual `Control`-node scenes are deferred to whenever a real Godot editor session exists to author and verify them against.
- **`src/ui/theme_palette.gd`** (`ThemePalette`) — art_direction §5's ten hex color anchors (carbon paper, folder ochre, sodium orange, etc.) as named `String` constants, plus `color()` wrapping Godot's built-in `Color(hex_string)` parser. A future real `Theme` resource sources its colors from these named constants, not the reverse, so the ten values are spelled out exactly once.
- **`src/ui/motion_constants.gd`** (`MotionConstants`) — art_direction §7's motion standards (120 ms micro-interactions, 180 ms panel transitions, 300 ms scene transitions, `cubic_out`/`cubic_in_out` easing names, a 100 ms input-acknowledgement budget) as named constants for a future UI's tween calls.
- **`src/ui/accessibility_settings.gd`** (`AccessibilitySettings`) — real, tested state for several of ux_charter §4's committed inventory items: stepped UI scale and subtitle size (4 steps each, per spec), and — the one item with a real mechanism already built to gate — Clarity Mode's actual on/off toggle. `clarity_flags_for()` is the literal integration `ClarityMode.active_flags()`'s own Pass 10 docstring named ("the on/off toggle itself belongs to a settings system (Pass 19's UI shell + accessibility scaffolding)"): disabled, it returns nothing at all, rather than flags nobody reads. Also carries the Ground hold-vs-toggle input-mode preference (§4.16) — `GroundState` (Pass 10) already accepts a plain "requested this tick" bool agnostic to how it was derived, so this setting has a real, if not-yet-wired, consumer contract waiting for it.
- **`src/ui/screen_spec.gd`** (`ScreenSpec`) and **`src/ui/mind_dashboard_screen.gd`** (`MindDashboardScreen`) — a "placeholder screen" as data (title + labeled rows) rather than a scene, with a real `screen_reader_text()` generator (ux_charter §4: "screen-reader on all paperwork/menu screens") built from the spec's own data since there's no scene tree to traverse yet. `MindDashboardScreen.build()` is the first concrete tie between Pass 18's `MindDashboard`, this pass's `AccessibilitySettings` ("numbers togglable in options"), and the screen-spec shape — a real, tested, three-system integration with zero new Godot-runtime risk.
- **22 new tests** across 5 files (`test_theme_palette` 4, `test_motion_constants` 3, `test_accessibility_settings` 7, `test_screen_spec` 4, `test_mind_dashboard_screen` 4), bringing the suite to 506 tests. One first-use-in-this-codebase Godot API call (`String.join()`) was caught and replaced with a plain string-concatenation loop before commit — not because `.join()` is known to be wrong, but because it had zero prior CI-proven usage anywhere in this codebase and a manual loop carries zero doubt, matching the same risk-aversion that drove the whole pass's `.tscn`-avoidance decision. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py`/`tools/dlgc.py` locally before commit (153 `.gd` files including the 10 new ones).

**Scoping decision — no real Godot scenes, no difficulty/psych sliders, no art-dependent accessibility items:** every "screen" this pass produces is data (`ScreenSpec`), not a Control-node tree — real scene authoring needs an actual Godot editor session to verify against, which this sandbox cannot provide; building one blind risked breaking CI's project-wide import step for a payoff (an unverifiable visual result) with no way to confirm it was worth the risk. Difficulty and psychological-content sliders (roadmap's M6 AC) need real missions to tune against, which don't exist yet. Several ux_charter §4 inventory items (colorblind-safe *rendering* — the double-coding presentation, not just a toggle bit; photosensitivity-safe rendering; subtitle plates; rumble; brightness/audio calibration; glyph set override) need real art/audio assets this 20-pass engineering arc has never produced (dev_log's own intro: "no real art, music, or voice work happens in these passes") — their *settings state* exists here where a real mechanism already permits it (Clarity Mode), and is honestly absent where it doesn't.

**Files added:** `src/ui/{theme_palette,motion_constants,accessibility_settings,screen_spec,mind_dashboard_screen}.gd`, `tests/unit/{test_theme_palette,test_motion_constants,test_accessibility_settings,test_screen_spec,test_mind_dashboard_screen}.gd`.

**Files changed:** `docs/roadmap.md` (M6 accessibility-pass item annotated with Pass 19's foundational work, not checked off).

**Confirmed green (commit `2c692c7`):** CI run [29687305247](https://github.com/DanielDmas/Afterimage/actions/runs/29687305247) — both jobs `success`. Test log shows the literal `ALL PASSED (506/506)` with zero `[FAIL]` lines; confirmed all 4 `test_theme_palette.gd` cases (including the first-ever `Color(hex_string)` parsing anywhere in this codebase) and all 4 `test_mind_dashboard_screen.gd` integration cases ran and passed by name in the raw log text. Lint job (`gdlint`, `gdformat --check`, `percept_truth_boundary_lint.py`, `content_validator.py`, `dlgc.py`) also green. Choosing data over `.tscn`/`.tres` files paid off exactly as reasoned — nothing here risked the project-wide import step.

---

## Pass 20 — Integration: playable prologue stub; full-suite green; 20-pass arc closed out

**Delivered:** the capstone this whole 20-pass arc was building toward — one coherent, scripted scenario tying together the Ground verb, the dialogue pipeline, a real distortion, replay reconstruction, and the debrief system, all in a single tested run.

- **`src/integration/prologue_stub.gd`** (`PrologueStub`) — a new top-level directory (the one place that legitimately depends on `TruthSim`, `DialogueRunner`/`InterruptMemory`, `SubtitleDrift`/`PerceptRenderer`, `ReplayTheater`/`OpTimelineSpan`, and `ClaimDrafter`/`DebriefLedger` all at once), staged as four methods matching the roadmap's own phrasing exactly:
  1. **`run_ground_tutorial()`** — holds Ground for exactly `GroundState.DURATION_TICKS` through a real `TruthSim`, producing a genuine Ground-verb completion (Pass 10), not a stand-in.
  2. **`play_scripted_misheard_line()`** — advances a real compiled-shape dialogue graph (transcribed from `tools/dlgc.py`'s actual output against `content/dialogue/prologue_sova.dlg`, the same discipline Pass 15's own tests used) down the "footsteps" branch, then renders that tick's percept view through a real `SubtitleDrift` + `PerceptRenderer` — proving for the first time that a distortion op can act on genuine dialogue-shaped data, not just a synthetic snapshot. The node's structured claim grant lands in `InterruptMemory` automatically, exactly as Pass 15 built it to.
  3. **`build_mini_theater()`** — reconstructs the exact scripted tick via `ReplayTheater`, with an `OpTimelineSpan` marking the `SubtitleDrift`'s one-tick activity — the same machinery Pass 14 proved standalone, now exercised against a real scripted run instead of a synthetic movement replay.
  4. **`draft_and_submit_debrief()`** — drafts a `Claim` from exactly what `InterruptMemory` recorded the player asserting ("heard footsteps"), and submits it As-Seen against the real ground truth ("there was no one in that hall") — the quiet knife, played out for real: an honest claim, still false, because that's what the player believed. `run_full_stub()` runs all four stages in sequence and returns everything a real (still-unbuilt) UI would eventually bind to.
- **One more small, real caution paid off before commit:** `ReplayTheater.new()`'s `op_timeline` parameter had only ever been called with a pre-declared typed `Array[OpTimelineSpan]` variable in Pass 14's own tests, never an inline array literal — so this pass built one explicitly (`var spans: Array[OpTimelineSpan] = [span]`) rather than assume the untested call shape would coerce correctly, the same standard applied to `ClaimDrafter.draft_from_perceived_events()` (which *did* already have CI-proven inline-literal precedent from Pass 17, so no change was needed there). A content-authoring mistake was also caught before committing: the mock dialogue's "drift" text was initially identical to its true text, which would have proven nothing about the distortion actually distorting anything — fixed to a genuinely different misheard line.
- **6 new tests** in `test_prologue_stub.gd`, each stage tested independently plus one end-to-end run through `run_full_stub()`, bringing the suite to **512 tests total**. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py`/`tools/dlgc.py` locally before commit (155 `.gd` files including the 2 new ones).
- **Final full-repo review before closing the arc:** confirmed no stray or orphaned files (`git status` showed only this pass's own additions), confirmed `src/`'s nine directories (`core`, `sim`, `percept`, `dialogue`, `social`, `debrief`, `hub`, `ui`, `integration`) hold 69 source files against 78 test files (more test files than source files is expected and already established — Pass 6 first split a test file to stay under `gdlint`'s 20-public-method cap, and several later passes did the same), and re-ran every CI-equivalent local check one final time before this entry was written.

**What's left for post-Pass-20 (real art/audio/content authoring, M5–M7):** this 20-pass arc was always scoped as engineering only — "no real art, music, or voice work happens in these passes" (this table's own header note, since Pass 1). What remains, honestly, by milestone:
- **M2/M3 tail:** a real distortion shader library + photosensitivity caps (needs actual shader authoring); the Grounding reveal's final VFX art pass (needs a human blind-AB playtest to call "beautiful" — not an engineering AC); the substance model (§4.4.5's stimulant/alcohol/sedative tradeoffs — deferred every pass since Pass 11 for lack of a real multi-day hub calendar to hang it on, which now exists as of Pass 18, so this is genuinely unblocked for a future pass, just never picked up).
- **M4 tail:** day-phase verbs beyond dialogue (converse/eavesdrop/observe/plant/lift/photograph/small talk — Pass 16's `SuspicionGraph`/`GossipSim` and Pass 15's `DialogueRunner` are the load-bearing pieces these would call into, both real); the journal/camera/evidence system (§4.17, unbuilt); Content validator v1's fuller AC (ID resolution/reachability/calendar lint — Pass 13 laid the schema-validation foundation, this needs real multi-mission content to validate across).
- **M5–M7 (real production):** an actual first mission ("Induction") in full; the remaining six `DistortionOp` classes and four AI archetypes (the taxonomy and archetype *shape* are done — `FairnessAuditor`'s duck typing was deliberately built to accept them sight-unseen, Pass 12 — only the concrete classes are missing); Theater v1's UI (the `ReplayTheater` data model underneath it is done, Pass 14); adaptive music; the naming-collision decision; Steam Deck profiling (needs real hardware); full slice production (prologue, two more missions, hub cast dialogue to depth); the full accessibility pass (`AccessibilitySettings`' state layer is real, Pass 19 — the remaining inventory items need real art/audio assets and an actual options screen); localization; two rounds of human playtesting; a sensitivity review; marketing/packaging; and the slice release gate itself (100% green determinism corpus over 14 consecutive nights, all bots finishing all missions, ux_charter targets met externally).
- **The throughline every one of the above shares:** none of it is a *mechanism* gap. Every remaining item either needs a human judgment call this sandbox structurally cannot make (is the reveal beautiful? is the Steam Deck framerate acceptable? did external playtesters enjoy it?), a real Godot editor session this sandbox does not have (to author and verify `.tscn`/`.tres` files, a real `Theme` resource, actual Control-node screens), or real art/audio/voice assets this 20-pass engineering arc was never scoped to produce. Every mechanism these depend on — the Predicate language, TruthSim, the percept/truth boundary, the Mind Model, the Distortion Director, the content pipeline, the dialogue DSL, the social graph, Claims/Provenance, the hub calendar, the UI shell's data layer, and now this integration stub — is built, tested, and green.

**Files added:** `src/integration/prologue_stub.gd`, `tests/unit/test_prologue_stub.gd`.

**Files changed:** none outside this entry — no roadmap checkbox maps cleanly onto "Pass 20" the way every prior pass's work did, since Pass 20 was always the integration/closing pass rather than a new roadmap AC of its own.

**Confirmed green (commit `1f85ca5`):** CI run [29687655484](https://github.com/DanielDmas/Afterimage/actions/runs/29687655484) — both jobs `success` (`GDScript style lint` — `gdlint`, `gdformat --check`, the percept/truth architecture boundary, content package schema validation, and dialogue DSL compile check, each individually `success`; `Unit test suite (headless Godot)` — `success`). Test log shows the literal `ALL PASSED (512/512)` with zero `[FAIL]` lines; confirmed all 6 `test_prologue_stub.gd` cases ran and passed by name in the raw log text — `test_run_ground_tutorial_completes_the_ground_verb_exactly_once`, `test_play_scripted_misheard_line_distorts_percept_but_never_truth`, `test_scripted_line_records_the_players_claim_in_interrupt_memory`, `test_mini_theater_reconstructs_the_exact_drift_tick`, `test_debrief_produces_a_false_delta_for_the_honest_but_mistaken_claim`, and the full end-to-end `test_run_full_stub_ties_every_stage_together`, which exercises the entire `TruthSim` + `DialogueRunner`/`InterruptMemory` + `SubtitleDrift`/`PerceptRenderer` + `ReplayTheater` + `DebriefLedger` pipeline together for the first time, verified only by hand-tracing before this push. **Pass 20 is closed. The 20-pass engineering arc is closed.**

---

## The 20-pass arc: closed

Twenty passes, 512 tests, zero known-broken CI runs left unresolved (every real failure — Pass 6's three rounds, Pass 7's off-by-one, Pass 9's `RefCounted` instance-id surprise, Pass 11's rounding-premise mistake, Pass 13's `JSON.parse_string()` float gotcha — was diagnosed from actual CI log text and fixed forward, never patched around). The engineering discipline that made this possible, restated once, plainly, since it won't be repeated pass-by-pass again: verify any real arithmetic externally before porting it; never rely on Dictionary iteration order; keep the percept/truth boundary enforced both structurally and by static lint; test with hand-built fixtures matching a real format when the real pipeline doesn't exist yet to produce one; and when in doubt about an untested Godot behavior, choose the version that doesn't need to be tested at all. `docs/roadmap.md` and this file are both current as of this entry; `README.md`'s progress paragraph reflects all 20 passes. What comes next is real production, not more engineering passes — and everything it needs to stand on is already here.

**Final CI confirmation:** Pass 20's own CI run ([29687655484](https://github.com/DanielDmas/Afterimage/actions/runs/29687655484), commit `1f85ca5`) is green — both jobs `success`, test log reading the literal `ALL PASSED (512/512)`, zero `[FAIL]` lines, all 6 capstone integration tests passing by name. There is no Pass 21. The arc ends here, confirmed green on real CI infrastructure, not just by local reasoning.

---

## Post-arc: bug audit + Substance model (§4.4.5)

The 20-pass arc is closed (above); this is genuine follow-up work requested after closure, not a renumbered Pass 21 — no new numbered pass is opened, and none of the arc's own entries above are revised.

**Independent bug audit.** Before adding anything, a fresh read-only pass re-verified the highest-risk areas this codebase has ever flagged as risky (Q16.16 arithmetic, the PRNG, every Mind Model formula, the Director's budget math, the Fairness Charter checks, suspicion decay, the hub calendar, the debrief ledger, the Pass 20 integration, and every `.keys()` call site for a Dictionary-order violation) — independently re-deriving the PRNG (splitmix64 + xoshiro128**) and the §4.3 budget worked example from scratch in Python and cross-checking both against the pinned test vectors, rather than just re-reading this codebase's own prior verification claims. Two items surfaced, both resolved without touching tested game-logic behavior:
1. **`master_plan.md` §4.4.3's "a knowing lie in debrief +3 (+5 if it conceals a death)"** read ambiguously as either a flat replacement (+5 total) or additive (+8 total) — `moral_injury_state.gd`'s `gain_knowing_lie_in_debrief()` (Pass 17) already implements the flat-replacement reading, deliberately, with its own dedicated test (`test_knowing_lie_costs_less_when_it_conceals_nothing`). Rather than reinterpret and silently change already-tested game-balance behavior based on an uncertain reading of prose I authored myself under this project's creative-freedom mandate, the prose itself was clarified instead: §4.4.3 now reads "+3, or +5 instead (not additively) if that lie conceals a death" — a documentation fix, zero behavior change.
2. **`FixedMath.mul()` floors (arithmetic `>>`) while `FixedMath.div()` truncates toward zero (GDScript's `/`)** — the same category of footgun Pass 4's `CollisionGrid._floor_div()` was built to avoid. In `swept_collision.gd`'s `circle_vs_aabb_swept()`, this asymmetry could in principle flip a `t_exit < 0` early-out at an exact-grazing collision — but only when a per-tick movement delta exceeds 65,536mm (65.5m in 1/30s, ~1966 m/s). `movement_profile.gd`'s real speeds top out at 80mm/tick (sprint), over 800× too slow to ever reach the boundary where the two roundings disagree. Verified the exact bound by hand rather than assume the audit's own "narrow trigger" hedge was right; confirmed unreachable, left the code untouched — changing well-tested, CI-green collision arithmetic for a mathematically-real but physically-unreachable case would have been exactly the kind of unnecessary risk this project's own principles warn against.

No other findings met the bar for "concrete, reachable, and wrong."

**Substance model (§4.4.5).** The one open roadmap.md item Pass 20's own closing note flagged as "genuinely unblocked... just never picked up" now that Pass 18's hub calendar exists: `src/sim/substance_model.gd` (`SubstanceModel`) implements stimulant/alcohol/sedative tradeoffs by sequencing calls into the existing Mind Model classes — the same "sequence calls, don't duplicate the math" discipline `HubCalendar` established, not a rewrite of anything. Two small, generic, per-variable primitives were added because no existing class had them: `FatigueState.set_temporary_floor()`/`clear_temporary_floor()` (a raise-only floor, the same contract `AcuteStressState.apply_hub_rest_floor()` already uses) and `AcuteStressState.set_gain_multiplier_fx()`/`clear_gain_multiplier()` (applies only to the six `gain_*` methods via a new `_add_gain_fx()` helper — never to `relieve_ground_completed()` or the per-tick decay, verified by a dedicated test). `DistortionDirector` gained `authorize_free_tier()` for alcohol's "one random tier-1 op authorized for the scene": bypasses the budget/weight gate entirely but still respects the density cap and still draws from the Director's own seeded stream, so determinism holds even for an authorized (not purchased) op — its record carries `"authorized": true` so Theater/debrief disclosure (Charter rule 8) never conflates it with a real purchase. `IdentityStrainState` gained `gain_alcohol_use()` (+2), matching every other named gain there.

Interpretation calls made explicit in `substance_model.gd`'s own class doc, since §4.4.5 states tradeoffs in prose, not exact mechanics: "fatigue floor +10 for 3 days" is read as a flat floor (minimum rises from 0 to 10, not "current value + 10") because it's simpler to test and disclose and costs the same honesty either way; "suppress all fatigue effects tonight" is the caller's job (skip that night's own `HubCalendar` billing) since `SubstanceModel` only starts the 3-day aftermath, verified to persist across exactly 3 `advance_day()` calls before clearing. Alcohol's "-1 suspicion per drink" is returned as a plain delta with no consumer yet — there's no scene/NPC wiring to apply it to, the same "real number now, real call site once a consumer exists" deferral used since Pass 5's `FLEE` `threat_level`. Sedatives force a full sleep block; skipping that night's moral-injury decay is the caller's job (simply don't call `decay_passive_daily()`).

**17 new tests** (`test_substance_model.gd`: 8; plus additions to `test_fatigue_state.gd`, `test_acute_stress_state.gd`, `test_identity_strain_state.gd`, `test_distortion_director.gd`), bringing the suite to **529 tests**. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py`/`tools/dlgc.py` locally before commit (157 `.gd` files including the 2 new ones).

**Scoping decision — no fairness-auditor dependency-dominance check yet:** §10's architecture note also commits the auditor to catching "dependency-dominant substance configurations" — that needs authored mission-level substance-availability content (which stimulant/alcohol/sedative options a given mission or hub day actually offers) to lint across, and no content schema or fixture mission offering them exists yet. Real content-pipeline work, not a mechanism gap — tracked honestly in `roadmap.md` rather than claimed done.

**Files added:** `src/sim/substance_model.gd`, `tests/unit/test_substance_model.gd`.

**Files changed:** `src/sim/fatigue_state.gd` (+temporary floor), `src/sim/acute_stress_state.gd` (+gain multiplier), `src/sim/identity_strain_state.gd` (+`gain_alcohol_use`), `src/sim/mind_model.gd` (docstring updated — substance modifiers are no longer categorically out of scope), `src/percept/distortion_director.gd` (+`authorize_free_tier`), `tests/unit/{test_fatigue_state,test_acute_stress_state,test_identity_strain_state,test_distortion_director}.gd`, `docs/master_plan.md` (§4.4.3 ambiguity resolved), `docs/roadmap.md` (substance-model item annotated with real progress, not checked off).

**Confirmed green (commit `a695cfd`):** CI run [29688497044](https://github.com/DanielDmas/Afterimage/actions/runs/29688497044) — both jobs `success` on the first push (`GDScript style lint` and `Unit test suite (headless Godot)`). Test log shows the literal `ALL PASSED (529/529)` with zero `[FAIL]` lines; confirmed all 8 `test_substance_model.gd` cases (including the exactly-3-day aftermath trace and the refresh-not-stack case) and every new case in `test_fatigue_state.gd`, `test_acute_stress_state.gd`, `test_identity_strain_state.gd`, and `test_distortion_director.gd` ran and passed by name in the raw log text. Lint job (`gdlint`, `gdformat --check`, `percept_truth_boundary_lint.py`, `content_validator.py`, `dlgc.py`) also green.

---

## Post-arc: first playable scene + Web export pipeline

Requested directly: "I want it playable today." This is the project's first Node-derived script and first `.tscn` scene — every prior pass (Pass 19's dev_log entry explicitly) avoided the scene tree because no local Godot editor exists in this authoring sandbox to visually verify one against. That constraint is unchanged; what changed is the goal. Since a scene genuinely can't be visually verified here, every Godot API this uses was checked against the real Godot 4.3 class docs (`docs.godotengine.org`, reachable from this sandbox even though `github.com`/`tuxfamily.org` — where the actual Godot binary and export templates live — are blocked by the org's egress policy) before being used: `Image.create_empty()` (not the deprecated `Image.create()`), `image.fill()`, `ImageTexture.create_from_image()`, `InputMap.action_add_event()`/`InputEventKey.physical_keycode`, every `KEY_*` constant, `RenderingServer.set_default_clear_color()`, and — the one that would have silently broken the entire goal — that Godot's Web export only supports the `gl_compatibility` rendering method (Forward+/Mobile need Vulkan/Metal/D3D12, unavailable in a browser). The project's `[rendering]` section was still set to `forward_plus` from Pass 1; changed to `gl_compatibility` (base and `.mobile` override) before this would have ever worked.

**`scenes/main.gd`/`scenes/main.tscn`** — a real graybox room (a perimeter-walled 12×8 cell room at 500mm cells, mirroring `tests/fixtures/graybox_room.gd`'s layout) driven by the actual `TruthSim` (Pass 3-7): a fixed 1/30s tick accumulator samples real input every elapsed tick (never once per rendered frame, matching the determinism contract), builds an `InputFrame` via `MovementProfile.resolve_delta()`, and calls `TruthSim.step()` — nothing about the simulation is faked for this. Rendering is `Sprite2D` nodes scaled from one shared 8×8 solid-color base texture per color (never per-node), chosen over `Control`-based `ColorRect` specifically to keep every position/scale calculation in one `Node2D`/`CanvasItem` coordinate model. Movement + collision was chosen as the first playable slice deliberately — it's the most heavily-tested subsystem in the whole codebase (150+ existing unit tests already prove `TruthSim`/`SweptCollision`/`CollisionGrid` correct) — combat, AI, the Mind Model, and distortions are not wired into this first scene.

**Input binding done in code, not in `project.godot`'s text format.** Pass 6 left every combat-verb `InputMap` action with an empty `events` array specifically because hand-authoring an `InputEventKey` resource literal blind (no editor to generate/verify one) risked a wrong numeric keycode with nothing to catch it. `scenes/main.gd`'s `_bind_movement_keys()` sidesteps that risk entirely: it binds real keys (arrows + WASD, Shift for sprint) at runtime via `InputMap.action_add_event()` using GDScript's named `KEY_*` constants — there is no numeric literal to get wrong, since the compiler resolves the constant names, not a hand-typed number.

**Export pipeline.** This sandbox cannot download the Godot editor binary or export templates at all (org egress policy blocks `github.com`/`tuxfamily.org` — confirmed via the sandbox's own proxy status endpoint, not assumed) and has no display to run a local export even if it could. GitHub Actions runners have full internet access and already download Godot successfully in `ci.yml`'s `test` job, so the export itself happens there instead: a new, independent `.github/workflows/export-web.yml` downloads the Godot editor + Web export templates, installs the templates via self-contained mode (a `._sc_` marker file next to the binary, confirmed against `docs.godotengine.org`'s data-paths page, keeping everything inside the already-cached workspace-relative directory rather than `~/.local/share/godot/`), imports the project, and runs `--export-release "Web" build/web/index.html` against a new `export_presets.cfg` (thread support off, to avoid requiring special cross-origin-isolation headers from whatever static host ends up serving it). The build is uploaded two ways: as a plain workflow artifact (always available, zero dependency on repo settings) and via `actions/upload-pages-artifact` + a separate `deploy-pages` job for GitHub Pages — the latter will fail cleanly if Pages isn't enabled for this repo (Settings → Pages → Source: GitHub Actions), which is a one-time manual toggle this session's tools cannot flip remotely; the artifact upload is the guaranteed-available fallback regardless.

`export_presets.cfg` was previously gitignored by Pass 1's own `.gitignore` (a standard Godot default) — removed that exclusion with an explanatory comment, since the file holds no secrets (confirmed against Godot's own docs: only `.godot/export_credentials.cfg`, already covered separately, is the sensitive half) and CI genuinely needs it tracked to know the Web preset exists.

**Honest uncertainty, stated plainly:** unlike every other system in this codebase, none of this can be verified by a local test run — there is no headless equivalent of "does the game render and respond to input" the way `tests/run_tests.gd` verifies logic. The individual facts above (API names, the renderer requirement, the self-contained-mode template path convention) were checked against primary sources wherever this sandbox's network access allowed it; the composition of all of them into one working export is only provable by the export actually running, which happens in CI, not here. If it fails, the fix-forward loop is the same one this whole project has used for every genuinely untestable-locally piece of Godot behavior since Pass 1's own class-name cache surprise: real log text, diagnosed, fixed, re-verified.

**Files added:** `scenes/main.gd`, `scenes/main.tscn`, `export_presets.cfg`, `.github/workflows/export-web.yml`.

**Files changed:** `project.godot` (`run/main_scene` set; `renderer/rendering_method` changed from `forward_plus` to `gl_compatibility`, required for Web export to run at all), `.gitignore` (`export_presets.cfg` un-ignored, `build/` ignored).

**Confirmed via real CI (commit `df8d282`):** [run 29689158562](https://github.com/DanielDmas/Afterimage/actions/runs/29689158562) — the export itself genuinely works: `Build HTML5/Web export` job `success` end to end, including the real `--export-release "Web" build/web/index.html` step and both artifact uploads. Every uncertain fact this pass relied on turned out correct on the first try: `gl_compatibility` was the right rendering method, self-contained mode's `._sc_`-marker template path was right, `export_presets.cfg`'s hand-authored preset was accepted without complaint. ci.yml's own `test`/`lint` jobs also stayed green on this same commit — the new scene and rendering-method change didn't regress anything. The one thing that did fail, exactly as anticipated in this entry's own risk note: `deploy-pages` — `Error: Failed to create deployment (status: 404)... Ensure GitHub Pages has been enabled: https://github.com/DanielDmas/Afterimage/settings/pages`. That's a one-time repository-settings toggle (Settings → Pages → Build and deployment → Source: **GitHub Actions**) this session's tools have no access to flip remotely; the plain `afterimage-web-build` workflow artifact is available as a guaranteed-today fallback regardless of that toggle, exactly as designed.

---

## Post-arc: full code review, blank-spot inventory, forward plan

A review-only pass (no code changed): a fresh, verified-by-grep read of the whole repo — including the brand-new `scenes/` + export pipeline — hunting specifically for *seams between individually-green systems*, since the pass-by-pass discipline verified each piece in isolation far better than it verified their composition. Full findings, enhancement possibilities, and a proposed P1–P5 execution order live in **`docs/review_and_forward_plan.md`**; the headlines:

1. **F1 (top finding):** the Fairness Auditor cannot audit the content pipeline's real decks — `DeckEntry`/`mission.json` carry no `fairness_tags`/`dramatic_intent` and nothing anywhere converts a purchased `op_class` string into a live `DistortionOp`. Master_plan §10's "auditor runs on every content commit" is currently unsatisfiable against real content. One `OpFactory` closes both halves.
2. **F2–F4:** CI lints `src/ tests/` but not `scenes/`; the playable scene renders truth-layer state directly (the percept seam must flip before the first distortion lands); the scene discards its `InputFrame`s instead of recording a `ReplayLog` — the determinism contract's "the replay IS the save" is unexercised in the one place real human input now exists.
3. **F5:** the social-consequence event chain (`GroundObserved` → suspicion → gossip) is publish-only end to end — zero subscribers anywhere, never once run even in a test.
4. **F7–F9:** the export workflow's branch filter dies on merge; dialogue content has no runtime path (dlgc's output is neither committed nor loadable in-game); the determinism corpus still hashes Pass 2's disposable StubSim, four passes after its deferral reason expired.
5. Plus: Ground (the signature verb) is unbound in the playable build, no touch/gamepad input, settings don't persist, zero `tr()` localization calls, and `ReplayTheater`'s full-tick cache won't scale to long missions.

The plan's centerpiece recommendation: a **"thesis demo" slice** — Ground verb + one scripted SubtitleDrift + a dual-view reveal, in the browser build — every mechanism for it already exists and is tested (PrologueStub proves the composition); it's the difference between "a box moves in a box" and the actual game's pitch being playable by a stranger in two minutes.

**Files added:** `docs/review_and_forward_plan.md`. **Files changed:** this entry.

---

## Post-arc: the thesis demo (review's P1/P2/P3 delivered)

Executes the review's own recommended order against the playable scene: quick trust repairs, the percept-seam flip, then the thesis demo itself.

**P1 — trust repairs:**
- `ci.yml`'s lint job now covers `scenes/` (`gdlint`/`gdformat --check` both extended) — F2 closed; the new directory can no longer silently drift from the codebase's own style rules.
- `export-web.yml` now triggers on `main` too, not just this working branch — F7 closed (the export would otherwise have gone stale the moment this work merged).
- Added a real placeholder icon (`icon.png`, generated via Pillow — a simple two-color geometric mark using exactly the `ThemePalette` anchor hex values, not a logo attempt; real branding stays out of this engineering arc's scope) and wired `config/icon` — closes the empty-favicon half of F13.

**P2 — the percept seam:** `scenes/main.gd::_update_visuals()` now builds a truth snapshot via `TruthSim.capture_percept_snapshot()`, runs it through `PerceptRenderer.render()`, and renders the *percept* result — never raw truth directly. With no active op this is a no-op pass-through (nothing visually changes from Stage 1), but the seam is now structurally correct before any distortion needed to exist to prove it — F3 closed.

**P3 — the thesis demo:**
- **`src/integration/drift_encounter.gd`** (`DriftEncounter`) — a new, pure-logic (`RefCounted`, no scene-tree dependency) state machine: `WAITING → ACTIVE → REVEALED`, triggered by player proximity to a fixed point, reusing PrologueStub's own Cold Open beat verbatim ("Footsteps. Someone was there." vs. "Just the radiator, ticking.") rather than inventing new narrative content. `REVEALED` is reached either by Ground completing mid-`ACTIVE` (self-correcting immediately, `was_grounded = true`) or by a 5-second display window simply elapsing (`was_grounded = false`) — Charter rule 5 ("everything is disclosable… no secret permanent gaslighting") made concrete: grounding gets you the correction sooner, but disclosure isn't gated behind having grounded at all. Kept as its own testable class specifically so the state machine gets real unit coverage rather than living untested inside a Node script — **7 new tests** in `tests/unit/test_drift_encounter.gd`, including one that exercises it against the real `SubtitleDrift`/`PerceptRenderer` classes end to end, not just its own transitions in isolation. Suite now **536 tests**.
- **The Ground verb, wired end to end:** `project.godot` never declared a `"ground"` InputMap action at all (Pass 10 built the verb before Pass 6's InputMap existed for it) — `scenes/main.gd` now adds the action *and* binds Space to it at runtime (`InputMap.add_action()` + the same code-bound-keys approach Stage 1 used for movement, sidestepping the same "don't hand-author InputEventKey blind" risk). A `[Space] hold to Ground` HUD line shows live progress/count, sourced from a real `EventBus` subscription to `"GroundCompleted"` — F6 closed (Stage 1 never passed an `EventBus` into `TruthSim` at all, so even the events that were published reached nothing).
- **Every `InputFrame` is now recorded into a real `ReplayLog`** (F4 closed) — no longer discarded — and it's load-bearing, not just a completeness fix: the reveal panel is backed by a genuine `ReplayTheater` re-simulation of the *entire* recorded session, the first time this mechanism has ever been exercised against a long, freely-played session rather than a short fixed scenario (PrologueStub, Pass 20) or a synthetic one (Pass 14's own tests). It reconstructs the player's exact position at the drift's start tick from the replay alone and displays it next to the live values, as a genuine "this was really re-simulated" proof, not a cosmetic label.
- **The disclosure panel** ("THE AFTERIMAGE"): a `Control`/`ColorRect`/`Label` overlay (screen-space UI, appropriately Control-based — unlike the world-space Sprite2D walls/player) showing what was heard, what was true, whether it was grounded in the moment, and the replay-reconstructed confirmation. The tick loop freezes once revealed — this is a bounded demo with a clear ending, not an open session.
- **`String.join()` used for real, this time verified**: Pass 19 avoided it for lack of any prior-CI-proven usage in this codebase; checked directly against `docs.godotengine.org`'s `class_string.html` this pass — real method, takes `PackedStringArray` specifically (not a generic `Array`), so the reveal panel's line list is built as a `PackedStringArray` explicitly rather than assumed to auto-coerce.
- **Palette correctness fix, caught before commit:** the Stage-1 wall color was hand-picked and mislabeled "carbon paper" in its own comment — `CARBON_PAPER` (`#E8E0CE`) is actually a light cream, nothing like the dark brown that was used. Recomputed all four scene colors directly from `ThemePalette`'s real hex anchors (`municipal_green` for walls — an institutional government-building color that also fits 2004 Vranov thematically better than the invented brown, `sodium_orange` for the player — already correct, `night_base` for the background, `fluorescent_white` for panel text) rather than calling `ThemePalette.color()` (GDScript `const` initializers can't call static methods, per `acute_stress_state.gd`'s own established note) — closes part of F12.

**Deliberately not in this pass** (staying honest about scope, matching the review's own P4/P5 ordering): no AI actor (the review flagged this as an optional addition to P3, not its acceptance bar); no `OpFactory`/content-pipeline auditor wiring (F1, P4 — bigger, separate scope); no dialogue-runtime decision (F8, P4); determinism corpus still on `StubSim` (F9, P4); no `HubDayLoop`, settings persistence, touch/gamepad input, or localization (F5/F10/F11/F12 remainder, P5).

**Files added:** `src/integration/drift_encounter.gd`, `tests/unit/test_drift_encounter.gd`, `icon.png`.

**Files changed:** `scenes/main.gd` (percept seam, Ground verb, EventBus, ReplayLog, reveal panel, palette fix), `project.godot` (`config/icon`), `.github/workflows/ci.yml` (lint `scenes/`), `.github/workflows/export-web.yml` (trigger on `main`), `docs/review_and_forward_plan.md` (findings marked addressed).

**Confirmed green (commit `4937ff1`):** CI run [29700496567](https://github.com/DanielDmas/Afterimage/actions/runs/29700496567) — both jobs `success`. Test log shows the literal `ALL PASSED (536/536)` with zero `[FAIL]` lines; confirmed all 7 `test_drift_encounter.gd` cases ran and passed by name, including the end-to-end one exercising the real `SubtitleDrift`/`PerceptRenderer` composition. `gdlint`/`gdformat --check` now covering `scenes/` also green. The export workflow ([run 29700496566](https://github.com/DanielDmas/Afterimage/actions/runs/29700496566)) rebuilt successfully too — `Build HTML5/Web export` job `success` end to end with the new scene content — the overall run shows `failure` only because `deploy-pages` still 404s on the same pre-existing, already-reported cause (GitHub Pages not yet enabled for this repo); not a regression from this pass. The `afterimage-web-build` artifact is available from this run as before.

---

## Post-arc: OpFactory + the fairness auditor's real content commit (F1 closed)

Also fixed a real, pre-existing documentation bug found while adding this entry: an earlier "Confirmed green" note for the substance-model pass had been appended to the very end of this file instead of after its own section — moved to the correct place, no content lost.

The review's own top finding, and its hardest: `FairnessAuditor.validate()` requires entries exposing `fairness_tags`/`dramatic_intent` and passing `entry is DistortionOp`, but `DeckEntry`/`mission.json` carried none of that, and nothing anywhere turned a purchased `op_class` string into a live op. The genuinely hard part wasn't the plumbing — it was that the four real op classes have four genuinely different constructors (`SubtitleDrift(drifted_text, ...)`, `AudioSwap(target_source_id, swapped_tag, ...)`, `PhantomAudio(phantom_position, phantom_tag, ...)`, `PhantomEntity(phantom_position, entity_kind, phantom_facing_dir, ...)`), so a generic factory needs real per-class param extraction, not a one-line lookup.

- **`DeckEntry` gained an optional `params: Dictionary` field** (default `{}`, so all 23 existing call sites across the test suite are untouched) — deliberately still ignorant of what shape any op_class needs; only `OpFactory` knows that, matching `DistortionDirector`'s own "plain data, no op-class knowledge" discipline.
- **`MissionLoader`** passes `params` through untouched (no validation, no casting) — the same "op-class-agnostic" stance.
- **`src/percept/op_factory.gd`** (`OpFactory`) is the actual bridge: `build(entry: DeckEntry) -> DistortionOp`, one `_build_*` branch per implemented op class, asserting clearly (matching `MissionLoader`'s own "contract violation, not a runtime case to design around" stance) for any of the six not-yet-implemented taxonomy classes. Positions/directions are authored in JSON as `{"x":.., "y":..}` objects (JSON has no vector type); `int()` casts are load-bearing given Godot's number-is-always-float JSON parsing, the same documented gotcha `MissionLoader` already guards against. Settled a real content-authoring design question along the way: `AudioSwap.target_source_id` can't be a raw authored integer in any meaningful sense (actor IDs are runtime-assigned), so `OpFactory.PLAYER_SOURCE_ID = 1` documents and formalizes the one stable, always-true convention this codebase already has — `ActorRegistry._next_id` starts at 1 and `TruthSim._init()` always spawns the player before anything else can call `spawn_ai()`, so the player's actor ID is deterministically always 1, in every mission, forever.
- **`tools/schemas/mission.schema.json`** gained an optional, loosely-typed `params` object per deck entry — the hand-rolled Python validator has no conditional-schema support (its own module docstring), so real per-op-class shape checking is `OpFactory.build()`'s job, not the schema's; updated that module's own docstring too, since it had anticipated a different (weaker) design for this exact moment.
- **`content/missions/m00_stub/mission.json`** — the real, committed fixture — now carries real params for all four deck entries (a muffled gunshot, footsteps behind you, a misheard fallback order, an armed figure that isn't there), replacing the placeholder-shaped entries from Pass 13.
- **`tests/unit/test_mission_content_fairness.gd`** (9 new tests) is the actual "runs on every content commit" proof master_plan.md §10 promises: loads the real `mission.json` through the real `MissionLoader`, builds every entry through `OpFactory`, and runs `FairnessAuditor.validate()` against the result, asserting zero violations — not a hand-built fixture, the real committed content.

**Building the real proof found two real bugs the fixture-only auditor tests could never have caught:** `PhantomAudio` (Pass 9) is in `FairnessAuditor.PHANTOM_OP_CLASSES` but its `_init()` never declared the Charter rule-1 tag it structurally always satisfies (a phantom sound has no body to damage/block with); `PhantomEntity` never declared the rule-3 tag every op needs unconditionally. Both invisible until real op classes — not `_FakeOp` doubles with deliberately-complete tags — ran through the real auditor for the first time. Both fixed directly in the op classes (the correct fix — these ops genuinely comply, the tags were just incompletely declared), each with its own new regression test (`test_phantom_audio.gd`, `test_phantom_entity.gd`) beyond the integration test that caught them.

**14 new tests total** (9 in `test_mission_content_fairness.gd`, 2 in `test_deck_entry.gd`, 1 in `test_mission_loader.gd`, 1 each in `test_phantom_audio.gd`/`test_phantom_entity.gd`), bringing the suite to **550 tests**. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py`/`tools/dlgc.py` locally before commit.

**Files added:** `src/percept/op_factory.gd`, `tests/unit/test_mission_content_fairness.gd`.

**Files changed:** `src/percept/deck_entry.gd` (+`params`), `src/percept/mission_loader.gd` (+params passthrough), `src/percept/phantom_audio.gd`/`phantom_entity.gd` (fairness-tag bug fixes), `tools/schemas/mission.schema.json` (+`params`), `tools/content_validator.py` (docstring corrected), `content/missions/m00_stub/mission.json` (real params for all four entries), `tests/unit/{test_deck_entry,test_mission_loader,test_phantom_audio,test_phantom_entity}.gd`, `docs/review_and_forward_plan.md` (F1 marked closed).

**Confirmed green (commit `eaf9e73`):** CI run [29823584217](https://github.com/DanielDmas/Afterimage/actions/runs/29823584217) — both jobs `success`. Test log shows the literal `ALL PASSED (550/550)` with zero `[FAIL]` lines; confirmed all 9 `test_mission_content_fairness.gd` cases ran and passed by name, including the load-bearing `test_real_stub_mission_deck_builds_and_passes_the_fairness_auditor`, plus both new regression tests (`test_declares_rule_1_and_rule_3_fairness_tags` in `test_phantom_audio.gd`, `test_declares_rule_3_fairness_tag` in `test_phantom_entity.gd`) and the `test_deck_entry.gd`/`test_mission_loader.gd` additions. `gdlint`/`gdformat --check`/`percept_truth_boundary_lint.py`/`content_validator.py`/`dlgc.py` all green too — the schema change and real mission.json content edit didn't break anything.

---

## Post-arc: determinism corpus re-pointed at real TruthSim (F9 closed)

Pass 2's `StubSim` was always explicitly disposable — its own class doc said "delete this file once TruthSim lands and point the corpus tests at that instead" — deferred at Pass 3 on the reasoning that a movement-only TruthSim with no walls had no more interesting divergence surface than the toy already proved against. That reasoning expired once Pass 7 gave TruthSim real collision, AI, and (Pass 10) the Ground verb; the review's F9 flagged it as still open four passes later.

- **`tests/fixtures/stub_sim.gd` deleted.** Replaced by **`tests/fixtures/truth_sim_digest.gd`** (`TruthSimDigest`): re-simulates a `ReplayLog` through a real `TruthSim` in the same graybox room `scenes/main.gd` plays in (its own copy of the wall-building logic — a test fixture depending on the playable scene, or vice versa, would be backwards), then hashes an explicit, hand-formatted state string built from `capture_percept_snapshot()`'s real fields (tick, every actor's position/hit points/alive flag, `ground_use_count()`) — not `JSON.stringify()` on a Dictionary containing `Vector2i` values, which this codebase has no prior CI-proven usage of and no reason to assume round-trips predictably.
- **All three `tests/corpus/*.json` fixtures rewritten**, not just re-hashed against the same trivial content: `run_001.json` (50 frames) is pure interior movement; `run_002.json` (80 frames) walks straight into the real east wall and keeps pressing past it — the fixture exists specifically to exercise `SweptCollision`'s real clamping behavior under re-simulation, not just position summation; `run_003.json` (120 frames) runs a diagonal approach to a corner, then a complete 75-tick Ground hold (`GroundState.DURATION_TICKS`) to completion, then a final movement stretch — the first corpus fixture whose digest is sensitive to the Ground verb's own tick-based state machine, not just position.
- **Self-consistency stayed the verification method**, deliberately unchanged from Pass 2: no fixture's digest value is hand-computed or hard-coded anywhere in the test suite — `test_replaying_a_fixture_twice_produces_identical_digests` is still the actual proof, and still the right one. Hand-writing a "golden" expected SHA-256 string here would mean trusting my own by-hand trace of `SweptCollision`'s Q16.16 arithmetic across dozens of ticks over trusting the mechanism itself — exactly the risk Pass 2's own dev_log entry already reasoned through and rejected.
- Updated two stale doc references that had drifted true out from under this change: `src/sim/combat_resolver.gd`'s own comment cited "the corpus staying on StubSim" as a still-current analogy (no longer accurate), and `docs/roadmap.md`'s M0/M1 scope notes both described the corpus as still StubSim-based.

No new test count change — this closes a real gap in what the *existing* corpus tests actually exercise, not a new feature with new test surface. Suite stays at **550 tests**, all now meaningfully exercising real `TruthSim` behavior instead of a toy accumulator.

**Files added:** `tests/fixtures/truth_sim_digest.gd`.

**Files changed:** `tests/fixtures/stub_sim.gd` (deleted), `tests/corpus/{run_001,run_002,run_003}.json` (rewritten), `tests/unit/test_determinism_corpus.gd` (points at `TruthSimDigest`, updated frame-count assertions and class doc), `src/sim/combat_resolver.gd` (stale comment corrected), `docs/roadmap.md` (M0/M1 scope notes updated), `docs/review_and_forward_plan.md` (F9 marked closed).

**Confirmed green (commit `9b34792`):** CI run [29823977637](https://github.com/DanielDmas/Afterimage/actions/runs/29823977637) — both jobs `success`. Test log shows the literal `ALL PASSED (550/550)` with zero `[FAIL]` lines; confirmed all six `test_determinism_corpus.gd` cases ran and passed by name against the real `TruthSim`-backed digest, including `test_all_three_fixtures_load_with_expected_frame_counts` (the rewritten 50/80/120-frame fixtures) and `test_replaying_a_fixture_twice_produces_identical_digests` (proving real wall-collision clamping and a full Ground-hold cycle re-simulate byte-identical). `gdlint`/`gdformat --check`/`percept_truth_boundary_lint.py`/`content_validator.py`/`dlgc.py` all green too.

---

## Post-arc: the suspicion pipeline runs end to end (F5 closed)

The designed pipeline — Ground observed → a suspicion entry → gossip propagation → (eventually) an org board — existed as three separately-tested segments (`TruthSim._resolve_ground_completion()` publishes `"GroundObserved"`; `SuspicionLedger`/`SuspicionGraph` track weighted entries; `GossipSim.propagate()` fans one out along gossip edges) that had never once run together, even in a test. Each individual "no consumer yet" deferral was reasonable at the time it was made; collectively they left a real, designed mechanic entirely disconnected.

**`src/social/ground_observation_bridge.gd`** (`GroundObservationBridge`) is the missing wiring — subscribes to `"GroundObserved"` on a real `EventBus`, and on receipt adds a `SuspicionLedger` entry and propagates it one `GossipSim` hop. The one real design gap the review's own suggested next step didn't call out: `TruthSim` identifies actors by sequential int ID (`ActorRegistry`); `SuspicionGraph` identifies NPCs by authored String id; nothing anywhere maps between the two namespaces. Rather than invent a convention neither system already commits to, the bridge takes that mapping as a constructor argument — a scene wires up which AI actor socially *is* which NPC, and an unmapped observer (a faceless patrol guard with no social identity) is silently skipped, not an error.

**`tests/unit/test_ground_observation_bridge.gd`** (4 tests) proves the full chain against real systems, not fixtures standing in for each other: a real `TruthSim`, a real `Sentry`-archetype AI actor positioned and facing so it genuinely sees the player (`VisionCone`/`LineOfSight`, not a mocked "can_see" flag), a full 75-tick Ground hold to completion, a real `SuspicionLedger` entry landing at the correct weight, and a real `GossipSim` propagation arriving at a confidant NPC on the correct future day (absent the day before, present the day of arrival — the ledger's own day-gating exercised for real, not asserted in isolation). A separate test confirms an AI with no NPC mapping is silently skipped rather than erroring, and another confirms Ground resolving with no observer at all leaves the graph untouched.

The org board itself (rendering this state) is real UI/content work, tracked separately in roadmap.md — this closes the *mechanism* gap, not the presentation layer.

**4 new tests**, bringing the suite to **554 tests**. Clean against `gdlint`/`gdformat --check`/`tools/percept_truth_boundary_lint.py`/`tools/content_validator.py`/`tools/dlgc.py` locally before commit.

**Files added:** `src/social/ground_observation_bridge.gd`, `tests/unit/test_ground_observation_bridge.gd`.

**Files changed:** `docs/review_and_forward_plan.md` (F5 marked closed).
